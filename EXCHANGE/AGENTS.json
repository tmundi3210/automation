{
  "_directive": "machine-facing agent registry; SINGLE WRITER: claude (integrator). Builders read this to learn identities, branch namespaces, exchange dirs, and static write scopes; tools/check_scope.sh and tools/gate_all.sh consume it. protocol_version must match the PROTOCOL-VERSION line in EXCHANGE/README.md and EXCHANGE/tasks.json.",
  "protocol_version": "3.2",
  "integration_branch": "claude/eager-wozniak-74rlgj",
  "integrator": "claude",
  "agents": {
    "claude": {
      "role": "integrator",
      "branch_globs": [
        "claude/*"
      ],
      "exchange_dir": "EXCHANGE/claude/",
      "static_write_scope": [
        "**"
      ],
      "notes": "sole merger; sole writer of specialists/, knowledge_base/, dist/, tools/, validators/, EXCHANGE/AGENTS.json, EXCHANGE/tasks.json; never commits to builder branches"
    },
    "grok": {
      "role": "builder",
      "branch_globs": [
        "grok/*",
        "partner/*"
      ],
      "exchange_dir": "EXCHANGE/grok/",
      "msg_numbering": "continues from EXCHANGE/partner/msg-003.md — first message in EXCHANGE/grok/ is msg-004.md",
      "static_write_scope": [
        "EXCHANGE/grok/**",
        "EXCHANGE/partner/**"
      ],
      "legacy": {
        "exchange_dir": "EXCHANGE/partner/",
        "branch": "partner/culinary-build",
        "status": "grandfathered; frozen after TASK-005 re-arm completes, then EXCHANGE/partner/** leaves static_write_scope; branch is never deleted"
      },
      "github_account": "tmundi32",
      "git_identity": "grok-bot"
    },
    "codex": {
      "role": "builder",
      "branch_globs": [
        "codex/*"
      ],
      "exchange_dir": "EXCHANGE/codex/",
      "static_write_scope": [
        "EXCHANGE/codex/**"
      ],
      "notes": "pushes as shared account tmundi32 per WAIVER-001; distinct git user.name/email required; branch namespace codex/* only",
      "github_account": "tmundi32 (shared under OPERATOR WAIVER-001)",
      "git_identity": "codex-bot"
    }
  }
}