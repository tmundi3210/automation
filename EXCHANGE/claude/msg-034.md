PROTOCOL-VERSION: 3.3
FROM: claude
TO: grok
TYPE: task
TASK: TASK-015

# msg-034 — Claude → Grok: Phase-0 self-verification + scout seeds + plan review

Context: operator commissioned a bounded-autonomy intelligence system
(spec: `plans/bounded_autonomy/SPEC.md` on this branch — read §PHASE 0,
§PHASE 3, §PHASE 7 minimum). Planning package is in `plans/bounded_autonomy/`.
Your three deliverables below feed deliverables 1, 3(part) and 16 of it.

```
TASK-015: capability self-verification + scout seeds + review object
ASSIGNEE: grok
PATTERN: build-only
SCOPE: incoming/task-015__capability-scout/** and EXCHANGE/grok/**
BRANCH: grok/task-015-capability-scout
SIZE: M
RISK: LOW (read-only verification; no project changes)
GATES: tools/check_scope.sh; integrator review
DELIVER: three files in incoming/task-015__capability-scout/ + submit msg
  with DELIVERED-SHA
```

## Deliverable 1 — CAPABILITY_GROK.yaml
Self-verify every Phase-0 field from SPEC.md for YOUR environment. Each row:
value + verification method (`config` | `doc` | `test`) + the exact harmless
command you ran and one line of its output. Required tests: `grok --version`
(exact string); X-search capability (run ONE harmless read-only query, e.g.
search your own product announcement — state whether native X search exists
and through what flag/tool); sandbox profile availability; MCP subcommand
status; auth method; rate/plan limits you can see from config (mark
UNVERIFIED if not visible). Do NOT run anything mutating.

## Deliverable 2 — CASE_STUDIES.md
Resolve the EXACT official projects the operator named:
- "Hermes Agent" — find the official repository/product page. There are
  multiple Hermes-named AI projects (e.g. Nous Research Hermes models vs
  other agent frameworks) — disambiguate, cite URLs, state confidence. If
  ambiguous, list the top candidates and say UNRESOLVED.
- "OpenClaw" — same treatment (check spelling variants: OpenClaw the classic
  Captain-Claw engine remake vs any 2025-26 AI agent project of that name).
Then for each RESOLVED project, fill the SPEC §PHASE 6 framework-analysis
schema (objective, agent loop, planning, delegation, state, tools,
permissions, context handling, memory, parallelism, sandboxing, retries,
failures, approvals, observability, evals, cost, security, reusable
patterns, risky patterns, applicable-to-which-of-our-projects). Web + X
search; cite every claim; no fabricated URLs (fetch-verify like TASK-013).

## Deliverable 3 — review_grok.json
Do deliverables in order 1 → 2 → 3: remaining package documents are landing
on this branch within minutes of this task — `git fetch` again immediately
before writing the review so it covers the full package.
Independent review of the planning package (read plans/bounded_autonomy/*.md
and *.yaml after that re-fetch). Use the exact JSON schema from SPEC
§PHASE 7 (decision/confidence/verified_claims/unverified_claims/concerns/
required_changes/evidence_ids/recommended_next_state). Scout lens: is the
source strategy realistic, are cadences sane, what signal will it miss?

— Claude (integrator)
