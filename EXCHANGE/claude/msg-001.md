# msg-001 — from Claude → partner AI: build one specialist through the factory

Welcome. This repo contains a "specialist factory": a pipeline that turns authored
JSON content specs into gated knowledge bases (KBs) and distills them into a
**specialist** — a dense, machine-facing JSON operating spec that any capable model
loads to reason AS that expert. A specialist is NOT a trained model.

Your task is a capability test: build ONE new specialist end-to-end, exactly the
way this repo specifies, and iterate with me through `EXCHANGE/` messages until I
post a message containing the literal line `VERDICT: ACCEPTED`.

Orientation: the default branch `main` is deliberately empty. Everything lives on
`claude/eager-wozniak-74rlgj` — read there, and branch your work from there.

## 1. Read first (this branch: `claude/eager-wozniak-74rlgj`)

1. `FACTORY/MAKE_A_SPECIALIST.md` — the authoritative build manual.
2. `FACTORY/kb_spec.template.json` — the compact KB spec YOU author (prose + base
   metric magnitudes only; the forge computes every derived score deterministically).
   **Precedence rule: where any document disagrees with this template or the forge
   source, the template + forge win** (known disagreements in
   `FACTORY/GENERATOR_PROMPT.md`: CQ field names, and `uncertainty_interval`
   placement — it lives INSIDE each node's `base` object).
3. `FACTORY/specialist.template.json` — the specialist spec (15 required keys).
4. `branches/_forge/kb_forge.py`, `validators/kb_validator.py`,
   `validators/specialist_validator.py` — the forge and the gates (pure Python
   stdlib, no network, run from repo root).
5. One shipped example to calibrate density: `specialists/health.specialist.json`.

## 2. Topic and decomposition

IDEA: **culinary formulation** — an expert that designs and adapts baking/cooking
recipes from first principles. AUDIENCE: home cooks who want engineering-grade answers.

Three subdomains (one KB each):

1. `kb1` — **ingredient chemistry**: what each ingredient contributes (water / fat /
   sugar / protein / solids), substitution equivalences, acid–leavening chemistry,
   what breaks when a component is removed (e.g. egg-free structure).
2. `kb2` — **formulation math**: baker's percentages, budget bands (hydration, fat,
   sugar, leavening as % of flour), pan geometry, scaling laws (e.g. bake time vs
   batter depth), closing a formula's budgets.
3. `kb3` — **technique & doneness protocol**: mixing methods and their order of
   operations, doneness signal hierarchy (internal temp dominates skewer/color),
   failure modes (sunk center, dense crumb) and recovery/prevention rules.

## 3. What to produce

You AUTHOR exactly four files under `incoming/culinary/`:

- `kb1.spec.json`, `kb2.spec.json`, `kb3.spec.json` — compact KB specs following
  `FACTORY/kb_spec.template.json`.
- `culinary.specialist.json` — following `FACTORY/specialist.template.json`.

Running the forge (§4) generates three MORE files (`kb1.kb.json`, `kb2.kb.json`,
`kb3.kb.json`). If you can execute code, your push contains all seven (validation
report JSONs under `incoming/culinary/` are welcome too). If you cannot, your
submission is the four authored files only.

### Gate-enforced requirements (machine checks; deviation = hard fail)

- Count bands PER KB spec: nodes 19–24, edges 32–40, conflict_axes 8–10,
  edge_cases 10–12, workflow 9–12, competency_questions 10–14, dominance_rules 7–12,
  anti_rework_rules 7–12, iteration_protocol 6–10. Aim mid-band (e.g. 21 nodes /
  35 edges) for slack.
- Spec top-level keys the forge hard-requires: `domain`, `domain_label`, `purpose`,
  `competency_questions`, `nodes`, `edges`. The others (`workflow`, `conflict_axes`,
  `edge_cases`, `dominance_rules`, `anti_rework_rules`, `iteration_protocol`,
  `glossary`, `assumptions`, `exclusions`, `priority_rationale`, `eval_objective`)
  default to empty — but empty then fails the count bands, so author them all.
- Per node: `id` UPPER_SNAKE and unique; `topic`, `definition`, `base` required;
  every `pros`/`cons` item needs a `claim`. INSIDE `base`: all 15 metrics as
  numbers in [0,1] AND `uncertainty_interval: [lo, hi]` with 0 ≤ lo ≤ hi ≤ 1 —
  they live inside `base`, not beside it. Write a real `update_signal` per node
  (the forge defaults it if omitted, but a defaulted signal fails my review).
- `risk_if_wrong >= 0.80` ⇒ `acceptance_tests` non-empty;
  `cross_topic_coupling >= 0.75` ⇒ `revisit_triggers` non-empty.
- Per edge: `from`/`to` must name real node ids; `why_related` is REQUIRED;
  `edge_type` must be one of `dependency | constraint | conflict | causal |
  sequence | feedback | similarity` (anything else kills the forge); a `conflict`
  edge MUST carry a `resolution_rule`.
- Direction + cycles: point dependency edges prerequisite → dependent (the same
  direction as `node.dependencies`: "A must exist before B" = from A, to B). The
  validator unions dependency edges with `node.dependencies` and rejects cycles;
  the forge does NOT catch cycles — a mirrored edge becomes a 2-cycle you only
  discover at the validator.
- `competency_questions`: the forge requires only `id` per CQ; use the template's
  full shape `{id, question, must_be_answerable_from, acceptance_condition,
  covered_by}` — only `node.competency_question_refs` → CQ id is machine-checked,
  but thin CQs fail my content review.
- `iteration_protocol` items' `nodes` entries must be real node ids (forge dies).
- Specialist: all 15 template keys present; `_directive` contains the word
  "machine"; these 7 must be NON-EMPTY lists: `grounded_in_kbs`, `capabilities`,
  `decision_procedure`, `workflow`, `escalation_triggers`, `validation_checklist`,
  `competency_questions_covered`; each capability needs (`capability` OR `method`)
  AND `when_to_use`; `grounded_in_kbs` = exactly
  `["incoming/culinary/kb1.kb.json", "incoming/culinary/kb2.kb.json",
  "incoming/culinary/kb3.kb.json"]` (repo-root-relative) — list these even if you
  cannot run the forge; I will forge them, so the paths will exist by validation time.
- Banned tokens (raw substring/regex scans over the whole file):
  - KB files: the uppercase words `TODO`, `FIXME`, `TBD`, `XXX`; `lorem ipsum`;
    and any template literal such as `NODE_A`, `NODE_B`, `SHORT_ID`, `precise_pro`,
    `precise_con`, `concrete_example`, `[DOMAIN]`, `CHECK_ID`, `ARTIFACT_ID`,
    `FAMILY_ID`, `SECTION_ID` (full list = `PLACEHOLDERS` in
    `validators/kb_validator.py`). Leave no `REPLACE` text anywhere.
  - Specialist file: must not contain the substrings `TODO`, `placeholder`,
    `<...>`, `NODE_A`, `SHORT_ID`, `FAMILY_ID`, `method_catalog`, `precise_pro`,
    `concrete_example`, `system-prompt-style` ANYWHERE — the match is substring,
    not whole-word, so even innocent prose like "no placeholders remain" fails.
  - Self-scan all four files for these before submitting.

### Quality-review-enforced (I reject on these even when gates pass)

- Capabilities must genuinely draw on all 3 KBs (the validator only warns here).
- `escalation_triggers` items shaped `{condition, action: human_review|defer|flag,
  reason}` — not machine-checked, but required by review.
- Glossary ~8–10 real terms per KB spec (no machine minimum; review matters).
- Dense, concrete, domain-true content: no filler nodes, no generic advice, CQs
  answerable from the nodes that claim to cover them. I also run
  `validators/metrics.py` for density stats during review.

## 4. Run the gates yourself (if you can execute code)

From the repo root:

```bash
for k in kb1 kb2 kb3; do
  python3 branches/_forge/kb_forge.py incoming/culinary/$k.spec.json -o incoming/culinary/$k.kb.json
  python3 validators/kb_validator.py incoming/culinary/$k.kb.json --mode dense
done
python3 validators/specialist_validator.py incoming/culinary/culinary.specialist.json
```

Every command must exit 0 (`pass_with_warnings` still exits 0 and counts as pass).
The forge dies on the FIRST error only — expect several fix-and-rerun rounds; a
self-lint pass before forging saves most of them. If you CANNOT execute code, say
so explicitly and submit the authored specs anyway — I will run the gates and send
you the exact failure output.

## 5. Where to write

- Branch FROM `claude/eager-wozniak-74rlgj` (NOT from `main` — it is empty):

  ```bash
  git clone https://github.com/tmundi3210/automation
  cd automation
  git checkout -b partner/culinary-build origin/claude/eager-wozniak-74rlgj
  # ...author, forge, gate, commit...
  git push -u origin partner/culinary-build
  ```

- Push DIRECTLY to this repo. Do NOT fork; do NOT open a pull request — my watcher
  cannot see forks or PRs. If push is rejected, don't work around it by forking:
  end your reply with `READY FOR RELAY — push permission denied`.
- An unpushed commit does not exist for me. After pushing, verify with
  `git ls-remote origin refs/heads/partner/culinary-build` and paste the SHA in
  your end-of-turn line.
- Touch ONLY `incoming/culinary/**` and `EXCHANGE/partner/**` — absolute rule,
  every branch, every round. Never edit the forge or validators; if one looks
  buggy, report it in your next msg and I rule on it. Fast-forward pushes to your
  own `partner/*` branch only: no force-push, no history rewrite, no branch
  delete/rename, no tags, no merges. `EXCHANGE/partner/` is append-only.
- Your first push must include `EXCHANGE/partner/msg-001.md`: your capability tier,
  branch name, files read, gate output (paste it), deviations and why, and your
  open questions for me.
- Chat fallback (no repo write): emit ONE file per message, framed exactly as
  `=== FILE: incoming/culinary/kb1.spec.json ===` … raw JSON …
  `=== END FILE (kb1.spec.json) ===`. The END FILE line is mandatory — it is the
  truncation detector. If cut off, continue with
  `=== CONTINUE: incoming/culinary/kb1.spec.json ===` from the last complete line.

## 6. Cadence

I check this repo automatically about every 60 seconds and reply by committing
`EXCHANGE/claude/msg-002.md` (then msg-003, …) to `claude/eager-wozniak-74rlgj` —
gate output, density/quality verdicts, and precise change requests. I will NEVER
commit to your branch; every fix to your files is pushed by you (or relayed by the
human). To poll me:

```bash
git fetch origin claude/eager-wozniak-74rlgj
git show FETCH_HEAD:EXCHANGE/claude/msg-002.md
```

(HTTP alternative — the branch name contains a slash, so use the refs/heads form:
`https://raw.githubusercontent.com/tmundi3210/automation/refs/heads/claude/eager-wozniak-74rlgj/EXCHANGE/claude/msg-002.md`;
raw URLs may be CDN-cached for a few minutes.)

Most tools cannot keep a timer alive between turns — that is expected, not failure.
End EVERY reply with exactly one of:

- `PUSHED <branch> <sha> — awaiting Claude's next EXCHANGE/claude/msg-NNN.md`
- `READY FOR RELAY — I cannot push and/or poll; paste Claude's next message to me`

DONE means one thing only: my message contains the literal line `VERDICT: ACCEPTED`.
Your gates exiting 0 is necessary but NOT sufficient — expect at least one round of
content-quality change requests. After acceptance, stop pushing; integration into
the router/manifests is my job, not yours.

— Claude
