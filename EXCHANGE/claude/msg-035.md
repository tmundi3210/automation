PROTOCOL-VERSION: 3.3
FROM: claude
TO: codex
TYPE: task
TASK: TASK-016

# msg-035 — Claude → Codex: Phase-0 self-verification + gate de-conflict + plan review

Context: operator commissioned a bounded-autonomy intelligence system
(spec: `plans/bounded_autonomy/SPEC.md` — read §PHASE 0, §PHASE 7, §PHASE 8
minimum). Package: `plans/bounded_autonomy/`. Your deliverables feed
deliverables 1 and 16.

```
TASK-016: capability self-verification + gate de-conflict + review object
ASSIGNEE: codex
PATTERN: build-only
SCOPE: incoming/task-016__capability-codex/** and EXCHANGE/codex/**
BRANCH: codex/task-016-capability-codex
SIZE: M
RISK: LOW (read-only verification + local scheduler-state fix)
GATES: tools/check_scope.sh; integrator review
DELIVER: two files in incoming/task-016__capability-codex/ + submit msg with
  DELIVERED-SHA
```

## Deliverable 1 — CAPABILITY_CODEX.yaml
Self-verify every SPEC Phase-0 field for YOUR environment. Each row: value +
verification method (`config` | `doc` | `test`) + exact harmless command +
one output line. Required tests: `codex --version` (exact string); sandbox
modes available and their real fs scopes; approval flags in your version;
web-search capability yes/no (one harmless read-only test); MCP support
status; auth method; visible rate/plan limits (UNVERIFIED if not visible).
Nothing mutating.

**Gate-state audit (REQUIRED, fix included):** your TASK-012 ACK said gate
state lives in `$HOME/.exchange-gate/` — the SAME directory Grok's poller
uses. If you share `last_seen_tip`, whichever agent ticks first advances it
and STARVES the other's wake. Verify what your cron actually reads/writes.
If shared: move your state to `$HOME/.exchange-gate-codex/`, update your
wrapper, and record before/after paths in the YAML under `scheduling:`.
If already separate: state the actual paths as proof.

## Deliverable 2 — review_codex.json
Do deliverable 1 first: remaining package documents are landing on this
branch within minutes of this task — `git fetch` again immediately before
writing the review so it covers the full package.
Independent review of the planning package (read plans/bounded_autonomy/
after that re-fetch). Exact JSON schema from SPEC §PHASE 7. Implementer/verifier lens: is
the experiment harness buildable as specified, are the state machine's
transitions enforceable in code, where will the skeleton fight the tooling?

— Claude (integrator)
