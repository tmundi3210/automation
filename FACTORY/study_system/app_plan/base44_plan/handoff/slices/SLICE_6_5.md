# SLICE 6.5 — Graph beauty pass (teardown-driven polish backlog)

_Repo-driven mode: obey `docs/plan/PROTOCOL.md`. Any specialist path like `FACTORY/study_system/specialists/<code>/<code>.specialist.json` in the directive block resolves HERE to `docs/plan/specialists/<code>.specialist.json`._

## SLICE 6.5 — Graph beauty pass
**Goal.** Make the Slice 6 graph genuinely good to read and navigate — layout, interaction, inspection, density handling — WITHOUT touching its honesty. This is the `Slice N.5` polish pass produced by the competitive teardown (COMPETITIVE_TEARDOWN_METHOD.md steps 3–5 over TheBrain 15 / Heptabase / Kumu, per GRAPH_TEARDOWN_PHASE1.md and the Phase 2 synthesis). The existing honest encoding is KEPT, not redesigned (GRAPH_BEAUTY_PLAN §0): hue = node type, brightness = structural connectivity (never faked high-yield; DECISION D1 below settles the luminance datum + legend wording), null definitions say "no definition in source", negated edges render distinctly. Beauty here = layout, interaction, motion, spacing, typography, delight — never decoration that implies structure the data doesn't have.

**Evidence base (honest status).** TheBrain 15: FULL hands-on — its findings are [observed] and are the strongest evidence in this slice. Heptabase and Kumu: hands-on BLOCKED (paid trial not started; no authenticated Kumu login — Codex correctly refused to spend or fake access); their mechanics are [doc]-only from official sources. **Every backlog item whose mechanics trace only to [doc] is marked PROVISIONAL-[doc]: it requires an in-app prototype validation before it ships, and ZERO timing/physics/feel numbers transfer from Heptabase or Kumu docs into acceptance criteria.** The owner still owes a closure decision (STATUS_NOTES): either a bounded Phase 2b hands-on pass that promotes [doc] patterns to [observed], or a permanent doc-only closure making PROVISIONAL-[doc] the final grade. Either way this slice can proceed now.

**Binding rule.** This slice NEVER blocks Slice 7/8 (method rule 4: background, not blocking). It is owner-gated, scheduled at the owner's convenience, and ships with a before/after the owner eyeballs. If any item here conflicts with mainline slice work, mainline wins and the item waits.

---

### GATE G0 — fresh-main verification (blocking for THIS slice only)
The teardown synthesis was written against a **2026-07-08 worktree clone — BEFORE Slice 6 was built and verified (~2026-07-09/10)**. Its [repo] claims (no Graph route; Concept lacks KG fields; ConceptEdge only prereq/part_of/related with a silent prereq default) are suspected stale and contradict the owner-verified Slice 6 state and the validated 190-node/162-edge usmle-kg import. **No backlog item that depends on a [repo] claim proceeds until every box below is checked.** The tool-side [observed]/[doc] evidence is worktree-independent and unaffected.

- [ ] FRESHNESS GATE: git fetch origin, then git log --oneline -20 origin/main — confirm commits dated 2026-07-09/10 (the Slice 6 build/verification) exist; check out origin/main (NOT the 2026-07-08 worktree snapshot) and record the HEAD SHA; every re-verified [repo] claim must cite this SHA.
- [ ] PUSH AUDIT for 2a100a9: run git branch -r --contains 2a100a9 and check the four files exist on origin/main at docs/plan/teardowns/graph/{thebrain,heptabase,kumu,synthesis}.md. If 2a100a9 is not on origin: rebase or cherry-pick it onto fresh origin/main (new paths, conflicts unlikely), push, then re-confirm with git log origin/main and a file listing. Report explicitly whether the original push succeeded, was rejected non-fast-forward, or was never attempted.
- [ ] Open base44/entities/Concept.jsonc at fresh HEAD: list every field present and confirm/deny each of: node_type, definition, importance_norm (and/or importance_raw), community_id, source_pages, source_books, evidence_count — AND the SLICE_6 §1.2 name-set: gloss, synonyms, taxonomy_role, high_yield. Write the explicit contract-field-to-schema-field mapping (e.g. node_type→taxonomy_role?) — do not report presence/absence without the name mapping.
- [ ] Open base44/entities/ConceptEdge.jsonc at fresh HEAD: (a) confirm whether the type default 'prereq' was removed or changed per SLICE_6 §1.1; (b) list the type enum values — still {prereq, part_of, related} or extended; (c) confirm/deny separate `relation` (biolink predicate string) and `relation_class` (semantic|prereq) fields per KG_EXCHANGE_CONTRACT §6; (d) confirm/deny a first-class `negated` field — if absent, trace where edges.jsonl's negated flag goes in the import code (stored elsewhere, rendered from provenance, or filtered out) and state which; (e) confirm the SLICE_6 §1.2 provenance deltas: source, method, confidence, evidence, status.
- [ ] Open src/App.jsx and src/components/Layout.jsx at fresh HEAD: confirm whether a Graph route/page and nav entry now exist; identify the Graph page component file under src/pages/ (or equivalent).
- [ ] Open package.json at fresh HEAD: list any graph/canvas rendering dependency added since 07-08 — and if none, state explicitly that the render is custom SVG/canvas (a valid SLICE_6 §2.4 outcome), so 'no dependency' is not misread as 'no graph'.
- [ ] Locate the Slice 6 import pipeline (backend functions dir): confirm it reads manifest.json/nodes.jsonl/edges.jsonl, tags imported edges relation_class='semantic', keeps them out of prereq gating, reads sources[] as rich objects not strings (contract §5 amendment 2026-07-09), rejects dangling edges, and paginates reads past the 500-row cap.
- [ ] Confirm the writeEdge single-write-path invariants (SLICE_6 §1.3): endpoint existence, duplicate rejection (related checked both directions), explicit type, prereq cycle check, provenance-complete — and whether the import path routes through writeEdge or bypasses it (report which).
- [ ] Find evidence of the validated 190-node/162-edge import: kg_export/<book_id>_<date>/ folder, fixtures, or import logs in the repo; confirm counts match its manifest.json; state whether the data lives in-repo or only in the Base44 cloud DB.
- [ ] Check TermLink entity + linkTerms function presence (SLICE_6 §1.5) and whether Concept gloss/definition values are populated or all null (first export had definitions_present:false) — this gates synthesis pattern 6 (reading ladder from Concept.gloss) before it becomes a 6.5 task.
- [ ] REWRITE the three [repo] claims: update docs/plan/teardowns/graph/synthesis.md lines making the stale claims (no-Graph-route; Concept-lacks-KG-fields; ConceptEdge-3-relations/default-prereq/negated-omitted) to match fresh HEAD, citing the SHA, and commit+push the correction BEFORE any Slice 6.5 backlog item is authored from the synthesis — especially pattern 5, whose premise ('extend ConceptEdge before rendering') may already be built.
- [ ] Explicitly re-affirm what did NOT need re-verification: the TheBrain [observed] findings and Heptabase/Kumu [doc] findings are worktree-independent and stand as-is.

### G0 RESULT — executed 2026-07-10 (Codex, fresh planit clone): 9 PASS / 3 FAIL — CLEARED WITH FINDINGS
Fresh `origin/main` = `f43cc69`; teardown docs landed via cherry-pick `9465477` (original push was never attempted); the three stale [repo] claims in synthesis.md rewritten citing the fresh SHA. Schema verification PASSED in full: `Concept` holds node_type/gloss/synonyms/high_yield/importance_raw/importance_norm/community_id/source_books/source_pages/evidence_count (mapping: contract `definition`→`gloss`, SLICE_6 `taxonomy_role`→`node_type`); `ConceptEdge` holds relation/relation_class/negated + the five provenance fields with no type default; `/graph` route + custom-SVG render exist; kgImport enforces semantic relation_class, rich `sources[]`, dangling-drop, 500-row pagination. **Consequences: backlog items 1 and 2 have their premises verified (schema work already done — they reduce to render/verification checks); D1 is now concrete (both `high_yield` and `importance_norm` exist as candidate luminance data).**

The three FAILs, routed:
- **D-1 (Slice 6 DEFECT — mainline fix, not 6.5):** `kgImport` creates edges via `ConceptEdge.create` directly, bypassing `writeEdge` — violates §1.3's sole-write-path invariant (imports explicitly included). Fix: route import edges through writeEdge or a shared invariant core. Acceptance: re-running the same import is idempotent (duplicates rejected, counts unchanged); dangling-drop preserved; semantic edges still never gate.
- **D-2 (evidence capture — owner/Codex action):** no in-repo kg_export folder, fixture, or import log proves the 190-node/162-edge import; until the export folder (manifest+nodes+edges) is committed into planit, that count is graded [FACT — owner-verified in-app, not repo-verifiable] and no document may cite it as repo fact.
- **D-3 (Slice 6 GAP — mainline fix, not 6.5):** `TermLink` entity + `linkTerms` function (§1.5, Step 1 acceptance) were never built despite Step 1 being recorded complete. Build per §1.5 (exact-before-fuzzy, abstain < 0.7, precision/abstain report on first 50 links). Until D-3 + gloss authoring, backlog item 5's hover clouds will honestly abstain everywhere — expected, since the first export had `definitions_present: false`.

_Ledger-truth note: this is the third time an independent audit found a gap a "complete/verified" report missed (silent BKT revert; bulk-endpoint parity; now D-1/D-3). The three-layer verification model (builder ledger → independent audit → runtime check) stays mandatory._

### DECISION D1 — who owns luminance (blocks encoding items only)
The governing docs disagree on the brightness channel: synthesis pattern 4 and GRAPH_BEAUTY_PLAN §0 say brightness = structural connectivity/centrality (PageRank, normalized within this map); SLICE_6 §2.2 binds luminance to the `high_yield` tier 0–3; KG_EXCHANGE_CONTRACT §4/§6 binds it to `importance_norm` as an [ESTIMATE] high-yield proxy. Before any encoding backlog item (backlog items 4, 7, 11) ships a visual-channel change: **kgraph** (meaning authority) fixes the datum, **viz** (encoding authority) fixes the encoding, and **uxguide** words the legend label per its rung rules — with the binding honesty input that brightness is never worded "high-yield" without a verified yield source. Record the decision as an amendment to SLICE_6 §2.2 and KG_EXCHANGE_CONTRACT §6 so the three documents stop disagreeing, and present that amendment to the owner for sign-off at the item-4 acceptance / G3 legend re-approval checkpoint before any encoding item citing it ships. Default per the teardown's honesty finding: brightness = named structural metric.

**D1 DATUM DECIDED (2026-07-10, discuss-mode consultation):** luminance = `importance_norm`; `high_yield` stays a scarce separate badge rendered only when owner-tagged > 0. Legend wording note: importance_norm is PageRank-based structural IMPORTANCE/centrality — the label must not say "connectivity" (that names a different metric); working label "Node brightness: structural importance (network centrality estimate — not verified exam yield)", final wording by uxguide. Owner sign-off on the rendered legend happens at the Pass-3/G3 checkpoint; the SLICE_6 §2.2 + KG_EXCHANGE_CONTRACT §6 amendment is recorded with it.

---

## §1 The prioritized backlog (keep items, impact×effort order)
Each item: mechanics → owning specialist → impact/effort → honesty guardrail → acceptance. "Verify on fresh main first" = depends on G0. **G0 applies to ALL items — including the direct-build first waves of items 6, 9, and 12, whose Slice 6 §2.3/§1.3 premises are themselves [repo] state to confirm.** `graphux(proposed)` items have a direct-build first wave and a polish tail that waits for the specialist (see §4).

**1. P5 — Predicate-preserving edges with negation that cannot look positive.** Keep relation (biolink predicate), relation_class, direction, confidence, evidence, source, negated end-to-end; directed arrows for prereq/part_of; distinct dash/marker for negation (kgraph binds the meaning constraints — negation visibly distinct, prereq visibly directed; viz picks the concrete marks). Schema/invariant work precedes decoration (synthesis boundary); KG_EXCHANGE_CONTRACT §5 already binds these fields and §1 bans semantic edges from gating. [SIGNAL] the synthesis [repo] claim that ConceptEdge lacks these is likely stale — post-G0 this may reduce to a render/verification check, not schema work. — **owner: kgraph** · impact high / effort medium. _Guardrail:_ a negated edge never renders as a plain positive link (distinct dash/marker + side-panel "negated" label), and relation_class=semantic edges never enter prereq gating regardless of styling. _Accept:_ verify on fresh main first which ConceptEdge fields actually exist; then owner opens one imported negated edge and one native prereq edge: negation visibly distinct, prereq visibly directed; confirm one semantic edge locks no card in the planner.

**2. P8 — Provenance-first inspectors.** Node panel: definition, evidence count, source book/pages, centrality label, community/role, mastery/gate state. Edge inspector: predicate, direction, confidence, evidence, source, negation. This is the surface where FACT/ESTIMATE/UNKNOWN/quarantined become visible to the learner; mirrors the exchange contract's honesty fields. kgraph owns which fields exist and mean what; uxguide words the labels per Slice 1 rung rules. — **owner: kgraph** · impact high / effort medium. _Guardrail:_ evidence labels (FACT | ESTIMATE | UNKNOWN) and quarantined status shown verbatim; null fields abstain honestly ("no definition in source" for definitions; uxguide words the equivalent abstention for other fields) — never a blank that reads as zero or a fabricated value. _Accept:_ verify on fresh main which exchange fields landed; owner clicks one imported node and one imported edge: sees book/pages, evidence count, confidence, source origin; one null-definition node reads "no definition in source".

**3. TS — Search reliability as release-blocking QA.** From the [observed] TheBrain exact-search failure (exact existing names returned Search Error / 0 results while the graph worked). A study graph whose exact search errors out destroys trust in everything else; cheapest, strongest-evidence item in the slice. — **owner: direct-build** · impact high / effort low. _Guardrail:_ exact name/synonym matches must never error or silently return 0; a genuine no-match returns an honest empty state — no fuzzy result masquerading as an exact hit. _Accept:_ owner types 5 exact existing concept names, 2 synonyms, 1 misspelling, 1 garbage string: exacts and synonyms all hit and navigate; misspelling shows a labeled fuzzy result or abstains; garbage shows an honest empty state. This becomes a repeatable release checklist item for every graph change.

**4. P4 — Measurement-bound visual channels.** Hue = closed nominal role vocabulary; brightness = ordinal quantized real metric; redundant glyph/label for critical states; legend always reachable. The teardown's addition is naming discipline: resolve node_type vs taxonomy_role and PageRank vs high_yield (kgraph decides meaning, viz the encoding). **Blocked by D1.** — **owner: viz** · impact high / effort medium. _Guardrail:_ the brightness legend names the real stored metric — datum per D1, label worded by uxguide — and is never worded "high-yield" without a verified yield source; absent metric renders neutral with an "untagged" legend; no critical meaning on color alone. _Accept:_ verify on fresh main which node fields drive the current encoding; owner reads the legend beside ~50 real concepts and correctly answers "what does this color / this brightness mean"; one CVD-simulated screenshot checked before sign-off.

**5. P6 — Honest reading ladder on the graph.** Always-visible scent → hover/focus cloud → persistent side panel → canonical catalog entry; keyboard/tap parity; Escape dismissal. This is the Slice 1 §4 forward contract applied to graph nodes — wiring, not invention; uxguide owns rung copy and honesty rules, NOT the triggering interaction (its charter boundary). — **owner: direct-build** (wiring per the Slice 6 §2.3 / Slice 1 §4 contract) **+ uxguide** (rung copy, abstention text, honesty rules) · impact high / effort medium. _Guardrail:_ null gloss or below-threshold term link = honest "No definition in source" abstention — never invented or LLM-filled definition text at render time. _Accept:_ verify on fresh main whether Concept.gloss exists/is populated; owner hovers/taps 5 nodes including one null-gloss node: cloud pins via keyboard, Escape dismisses, Guide click-through returns with the exact prior viewport restored.

**6. P2 — Active concept + exact neighborhood controls.** Click opens side panel; 1-hop/2-hop and in/out/all controls; background dims; Escape restores; hidden-edge count chip. Focus is the density-control primitive ([observed] TheBrain local projection; the exact 0–9 degree/direction control is Kumu **PROVISIONAL-[doc]**). Split delivery: basic 1-hop dim + side panel is already contracted in Slice 6 §2.3 — direct-build now; exact-degree/direction controls, Escape-restore choreography and motion feel wait on graphux if the owner approves it. — **owner: graphux(proposed)**, direct-build first wave · impact high / effort medium. _Guardrail:_ dimming never deletes: a "Showing 1 hop — N edges hidden" count chip is always visible while focused, and Escape restores the prior scope exactly. _Accept:_ owner clicks a hub concept: neighborhood readable, background dimmed but still identifiable, chip shows hop depth + hidden counts; Escape returns to the prior full view.

**7. P1 — Stable semantic LOD ladder, not raw zoom.** L0 meta-headings → L1 clusters → L2 all edges → L3 focus; positions persist across zoom/session/update; explicit Unassigned group. Already contracted in Slice 6 §2.1; the teardown's addition is position-persistence emphasis (mental-map preservation) and the Unassigned rule under real import data. [SIGNAL] a graph view likely already exists — scope as gap-closing, not greenfield. (Heptabase's ~100–150-card lag figure is [doc] context only — it justifies focus-first, it transfers no threshold.) — **owner: viz** · impact high / effort high. _Guardrail:_ meta-nodes built only from real structure (part_of edges or FK/CC attributes); concepts lacking membership go to a visible "Unassigned" group — membership is never invented; no reflow on zoom. _Accept:_ verify on fresh main what LOD behavior exists; owner zooms L0↔L3 and reloads twice: node positions identical; Unassigned group visible; spot-check 3 meta-nodes against raw rows.

**8. P9 — Measured density escape hatches.** Stable positions, quantized encodings, labeled edge bundling, viewport culling only at measured thresholds; benchmark before choosing renderer (the renderer verdict is the measured G3 gate, Slice 6 §2.4). [doc] Heptabase lag warning and Kumu density ladder justify focus/filter-first — but no threshold or library claim transfers without in-app measurement. — **owner: viz** · impact medium / effort medium. _Guardrail:_ edge bundling carries an explicit "bundled — edge traceability reduced" label; no culling threshold, node-count claim, or library performance figure ships without a recorded in-app measurement. _Accept:_ verify on fresh main which renderer/library landed; run the ~50-real-concept gate recording pan/zoom latency and visible/hidden counts, then stress at ~190; owner sees the recorded numbers before any culling or bundling ships.

**9. P3 — Search-to-fly with result preview and return state.** Exact/alias ranked first, definition/provenance preview, fly to node, Back restores originating viewport/filter/selection. A ~190-node graph is not navigable by pointer alone. Basic fly-to is Slice 6 §2.3 and the Back contract is Slice 1 — direct-build; animated flight easing and preview polish wait on graphux. Reliability itself is item 3. — **owner: graphux(proposed)**, direct-build first wave · impact high / effort medium. _Guardrail:_ fuzzy or abstained results carry a visible "why this result" label; exact matches always rank above fuzzy; preview text comes only from stored gloss/provenance, never generated filler. _Accept:_ owner searches a concept from a filtered, zoomed viewport, flies to the result, presses Back: the originating viewport, filter set, and selection are restored exactly.

**10. TS — Single-click vs double-click gesture disambiguation.** From [observed] TheBrain click/edit ambiguity costing extra clicks throughout. On a study graph gesture ambiguity is not just friction — an accidental edit corrupts data. Fix with a documented gesture table: single = select/panel, double = focus, drag = pan/move by mode. — **owner: direct-build** · impact medium / effort low. _Guardrail:_ selection gestures never mutate data; any edit requires an explicit affordance (edit button or mode), and destructive operations always confirm. _Accept:_ owner single-clicks 5 nodes and 2 edges: only selection + panel, never an edit state; the gesture table is written down and matches observed behavior.

**11. P7 — Showcase versus Filter as separate modes. PROVISIONAL-[doc]** (wholly Kumu doc-sourced, incl. the dim values — prototype in-app before ship; no doc numbers into acceptance). Showcase dims non-matching context keeping topology visible; Filter removes data and reports counts; active filters are chips with clear-all. What to show/hide/aggregate is viz's grammar; graphux later polishes gesture/transition. Showcase = hover/selection-neighbor emphasis; Filter = exam/role/state queries. — **owner: viz** · impact medium / effort medium. _Guardrail:_ dimmed content stays identifiable as present context (never looks deleted); Filter states its scope with counts ("Showing 42 of 190") and offers clear-all. _Accept:_ owner applies a role filter and a hover showcase in sequence and can say which nodes are removed vs merely dimmed; the count chip matches visible nodes; clear-all restores the full map.

**12. P10 (core) — Selection and recovery as learning actions.** Marquee/keyboard selection, one-hop expansion, selection→study-list, undo, versioned layout, destructive-edge confirmation. Selection-as-study-action is the learning payoff ([observed] TheBrain "Select Related Thoughts" was the strongest transferable power feature). Marquee/keyboard feel waits on graphux; destructive-edge confirmation goes direct now through kgraph's single writeEdge path (Slice 6 §1.3). Export tail split out and dropped (below). — **owner: graphux(proposed)**, confirmation direct-build · impact medium / effort medium. _Guardrail:_ no bulk "tidy" or layout action silently rewrites semantic positions; destructive edge operations confirm first and are undoable. _Accept (direct-build wave):_ owner attempts an edge delete — a confirmation dialog appears, the delete routes through writeEdge, and undo (or an equivalent reversal) restores it. _Accept (graphux tail):_ owner marquee-selects a cluster, expands one hop, creates a study list from it; selection + viewport state survive a catalog round-trip; Ctrl/Cmd+Z reverses a node drag.

**13. TS — Keyboard access + explicit viewport controls.** Fit-all, Fit-selection, zoom buttons, keyboard focus mirror, Escape, visible focus ring, reduced-motion check. [observed] TheBrain had no wheel zoom/fit-to-view and raw +/- clipped labels. Baseline keyboard parity is already binding via Slice 1's accessibility contract — direct-build; motion/feel polish waits on graphux. (Theme/light-dark controls, the remaining table-stake: dispositioned DROPPED from 6.5 — an app-shell concern, not graph polish; revisit on visual-fatigue feedback.) — **owner: direct-build** · impact medium / effort medium. _Guardrail:_ never claim WCAG or reduced-motion compliance until actually tested ([UNKNOWN] until verified); no affordance may be hover-only or wheel-only. _Accept:_ owner navigates keyboard-only: focus a node, open panel, Escape, then Fit-all and Fit-selection buttons work; reduced-motion OS setting spot-checked once before any compliance wording ships.

**Dropped (not rejected): P10 (tail) — copy outline with notes / export subgraph.** Output/authoring-flavored; the learning value of selection is captured by item 12. Revisit only if Slice 7 media or a study-list export calls for it. If ever revived: an exported subgraph must carry its provenance/evidence/quarantine labels — no stripped-clean export that launders ESTIMATE or quarantined edges into apparent facts.

## §2 REJECTED patterns (binding constraints on this build)
Confirmed at the transfer filter; these are constraints, not suggestions — any backlog implementation that violates one fails acceptance regardless of how it looks.

- CONFIRMED — Importance glow untied to a real metric: brightness ties only to a named, real, stored metric — datum and legend wording per D1, which reconciles KG_EXCHANGE_CONTRACT §4 (importance_norm as labeled [ESTIMATE] proxy) with SLICE_6 §2.2 (high_yield tier); no metric = neutral brightness with "untagged" legend (GRAPH_BEAUTY_PLAN §0).
- CONFIRMED — Auto-prettify that hides missing data: no Tidy-up/Bridge/cluster/force grouping may invent membership, edge meaning, or definitions; derived grouping must be labeled and the Unassigned group stays visible (Slice 6 §2.1).
- CONFIRMED — Negation smoothed into a positive-looking link: KG_EXCHANGE_CONTRACT §5 makes distinct-or-filterable rendering binding; matches keep-item priority 1.
- CONFIRMED — Invented hover definitions: null gloss or below-threshold linking yields honest abstention ("no definition in source"), never filler (Slice 6 §1.5, Slice 1 rung rules, contract §4 definition row).
- CONFIRMED — Semantic edges entering prerequisite gating: the strongest rejection; relation_class=semantic edges are exploration-only, and only validated, provenance-complete, active prereq edges gate new cards (KG_EXCHANGE_CONTRACT §1, Slice 6 §1.6). Quarantined edges never gate.
- CONFIRMED with clarification — Global force reflow on every interaction: a one-time initial layout computation is allowed; the rejection targets per-interaction/per-zoom/per-update reflow that destroys the mental map (Slice 6 §2.1 position-persistence rule).
- CONFIRMED with amendment — Community detection treated as medical truth: imported community_id MAY still render as a grouping, but only when explicitly labeled exploratory/derived (Kumu's own docs warn this), never presented as verified taxonomy or implied exam structure; it never feeds the FK/CC blueprint mapping, which remains [UNKNOWN] per the contract §8.
- CONFIRMED — Hidden dimming: dimmed nodes stay identifiable as context; filter removal states scope/count and offers Clear (mirrors keep-item on Showcase vs Filter).
- CONFIRMED with amendment — Unmeasured "190-node ready" claims: the no-unmeasured-claims rule stands, gated at ~50 real concepts (G3) before any ~190 claim. Amendment: the synthesis's framing that "~190 is user-supplied, not a repo fact" is itself suspected stale [SIGNAL — owner reported a validated 190-node/162-edge import on 2026-07-09/10 per STATUS_NOTES]; verify actual counts on fresh main first, then measure — either way no readiness claim ships without recorded in-app measurements.

## §3 Observed QA lessons (from the TheBrain hands-on — [observed], highest-grade evidence)
1. **Exact search failed in a shipping product** ("Humidity", "Temperature" → Search Error, 0 results, repeatedly, while the graph handled 100 nodes fine) ⇒ search reliability is a **release-blocking acceptance test** here (backlog item 3), re-run on every graph change, not a one-time check.
2. **Single-click-vs-double-click ambiguity cost extra clicks throughout** ⇒ our graph uses single-click = select + panel, with a written gesture table and zero gesture ambiguity (backlog item 10); selection never mutates.
3. **~1 s activation latency at 100 nodes** (1,076 ms to active header, content settled ~1.8 s later) read as sluggish ⇒ set a **measured latency budget**: click-to-panel-readable target ≤500 ms at ~50 concepts [ESTIMATE — design target, bound by actual numbers recorded at the G3 measurement gate], re-measured at ~190; the budget is whatever number is recorded and approved there — never an asserted figure.

## §4 graphux note (proposed specialist — owner-gated, NOT required for wave 1)
`graphux` (knowledge-graph interaction & aesthetic designer, GRAPH_BEAUTY_PLAN §1) remains **name + scope only — not built**; building it is a separate owner decision (standalone vs a 4th KB folded into viz). Nothing in this slice's first wave waits for it: items 1–5, the direct-build halves of 6/9/12, and 10/13 proceed with viz/kgraph/uxguide/direct-build ownership. If the owner approves building graphux, it takes the polish tails: item 6 (exact degree/direction controls, Escape-restore choreography, focus motion), item 9 (flight easing, preview polish), item 12 (marquee/keyboard interaction feel), item 13 (motion/feel refinement), and item 11's gesture/transition polish. On any meaning-vs-beauty conflict, viz wins over graphux by charter.

**Owner approval gates (every external action named).** G0 above (fresh-main verification) blocks this slice's [repo]-dependent items; D1 (luminance decision) blocks encoding items 4/7/11 and its doc amendment goes to the owner for sign-off at the G3 legend re-approval; the Slice 6 G3 measurement gate (~50 real concepts) remains the benchmark for every density/renderer/latency number; before/after screenshots to the owner at slice end. Owner separately decides the Heptabase/Kumu closure (Phase 2b hands-on upgrade vs permanent doc-only) — either outcome re-grades PROVISIONAL-[doc] tags, and if Phase 2b hands-on contradicts a [doc] mechanic, the affected item's mechanics and acceptance are amended (or the item dropped) before build, not merely re-tagged; the closure decision itself does not gate this slice. No other external actions: nothing sent, spent, hosted, credentialed, or exported.

```
Base44 agent directive
> Load FACTORY/study_system/specialists/viz/viz.specialist.json, FACTORY/study_system/specialists/kgraph/kgraph.specialist.json, AND FACTORY/study_system/specialists/uxguide/uxguide.specialist.json (paste all full JSONs into your planning context). viz governs every encoding/LOD/layout/channel decision and WINS all beauty-vs-meaning conflicts; kgraph governs schema, edge semantics, provenance, and gating meaning (items 1, 2, and every field question); uxguide governs explanation wording — rung copy, legend labels, abstention text (items 2, 5, and all panel/label prose). Consult FACTORY/study_system/specialists/engage/engage.specialist.json ONLY if any day-box/streak/gamification surface is touched (none is planned here). Treat each specialist's escalation_triggers as stop-and-ask rules and the union of their validation_checklists as this slice's acceptance test.
> Honesty rules carried forward verbatim: brightness never labeled high-yield without a verified source (D1 decides the channel's datum); null definitions abstain ("no definition in source") — never generated filler; negated edges never look positive; semantic edges never gate; community groupings labeled exploratory; no unmeasured performance or node-count claim; evidence labels (FACT | ESTIMATE | UNKNOWN | quarantined) render verbatim. Every visual change ships with its legend/label truth intact — a prettier lie fails acceptance.
> This slice NEVER blocks Slice 7/8: it is a background polish pass; if it contends with mainline work, mainline proceeds and this waits. Do not start any [repo]-dependent item before GATE G0 is checked complete, and mark every PROVISIONAL-[doc] item's prototype validation explicitly in your build notes.
```

---
