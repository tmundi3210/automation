{
  "domain": "study_system_viz_semantic_zoom_lod_graph_drawing",
  "domain_label": "Study System — Visual-Encoding & Graph-View Cartographer: Semantic Zoom, Level-of-Detail & Graph Drawing",
  "purpose": "How the concept knowledge graph is DRAWN as an interactive semantic-zoom map: the cartographic-generalization science that decides what to show, hide, aggregate and rename at each zoom level (few category headings zoomed out, concise concept names as you zoom in), plus the graph-drawing machinery — force-directed and hierarchical layout, edge bundling, and label decluttering — that turns the Concept/ConceptEdge data into a legible, honest, performant picture, kept truthful by tagging every domain-science claim ESTIMATE-to-source-verify and every app fact to APP_RECON.",
  "assumptions": [
    "no observed dataset supplied; all scores are heuristic priors and every layout/LOD choice is an ESTIMATE synthesized from information-visualization and cartography domain knowledge, not measured on this app's graph",
    "the app has Concept and ConceptEdge entities (prereq/related/part_of edge types) but NO installed graph library and NO page that renders any graph, so this KB specifies a drawn view that does not yet exist [FACT-source: APP_RECON §5.5]",
    "domain-science references (semantic-zoom/ZUI lineage, cartographic generalization operators, force-directed and layered layout, edge bundling, NP-hard label placement) are stated from confident general knowledge but were NOT source-verified in this session, so they are carried as [ESTIMATE — to source-verify] rather than cited to a specific title/year",
    "the MEANING encoded by color (high-yield intensity, mastery green/gray/red-dot) and the analytics charts beyond the day-box are neighboring KBs in this same specialist; this KB owns only geometry, zoom, generalization and layout"
  ],
  "exclusions": [
    "the color system that encodes meaning (brightness-coded high-yield intensity, mastery-state green/gray/red-dot, colorblind-safe perceptual palettes) — neighboring viz KB — beyond consuming a node's precomputed importance/mastery as a selection and label input",
    "the analytics charts (calibration trends, mastery-over-time) beyond the day-box — neighboring viz KB — since those are statistical-chart encodings, not graph drawing",
    "the knowledge-graph DATA and meaning layer (node/edge schema, hub/prerequisite structure, terminology linking) owned by the kgraph specialist — this KB draws whatever graph kgraph defines and does not decide its ontology",
    "the actual pixel styling and component code, exact library API calls, and any invented graph-library benchmark numbers — the KB specifies decision criteria, not an implementation or fabricated performance figures"
  ],
  "competency_questions": [
    { "id": "CQ_01", "question": "What is the difference between geometric zoom and semantic zoom, and why does the concept map need semantic zoom?", "must_be_answerable_from": ["SEMANTIC_VS_GEOMETRIC_ZOOM"], "acceptance_condition": "distinguishes geometric zoom (scaling the same pixels larger/smaller) from semantic zoom (changing WHAT is drawn and its representation per level), names the zoomable-user-interface lineage as ESTIMATE-to-source-verify, and ties it to the few-headings-out / concise-names-in requirement", "covered_by": ["SEMANTIC_VS_GEOMETRIC_ZOOM", "VIEW_PROBLEM_FRAME"] },
    { "id": "CQ_02", "question": "What is a level-of-detail hierarchy and how is a per-level visual-complexity budget set?", "must_be_answerable_from": ["LOD_HIERARCHY"], "acceptance_condition": "defines discrete LOD levels each with a target on-screen element budget, states that each level shows/hides/aggregates to stay within the budget, and frames LOD as the control structure the generalization operators serve", "covered_by": ["LOD_HIERARCHY"] },
    { "id": "CQ_03", "question": "What are the cartographic generalization operators that decide what to show, hide, merge or rename at each zoom?", "must_be_answerable_from": ["CARTO_GENERALIZATION_OPERATORS"], "acceptance_condition": "enumerates selection/elimination, simplification, aggregation, typification, displacement and symbolization as the map-making operators, maps them onto graph elements, and tags the taxonomy ESTIMATE-to-source-verify", "covered_by": ["CARTO_GENERALIZATION_OPERATORS"] },
    { "id": "CQ_04", "question": "How does selection/elimination decide which nodes survive when zoomed out?", "must_be_answerable_from": ["SELECTION_ELIMINATION"], "acceptance_condition": "states that an importance ranking (hub degree, high-yield weight, mastery relevance) orders nodes so the most important survive to the coarsest levels and the rest are eliminated or absorbed, not a random or purely degree-based cut", "covered_by": ["SELECTION_ELIMINATION"] },
    { "id": "CQ_05", "question": "How are clusters aggregated into meta-nodes (headings) that expand into members as you zoom in?", "must_be_answerable_from": ["AGGREGATION_META_NODES"], "acceptance_condition": "describes collapsing a concept cluster (e.g. an FK area or CC section) into a single meta-node heading zoomed out and expanding it into its member concepts zoomed in, with a stable membership mapping", "covered_by": ["AGGREGATION_META_NODES"] },
    { "id": "CQ_06", "question": "How is the 'concise names zoomed out, fuller names zoomed in' requirement realized as label generalization?", "must_be_answerable_from": ["LABEL_GENERALIZATION"], "acceptance_condition": "specifies short category/heading labels at coarse levels and progressively fuller concept names at finer levels, per-node label variants keyed to LOD level, and the ZOOM_LOD_MAPPING that assigns them", "covered_by": ["LABEL_GENERALIZATION", "ZOOM_LOD_MAPPING"] },
    { "id": "CQ_07", "question": "What makes a zoomed-out abstraction HONEST rather than a misleading simplification?", "must_be_answerable_from": ["HONEST_ABSTRACTION"], "acceptance_condition": "requires aggregated overviews to faithfully summarize members (no invented groupings, member counts and coverage shown, mastery roll-ups are real aggregates), so hiding detail never implies a false structure", "covered_by": ["HONEST_ABSTRACTION"] },
    { "id": "CQ_08", "question": "How does force-directed layout position nodes, and how is it scaled to larger graphs?", "must_be_answerable_from": ["FORCE_DIRECTED_LAYOUT"], "acceptance_condition": "explains attractive edge springs plus pairwise node repulsion minimizing an energy/stress, names spring-embedder and stress approaches as ESTIMATE-to-source-verify, and gives the O(n^2)-to-O(n log n) Barnes-Hut quadtree approximation for scale", "covered_by": ["FORCE_DIRECTED_LAYOUT", "BARNES_HUT_SCALING"] },
    { "id": "CQ_09", "question": "When is a hierarchical (layered) layout preferred, and how does it draw the directed prerequisite structure?", "must_be_answerable_from": ["HIERARCHICAL_LAYOUT"], "acceptance_condition": "describes layered layout for directed acyclic prerequisite edges — layer assignment, crossing minimization, coordinate assignment — as the way to make prereq flow readable, tagged ESTIMATE-to-source-verify", "covered_by": ["HIERARCHICAL_LAYOUT"] },
    { "id": "CQ_10", "question": "How do edge bundling and label decluttering reduce clutter, and what do they cost?", "must_be_answerable_from": ["EDGE_BUNDLING"], "acceptance_condition": "explains bundling near-parallel edges to expose overall flow at the cost of individual-edge traceability, and label decluttering (collision detection, priority, zoom thresholds) as an NP-hard placement problem solved greedily, both tagged ESTIMATE-to-source-verify", "covered_by": ["EDGE_BUNDLING", "LABEL_PLACEMENT_DECLUTTER"] },
    { "id": "CQ_11", "question": "How is the user's mental map preserved across zoom levels and updates?", "must_be_answerable_from": ["LAYOUT_STABILITY"], "acceptance_condition": "states that incremental/stable layout and pinned anchors keep nodes from jumping between levels or re-runs, and that animated expand/collapse transitions preserve object constancy so a heading is seen becoming its members", "covered_by": ["LAYOUT_STABILITY", "TRANSITION_ANIMATION"] },
    { "id": "CQ_12", "question": "How are LOD levels mapped to concrete render decisions, and why does viewport culling matter?", "must_be_answerable_from": ["ZOOM_LOD_MAPPING"], "acceptance_condition": "gives a concrete level-to-render map (headings only, then clusters, then concise-named concepts, then full names plus edges) and states that only viewport-plus-neighborhood nodes are laid out/rendered, respecting the app's 500-row query caps", "covered_by": ["ZOOM_LOD_MAPPING", "VIEWPORT_CULLING"] },
    { "id": "CQ_13", "question": "Given no graph library exists, how are render target and build-vs-adopt decided, and what is the drawn-view verdict?", "must_be_answerable_from": ["VIEW_SPEC_VERDICT"], "acceptance_condition": "chooses SVG below and canvas/WebGL above a node-count threshold, weighs adopting a proven layout library against building custom, and composes the go/no-go drawn-view spec (semantic zoom + LOD generalization + hybrid layout + bundling + decluttered honest labels + stable transitions)", "covered_by": ["VIEW_SPEC_VERDICT", "RENDER_TARGET_SVG_CANVAS", "LIBRARY_SELECTION"] }
  ],
  "glossary": [
    { "term": "semantic zoom", "definition": "Zoom that changes WHAT is represented and HOW at each scale — a coarse level shows category headings, a fine level shows individual concepts with fuller names — as opposed to geometric zoom, which only scales the same fixed picture; the mechanism behind 'few nodes zoomed out, more detail and concise names zoomed in' [ESTIMATE — zoomable-user-interface lineage, to source-verify]." },
    { "term": "level of detail (LOD)", "definition": "A ladder of discrete zoom levels, each with a target visual-complexity budget (how many nodes, edges and labels may appear), that the generalization operators are applied to satisfy; borrowed from computer-graphics LOD and cartographic scale bands [ESTIMATE — to source-verify]." },
    { "term": "cartographic generalization", "definition": "The map-making science of representing less detail at smaller scale through operators — selection/elimination, simplification, aggregation, typification, displacement, symbolization — repurposed here to decide which concept nodes, edges and labels appear at each zoom level [ESTIMATE — operator taxonomy, to source-verify]." },
    { "term": "aggregation / meta-node", "definition": "Collapsing a cluster of concepts (e.g. an INBDE FK area or CC section) into a single super-node heading at coarse zoom that expands into its member concepts as the user zooms in, with a fixed, honest membership mapping." },
    { "term": "force-directed layout", "definition": "A physics-analogy layout in which edges act as springs pulling connected nodes together while all node pairs repel, iterated until the energy/stress is minimized, yielding an organic clustering; scales from O(n^2) to O(n log n) with a Barnes-Hut quadtree approximation [ESTIMATE — spring-embedder/stress methods, to source-verify]." },
    { "term": "hierarchical (layered) layout", "definition": "A layout for directed acyclic graphs that assigns nodes to layers, minimizes edge crossings between adjacent layers, then assigns coordinates, making prerequisite flow read top-to-bottom; requires cycles to be broken first for layout purposes [ESTIMATE — layered/Sugiyama-style, to source-verify]." },
    { "term": "edge bundling", "definition": "Routing near-parallel or hierarchically related edges along shared paths so a dense graph shows its overall flow instead of a hairball, trading away the traceability of any single edge [ESTIMATE — hierarchical/force-directed bundling, to source-verify]." },
    { "term": "label decluttering", "definition": "Placing node labels without overlap by collision detection, priority ordering and zoom thresholds (only important/visible nodes are labelled at coarse zoom); the underlying point-feature label-placement problem is NP-hard and solved greedily/heuristically [ESTIMATE — to source-verify]." },
    { "term": "mental-map preservation", "definition": "Keeping node positions stable across zoom levels, expansions and re-layouts (incremental layout, pinned anchors, animated transitions) so the user does not lose track of where things are when the view changes." },
    { "term": "honest abstraction", "definition": "The rule that a zoomed-out summary must faithfully represent what it hides — no invented groupings, member counts and coverage visible, mastery/high-yield roll-ups computed as real aggregates — so removing detail never implies a structure that is not in the data." }
  ],
  "nodes": [
    {
      "id": "VIEW_PROBLEM_FRAME",
      "topic": "drawn_concept_graph_view_problem_frame",
      "definition": "Frame the problem this KB solves: render the concept knowledge graph as an interactive semantic-zoom MAP — few category headings when zoomed out, progressively more concepts and edges with more concise-then-fuller names as the user zooms in — over data the app already models (Concept and ConceptEdge entities with prereq/related/part_of edge types) but does not yet draw. There is NO installed graph library and NO page rendering any graph today, and ConceptEdge is read only once in the pipeline's prerequisite check, so this KB specifies a view that must be built from scratch [FACT-source: APP_RECON §5.5]. The drawn view is a cartography problem (what to show at each scale) fused with a graph-drawing problem (where to put nodes and edges), kept honest and legible.",
      "group": "foundations",
      "dependencies": [],
      "competency_question_refs": ["CQ_01"],
      "scope_boundary": { "included": ["the semantic-zoom concept-map goal", "the map-plus-graph-drawing framing", "the app fact that no graph or library exists yet"], "excluded": ["the color/meaning encoding (neighboring KB)", "the graph's ontology and edge semantics (kgraph specialist)"] },
      "pros": [ { "claim": "Framing the view as cartography-plus-graph-drawing separates 'what to show at each scale' from 'where to place it', so generalization and layout are designed independently", "example": "deciding FK-area headings appear at the coarsest level before deciding the force-directed positions of the concepts inside them", "weight": 0.82 } ],
      "cons": [ { "claim": "The frame commits to a graph view for data that today lives only in entities, so it presumes a rendering effort the app has never had", "example": "specifying semantic zoom over ConceptEdge rows that no code currently even creates", "weight": 0.58 } ],
      "failure_modes": ["conflating the view's geometry with the graph's meaning owned by kgraph", "assuming a graph library or renderer already exists"],
      "acceptance_tests": ["the frame states the semantic-zoom concept-map goal, the cartography-plus-graph-drawing split, and the APP_RECON §5.5 fact that no graph or library exists yet"],
      "revisit_triggers": ["kgraph changes the node/edge schema this view draws", "a graph library is adopted", "the neighboring color KB changes what a node encodes"],
      "base": { "criticality": 0.86, "business_value": 0.76, "user_value": 0.82, "technical_complexity": 0.42, "risk_if_wrong": 0.7, "cross_topic_coupling": 0.84, "irreversibility": 0.35, "confidence": 0.7, "node_conflict_pressure": 0.34, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.85, "prior_importance": 0.86, "evidence_confidence": 0.66, "failure_rate": 0.16, "downside_weight": 0.45, "uncertainty_interval": [0.24, 0.52], "update_signal": "a schema change from kgraph or the adoption of a graph library revises the frame" }
    },
    {
      "id": "SEMANTIC_VS_GEOMETRIC_ZOOM",
      "topic": "semantic_versus_geometric_zoom_distinction",
      "definition": "Fix the core distinction that drives everything downstream. GEOMETRIC zoom scales one fixed picture: zooming in makes the same nodes and labels larger, zooming out makes them smaller until unreadable. SEMANTIC zoom changes WHAT is drawn and its representation at each level: a coarse level shows a handful of category headings, a fine level shows individual concepts with their own names and edges — new objects appear and old ones change form, not just size [ESTIMATE — zoomable-user-interface lineage, to source-verify]. The requirement 'few nodes/headings zoomed out, more detail and more concise-then-fuller names as you zoom in' is by definition a semantic-zoom requirement; geometric zoom alone cannot satisfy it because it cannot add or rename objects.",
      "group": "foundations",
      "dependencies": ["VIEW_PROBLEM_FRAME"],
      "competency_question_refs": ["CQ_01"],
      "scope_boundary": { "included": ["geometric vs semantic zoom definitions", "why the requirement is semantic not geometric", "objects appearing/changing form across levels"], "excluded": ["the discrete LOD-level budget structure", "the specific generalization operators"] },
      "pros": [ { "claim": "Naming the distinction prevents shipping a mere scale-transform and calling it semantic zoom", "example": "rejecting a design that just enlarges the whole hairball on zoom-in instead of revealing hidden concepts", "weight": 0.82 } ],
      "cons": [ { "claim": "Semantic zoom needs per-level representations authored for every element, far more work than a single scalable picture", "example": "maintaining heading, cluster and concept renderings of the same region for three zoom bands", "weight": 0.62 } ],
      "failure_modes": ["implementing geometric scaling and mislabelling it semantic zoom", "mixing the two so some elements rescale while others swap, confusingly"],
      "acceptance_tests": ["the node defines geometric vs semantic zoom, states objects appear/change form across semantic levels, and ties the requirement to semantic zoom with the ZUI lineage tagged ESTIMATE-to-source-verify"],
      "revisit_triggers": ["users report the zoom feels like mere scaling", "a hybrid zoom behavior is proposed", "the number of zoom bands changes"],
      "base": { "criticality": 0.84, "business_value": 0.74, "user_value": 0.82, "technical_complexity": 0.44, "risk_if_wrong": 0.68, "cross_topic_coupling": 0.7, "irreversibility": 0.35, "confidence": 0.72, "node_conflict_pressure": 0.42, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.84, "evidence_confidence": 0.66, "failure_rate": 0.14, "downside_weight": 0.42, "uncertainty_interval": [0.24, 0.52], "update_signal": "a scaling-only complaint or a hybrid-zoom proposal changes the distinction's application" }
    },
    {
      "id": "LOD_HIERARCHY",
      "topic": "level_of_detail_levels_and_complexity_budget",
      "definition": "Structure semantic zoom as a level-of-detail hierarchy: a small ladder of discrete zoom levels, each with an explicit visual-complexity BUDGET — a target ceiling on how many nodes, edges and labels may appear on screen so the view stays legible rather than a hairball [ESTIMATE — LOD from graphics and cartographic scale bands, to source-verify]. Each level's budget is what the generalization operators are applied to satisfy: the coarsest level might allow only a dozen heading meta-nodes, the finest a few hundred concept nodes within the viewport. Discrete levels (with hysteresis at the thresholds to avoid flicker) are chosen over continuous morphing because they make per-level authoring, labelling and testing tractable.",
      "group": "foundations",
      "dependencies": ["SEMANTIC_VS_GEOMETRIC_ZOOM"],
      "competency_question_refs": ["CQ_02"],
      "scope_boundary": { "included": ["discrete LOD levels each with an element budget", "budgets as the target the operators satisfy", "hysteresis at level thresholds to avoid flicker"], "excluded": ["the operators themselves", "the concrete per-level render map"] },
      "pros": [ { "claim": "A per-level element budget turns 'keep it legible' into a testable ceiling the generalization can be measured against", "example": "asserting the overview never draws more than ~15 headings so it cannot degrade into clutter", "weight": 0.82 } ],
      "cons": [ { "claim": "Discrete levels can feel abrupt at the thresholds and require authoring each band separately", "example": "a visible pop as a cluster suddenly expands into its members when a zoom threshold is crossed", "weight": 0.6 } ],
      "failure_modes": ["setting no budget so a level silently becomes a hairball", "placing level thresholds without hysteresis so the view flickers between levels"],
      "acceptance_tests": ["the node defines discrete LOD levels each with an element budget, frames budgets as what the operators satisfy, and specifies threshold hysteresis to avoid flicker"],
      "revisit_triggers": ["a level regularly exceeds its budget", "users report flicker at a zoom threshold", "the number of levels is changed"],
      "base": { "criticality": 0.84, "business_value": 0.74, "user_value": 0.8, "technical_complexity": 0.48, "risk_if_wrong": 0.68, "cross_topic_coupling": 0.78, "irreversibility": 0.35, "confidence": 0.7, "node_conflict_pressure": 0.4, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.84, "evidence_confidence": 0.64, "failure_rate": 0.16, "downside_weight": 0.45, "uncertainty_interval": [0.26, 0.54], "update_signal": "a budget overrun or a threshold-flicker report changes the level structure" }
    },
    {
      "id": "CARTO_GENERALIZATION_OPERATORS",
      "topic": "cartographic_generalization_operator_taxonomy",
      "definition": "Adopt the cartographic generalization operators as the vocabulary for meeting each LOD budget [ESTIMATE — operator taxonomy from map generalization, to source-verify]: SELECTION/ELIMINATION (drop or keep whole features), SIMPLIFICATION (reduce a feature's detail), AGGREGATION (merge many features into one), TYPIFICATION (replace a dense group by a representative few that preserve its pattern), DISPLACEMENT (nudge features apart so they remain distinct), and SYMBOLIZATION (change how a feature is drawn, e.g. a cluster becomes a heading glyph). Mapped to the graph: eliminate low-importance nodes at coarse zoom, aggregate clusters into meta-nodes, typify a hub's many leaves by a labelled count, displace overlapping nodes, and symbolize a collapsed region as a single heading. These operators are the concrete moves generalization makes; they are not app-specific and are borrowed wholesale from map-making.",
      "group": "generalization",
      "dependencies": ["LOD_HIERARCHY"],
      "competency_question_refs": ["CQ_03"],
      "scope_boundary": { "included": ["the selection/aggregation/simplification/typification/displacement/symbolization operator set", "their mapping onto graph nodes, edges and labels"], "excluded": ["the importance ranking that drives selection (its own node)", "the meta-node membership mapping (its own node)"] },
      "pros": [ { "claim": "A named operator set makes generalization a checklist of concrete moves rather than ad-hoc hiding", "example": "choosing typification (a labelled '+40 more') over blunt elimination when a hub has many leaf concepts", "weight": 0.8 } ],
      "cons": [ { "claim": "The operators are a cartographic import whose exact taxonomy this KB did not source-verify, so names/counts may differ from the canonical reference", "example": "treating typification and aggregation as cleanly separate when a specific source may split them differently", "weight": 0.6 } ],
      "failure_modes": ["hiding detail with no operator discipline so the overview is arbitrary", "applying displacement so aggressively that positions no longer reflect structure"],
      "acceptance_tests": ["the node enumerates selection/aggregation/simplification/typification/displacement/symbolization, maps them onto graph elements, and tags the taxonomy ESTIMATE-to-source-verify"],
      "revisit_triggers": ["the canonical operator taxonomy is source-verified and differs", "an operator proves unused or insufficient", "a new generalization move is needed"],
      "base": { "criticality": 0.8, "business_value": 0.72, "user_value": 0.76, "technical_complexity": 0.5, "risk_if_wrong": 0.64, "cross_topic_coupling": 0.76, "irreversibility": 0.35, "confidence": 0.64, "node_conflict_pressure": 0.4, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.8, "evidence_confidence": 0.6, "failure_rate": 0.16, "downside_weight": 0.42, "uncertainty_interval": [0.3, 0.58], "update_signal": "source-verifying the taxonomy or an unused operator changes the operator set" }
    },
    {
      "id": "SELECTION_ELIMINATION",
      "topic": "importance_ranked_selection_of_surviving_nodes",
      "definition": "Specify how the selection/elimination operator decides which nodes survive at coarse zoom: an IMPORTANCE RANKING orders every node, and only the top-ranked survive to the coarsest levels while the rest are eliminated or absorbed into an aggregate. Importance blends structural centrality (hub degree / prerequisite fan-out in the ConceptEdge graph), high-yield weight, and mastery relevance (a confidently weak concept the learner needs is worth keeping visible) [ESTIMATE — design synthesis over standard graph-centrality and the app's own importance signals]. A pure degree cut is rejected because it would drop a low-degree but high-yield or weak concept the user most needs; importance must be a deliberate blend so the overview surfaces what matters, not merely what is well-connected.",
      "group": "generalization",
      "dependencies": ["CARTO_GENERALIZATION_OPERATORS"],
      "competency_question_refs": ["CQ_04"],
      "scope_boundary": { "included": ["an importance ranking blending centrality, high-yield and mastery relevance", "top-ranked nodes surviving to coarse levels", "why a pure degree cut is rejected"], "excluded": ["the color encoding of high-yield/mastery (neighboring KB)", "the aggregation of eliminated nodes into meta-nodes (next node)"] },
      "pros": [ { "claim": "Ranking by a blended importance keeps the few nodes shown at overview the ones that actually matter to the learner", "example": "a low-degree but high-yield weak concept persisting into the overview while a well-connected trivial node is absorbed", "weight": 0.82 } ],
      "cons": [ { "claim": "The importance blend's weights are an unvalidated heuristic, so the wrong node can survive or be dropped until tuned", "example": "over-weighting degree and letting a hub of trivial siblings crowd out a critical concept", "weight": 0.62 } ],
      "failure_modes": ["cutting by degree alone and dropping a high-yield or weak concept", "letting the survivor set change unpredictably as importance is recomputed"],
      "acceptance_tests": ["the node defines an importance ranking blending centrality, high-yield and mastery relevance, has top-ranked nodes survive to coarse levels, and rejects a pure degree cut"],
      "revisit_triggers": ["an important concept is missing from the overview", "the importance weights are retuned", "a new importance signal (e.g. exam proximity) is added"],
      "base": { "criticality": 0.82, "business_value": 0.74, "user_value": 0.82, "technical_complexity": 0.5, "risk_if_wrong": 0.7, "cross_topic_coupling": 0.8, "irreversibility": 0.35, "confidence": 0.66, "node_conflict_pressure": 0.44, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.82, "evidence_confidence": 0.62, "failure_rate": 0.18, "downside_weight": 0.48, "uncertainty_interval": [0.28, 0.56], "update_signal": "a missing-important-node report or an importance retune changes the selection ranking" }
    },
    {
      "id": "AGGREGATION_META_NODES",
      "topic": "cluster_aggregation_into_expandable_meta_nodes",
      "definition": "Define aggregation: collapse a cluster of related concepts into a single META-NODE (a heading) at coarse zoom that expands into its member concepts as the user zooms in. The natural clusters for this app are the INBDE taxonomy groupings the pipeline already uses — FK areas and CC sections (mastery is already averaged per 'fk|N' and 'cc|DTP/OHM/PP') — so an FK area becomes one heading node zoomed out and its concepts appear zoomed in [FACT-source: APP_RECON — pipeline averages mastery per FK area and CC section]. The membership mapping (which concept belongs to which meta-node) must be FIXED and honest, not recomputed differently per frame, so a heading always contains the same members. Aggregation is what makes the coarse level a 'few headings' view and gives the expand/collapse interaction its structure.",
      "group": "aggregation",
      "dependencies": ["SELECTION_ELIMINATION"],
      "competency_question_refs": ["CQ_05"],
      "scope_boundary": { "included": ["collapsing clusters into meta-node headings and expanding into members", "using FK-area/CC-section groupings as the clusters", "a fixed honest membership mapping"], "excluded": ["the label text of headings vs members (next node)", "the honest-roll-up rules (its own node)"] },
      "pros": [ { "claim": "Aggregating on the app's existing FK/CC groupings gives ready-made, meaningful headings without inventing clusters", "example": "showing the FK areas as ~10 headings at overview, each expanding to its concepts, reusing the pipeline's own grouping", "weight": 0.84 } ],
      "cons": [ { "claim": "Fixed taxonomy clusters may not match the graph's actual edge structure, so a meta-node can group loosely connected concepts", "example": "an FK-area heading whose members are spread across the force-directed layout rather than sitting together", "weight": 0.62 } ],
      "failure_modes": ["recomputing membership per frame so a heading's contents shift", "aggregating on clusters that contradict the visible edge structure"],
      "acceptance_tests": ["the node describes collapse-to-heading / expand-to-members, uses FK-area and CC-section groupings as clusters (APP_RECON), and requires a fixed honest membership mapping"],
      "revisit_triggers": ["FK/CC groupings change or a graph-derived clustering is preferred", "meta-node members scatter in the layout", "the expand/collapse interaction is redesigned"],
      "base": { "criticality": 0.84, "business_value": 0.76, "user_value": 0.82, "technical_complexity": 0.5, "risk_if_wrong": 0.7, "cross_topic_coupling": 0.82, "irreversibility": 0.4, "confidence": 0.7, "node_conflict_pressure": 0.42, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.84, "evidence_confidence": 0.66, "failure_rate": 0.16, "downside_weight": 0.48, "uncertainty_interval": [0.26, 0.54], "update_signal": "a grouping change or scattered meta-node members changes the aggregation mapping" }
    },
    {
      "id": "LABEL_GENERALIZATION",
      "topic": "per_level_label_variants_concise_to_full",
      "definition": "Realize the 'concise names zoomed out, fuller names zoomed in' requirement as label generalization: each drawable element carries per-LOD-level label VARIANTS. At the coarsest level a meta-node shows a short category heading (e.g. 'Pharmacology'); at mid zoom a concept shows a concise name; at the finest zoom it shows its fuller, disambiguated name. Labels are authored or derived per level and keyed to the ZOOM_LOD_MAPPING, never by naive string truncation that could mangle a term [ESTIMATE — design synthesis; abbreviation must preserve meaning]. This is the label side of semantic zoom: as the user zooms in, both MORE labels appear (more nodes are labelled) and each label grows from a terse form toward its full concept name.",
      "group": "labels",
      "dependencies": ["AGGREGATION_META_NODES"],
      "competency_question_refs": ["CQ_06"],
      "scope_boundary": { "included": ["per-level label variants from heading to concise to full", "labels keyed to LOD level not naive truncation", "more nodes labelled and names lengthening on zoom-in"], "excluded": ["the collision-avoidance placement of labels (its own node)", "the LOD-to-render mapping (its own node)"] },
      "pros": [ { "claim": "Authoring label variants per level lets names be concise where space is tight and full where space allows, without truncation artifacts", "example": "'Pharmacology' as a heading becoming 'Beta-blocker pharmacodynamics' at the deepest zoom rather than a cut-off string", "weight": 0.82 } ],
      "cons": [ { "claim": "Per-level label variants must be authored or derived and kept in sync with the concept data, adding maintenance", "example": "a renamed concept whose concise and full variants both need updating to stay consistent", "weight": 0.6 } ],
      "failure_modes": ["truncating names naively and producing ambiguous or mangled labels", "showing the full name at coarse zoom so headings overflow"],
      "acceptance_tests": ["the node specifies per-level label variants (heading/concise/full), keys them to LOD level rather than naive truncation, and states both more labels and longer names appear on zoom-in"],
      "revisit_triggers": ["a concise label proves ambiguous", "concept names change and variants drift", "a level's label density is wrong"],
      "base": { "criticality": 0.82, "business_value": 0.74, "user_value": 0.84, "technical_complexity": 0.46, "risk_if_wrong": 0.66, "cross_topic_coupling": 0.76, "irreversibility": 0.35, "confidence": 0.7, "node_conflict_pressure": 0.4, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.82, "evidence_confidence": 0.64, "failure_rate": 0.16, "downside_weight": 0.44, "uncertainty_interval": [0.26, 0.54], "update_signal": "an ambiguous concise label or a name-sync drift changes the label-variant policy" }
    },
    {
      "id": "HONEST_ABSTRACTION",
      "topic": "faithful_non_misleading_zoomed_out_summaries",
      "definition": "Enforce the honesty contract on abstraction: a zoomed-out summary must faithfully represent what it hides. Concretely — a meta-node heading must group ONLY its real members (no invented or vanity groupings), it should expose member COUNT and coverage so the user knows how much is folded inside, and any rolled-up value (a heading's mastery or high-yield intensity) must be a REAL aggregate of its members, not a hand-picked or cosmetic number [ESTIMATE — design synthesis binding this KB to the factory honesty contract]. Hiding detail for legibility is legitimate; implying a structure, completeness or mastery that the data does not support is not. This is the visualization analogue of the KB honesty tags: the overview may say less, but everything it says must be true.",
      "group": "generalization",
      "dependencies": ["LABEL_GENERALIZATION", "AGGREGATION_META_NODES"],
      "competency_question_refs": ["CQ_07"],
      "scope_boundary": { "included": ["faithful member-only groupings", "showing member counts/coverage", "roll-ups as real aggregates not cosmetic numbers"], "excluded": ["the exact color mapping of a rolled-up value (neighboring KB)", "the statistical-chart honesty for analytics (neighboring KB)"] },
      "pros": [ { "claim": "Requiring faithful roll-ups and visible counts stops the overview from implying false structure or false mastery", "example": "a heading showing 'Pathology (24 concepts, 40% mastered)' as a true average rather than a flattering rounded-up figure", "weight": 0.84 } ],
      "cons": [ { "claim": "Honesty constraints can make the overview busier (counts, coverage) than a clean but misleading summary", "example": "adding member counts and coverage bars that a purely decorative overview would omit", "weight": 0.56 } ],
      "failure_modes": ["a heading whose rolled-up mastery is cosmetic rather than a true aggregate", "an aggregate implying full coverage when members are missing or truncated"],
      "acceptance_tests": ["the node requires member-only groupings, visible member counts/coverage, and real-aggregate roll-ups, framing abstraction as saying-less-but-true"],
      "revisit_triggers": ["a roll-up is found to misrepresent members", "truncation hides members from a count", "a new rolled-up value is displayed"],
      "base": { "criticality": 0.86, "business_value": 0.76, "user_value": 0.82, "technical_complexity": 0.44, "risk_if_wrong": 0.82, "cross_topic_coupling": 0.8, "irreversibility": 0.4, "confidence": 0.68, "node_conflict_pressure": 0.5, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.86, "evidence_confidence": 0.64, "failure_rate": 0.16, "downside_weight": 0.58, "uncertainty_interval": [0.26, 0.56], "update_signal": "a misrepresenting roll-up or a truncation-hidden member changes the honesty rules" }
    },
    {
      "id": "FORCE_DIRECTED_LAYOUT",
      "topic": "force_directed_spring_embedder_layout",
      "definition": "Specify force-directed layout as the default for the general (related-edge) graph: model edges as springs pulling connected nodes together and all node pairs as mutually repelling charges, then iterate until the total energy/stress is minimized, producing an organic layout where tightly linked concepts cluster and unrelated ones spread apart [ESTIMATE — spring-embedder and stress-majorization methods, to source-verify]. It needs no predefined hierarchy, adapts to arbitrary edge structure, and reveals community structure visually, which suits the 'related' ConceptEdge type. Its costs — a naive O(n^2) repulsion step, non-determinism, and instability under change — are addressed by the scaling and stability nodes that follow.",
      "group": "graphdraw",
      "dependencies": ["VIEW_PROBLEM_FRAME"],
      "competency_question_refs": ["CQ_08"],
      "scope_boundary": { "included": ["spring attraction plus pairwise repulsion minimizing energy/stress", "organic clustering suited to related edges", "naming its cost/instability handed to later nodes"], "excluded": ["the Barnes-Hut scaling approximation (next node)", "the layered layout for directed prereq edges (its own node)"] },
      "pros": [ { "claim": "Force-directed layout needs no predefined structure and visually clusters related concepts, matching the related-edge graph", "example": "concepts that share many 'related' edges settling into a visible community without any manual grouping", "weight": 0.82 } ],
      "cons": [ { "claim": "It is non-deterministic, unstable under edits, and naively O(n^2), so large or changing graphs need extra machinery", "example": "the same graph laid out twice giving mirrored or rotated results that disorient a returning user", "weight": 0.64 } ],
      "failure_modes": ["running naive O(n^2) repulsion on a large graph and freezing the browser", "letting non-determinism reposition everything on each load"],
      "acceptance_tests": ["the node defines spring attraction plus pairwise repulsion minimizing energy/stress, ties it to the related-edge graph, and names its O(n^2)/instability costs deferred to later nodes, tagged ESTIMATE-to-source-verify"],
      "revisit_triggers": ["layout non-determinism disorients users", "the graph grows past the naive-cost threshold", "a stress-majorization variant is adopted"],
      "base": { "criticality": 0.84, "business_value": 0.74, "user_value": 0.8, "technical_complexity": 0.58, "risk_if_wrong": 0.7, "cross_topic_coupling": 0.76, "irreversibility": 0.4, "confidence": 0.66, "node_conflict_pressure": 0.46, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.84, "evidence_confidence": 0.62, "failure_rate": 0.18, "downside_weight": 0.48, "uncertainty_interval": [0.28, 0.56], "update_signal": "a non-determinism complaint or a scale threshold crossing changes the layout choice" }
    },
    {
      "id": "BARNES_HUT_SCALING",
      "topic": "barnes_hut_quadtree_layout_scaling",
      "definition": "Address force-directed layout's scaling: the pairwise repulsion is O(n^2) per iteration, which does not survive a graph of hundreds-to-thousands of concepts. The standard remedy is a Barnes-Hut quadtree approximation that groups distant nodes into cells and approximates their combined repulsion, reducing each iteration to about O(n log n) [ESTIMATE — Barnes-Hut n-body approximation applied to graph layout, to source-verify]. Combined with a capped iteration count and a cooling schedule, this keeps layout interactive at the app's planned scale (the owner plans 2000+ cards, and the pipeline already imposes 500-row query caps that this view must respect) [FACT-source: APP_RECON — 2000-card plan and 500-row caps]. Scaling is a precondition for choosing a render target and for viewport culling.",
      "group": "graphdraw",
      "dependencies": ["FORCE_DIRECTED_LAYOUT"],
      "competency_question_refs": ["CQ_08"],
      "scope_boundary": { "included": ["the O(n^2)-to-O(n log n) Barnes-Hut approximation", "capped iterations and cooling for interactivity", "respecting the app's scale and 500-row caps"], "excluded": ["the render-target (SVG/canvas) choice (its own node)", "viewport culling (its own node)"] },
      "pros": [ { "claim": "Barnes-Hut brings force-directed cost down to roughly O(n log n), making layout feasible at the app's 2000+ node scale", "example": "laying out a two-thousand-concept graph in bounded iterations instead of a quadratic blow-up", "weight": 0.82 } ],
      "cons": [ { "claim": "The approximation trades exactness for speed and adds a quadtree parameter (opening angle) that mis-set can distort layout", "example": "too coarse an opening angle merging distinct clusters' repulsion and blurring their separation", "weight": 0.6 } ],
      "failure_modes": ["shipping naive O(n^2) and freezing at scale", "mis-tuning the quadtree approximation so clusters distort"],
      "acceptance_tests": ["the node gives the O(n^2)-to-O(n log n) Barnes-Hut approximation with capped iterations/cooling, and ties feasibility to the app's 2000+ scale and 500-row caps (APP_RECON), tagged ESTIMATE-to-source-verify"],
      "revisit_triggers": ["layout still stalls at scale", "the quadtree opening angle distorts clusters", "the 500-row cap forces paged loading"],
      "base": { "criticality": 0.8, "business_value": 0.72, "user_value": 0.74, "technical_complexity": 0.62, "risk_if_wrong": 0.68, "cross_topic_coupling": 0.72, "irreversibility": 0.4, "confidence": 0.64, "node_conflict_pressure": 0.42, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.8, "evidence_confidence": 0.6, "failure_rate": 0.2, "downside_weight": 0.46, "uncertainty_interval": [0.3, 0.58], "update_signal": "a persistent stall or a cap-forced paging changes the scaling approach" }
    },
    {
      "id": "HIERARCHICAL_LAYOUT",
      "topic": "layered_layout_for_prerequisite_dag",
      "definition": "Specify hierarchical (layered) layout for the DIRECTED prerequisite structure: the ConceptEdge 'prereq' edges form a directed graph whose flow (what must be learned before what) reads best as layers. A layered layout assigns each node to a layer consistent with edge direction, minimizes edge crossings between adjacent layers, then assigns coordinates so the prerequisite flow runs cleanly in one direction [ESTIMATE — layered/Sugiyama-style layout, to source-verify]. It requires the prereq subgraph to be acyclic; any cycle must be detected and an edge temporarily reversed for layout purposes only. Force-directed layout suits the 'related' edges while layered layout suits the 'prereq' edges, so the view uses each where it fits rather than forcing one algorithm on both edge types.",
      "group": "graphdraw",
      "dependencies": ["VIEW_PROBLEM_FRAME"],
      "competency_question_refs": ["CQ_09"],
      "scope_boundary": { "included": ["layer assignment, crossing minimization, coordinate assignment for prereq edges", "the DAG requirement and cycle-breaking for layout", "using layered for prereq and force-directed for related edges"], "excluded": ["the force-directed layout itself (prior node)", "edge bundling of the layered edges (its own node)"] },
      "pros": [ { "claim": "Layered layout makes prerequisite flow read directionally, which force-directed layout cannot show", "example": "foundational concepts sitting in top layers and dependent ones below so the learning order is visible", "weight": 0.82 } ],
      "cons": [ { "claim": "It demands an acyclic prereq graph and can sprawl wide, and mixing it with force-directed layout complicates the combined view", "example": "a prereq cycle in the data breaking layer assignment until an edge is reversed for layout", "weight": 0.62 } ],
      "failure_modes": ["running layered layout on a cyclic prereq graph and getting a broken assignment", "forcing one layout on both edge types and losing either flow or clustering"],
      "acceptance_tests": ["the node describes layer assignment, crossing minimization and coordinate assignment for prereq edges, requires a DAG with cycle-breaking, and splits layered-for-prereq vs force-directed-for-related, tagged ESTIMATE-to-source-verify"],
      "revisit_triggers": ["a prereq cycle appears in the data", "the layered view sprawls too wide", "the hybrid with force-directed proves confusing"],
      "base": { "criticality": 0.8, "business_value": 0.72, "user_value": 0.78, "technical_complexity": 0.6, "risk_if_wrong": 0.68, "cross_topic_coupling": 0.76, "irreversibility": 0.4, "confidence": 0.66, "node_conflict_pressure": 0.46, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.8, "evidence_confidence": 0.62, "failure_rate": 0.18, "downside_weight": 0.46, "uncertainty_interval": [0.28, 0.56], "update_signal": "a prereq cycle or a confusing hybrid changes the layered-layout use" }
    },
    {
      "id": "LAYOUT_STABILITY",
      "topic": "mental_map_preserving_stable_layout",
      "definition": "Preserve the user's mental map across zoom, expansion and data updates: node positions must stay stable so a user does not lose track when the view changes. Techniques are incremental layout (seed a new layout from the previous positions rather than from scratch), pinned anchors (fix meta-node/heading positions so their members expand around a known point), and deterministic seeding to defeat force-directed non-determinism [ESTIMATE — dynamic-graph mental-map preservation, to source-verify]. Stability trades against layout optimality: the mathematically best re-layout after every change would move everything, so the view accepts a slightly worse but stable layout. This is what lets expand/collapse and zoom feel like the same map gaining detail rather than a new picture each time.",
      "group": "graphdraw",
      "dependencies": ["FORCE_DIRECTED_LAYOUT", "HIERARCHICAL_LAYOUT"],
      "competency_question_refs": ["CQ_11"],
      "scope_boundary": { "included": ["incremental layout, pinned anchors and deterministic seeding", "the stability-vs-optimality trade", "keeping the map recognizable across changes"], "excluded": ["the animated transitions that show the change (its own node)", "the render target that draws it (its own node)"] },
      "pros": [ { "claim": "Stable, incremental layout keeps the graph recognizable across zoom and edits, protecting the user's spatial memory", "example": "expanding a heading and seeing its members grow around its fixed position instead of the whole graph rearranging", "weight": 0.84 } ],
      "cons": [ { "claim": "Prioritizing stability yields a slightly sub-optimal layout and can accumulate distortion as the graph changes", "example": "an incremental layout keeping an old, now-crowded arrangement rather than the cleaner from-scratch one", "weight": 0.6 } ],
      "failure_modes": ["re-laying out from scratch on every change so nodes jump", "over-pinning so the layout can never improve as data grows"],
      "acceptance_tests": ["the node specifies incremental layout, pinned anchors and deterministic seeding, states the stability-vs-optimality trade, and ties it to preserving the mental map, tagged ESTIMATE-to-source-verify"],
      "revisit_triggers": ["users report nodes jumping between views", "accumulated incremental distortion needs a reset", "a from-scratch relayout is proposed"],
      "base": { "criticality": 0.84, "business_value": 0.74, "user_value": 0.84, "technical_complexity": 0.56, "risk_if_wrong": 0.7, "cross_topic_coupling": 0.8, "irreversibility": 0.4, "confidence": 0.66, "node_conflict_pressure": 0.48, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.84, "evidence_confidence": 0.62, "failure_rate": 0.18, "downside_weight": 0.5, "uncertainty_interval": [0.28, 0.56], "update_signal": "a nodes-jumping report or accumulated distortion changes the stability policy" }
    },
    {
      "id": "EDGE_BUNDLING",
      "topic": "edge_bundling_for_hairball_reduction",
      "definition": "Apply edge bundling to control clutter when many edges cross: route near-parallel or hierarchically related edges along shared curved paths so the drawing shows overall flow between regions instead of a hairball of individual lines [ESTIMATE — hierarchical and force-directed edge bundling, to source-verify]. Bundling dramatically improves the readability of the big picture at a real cost: individual edges become hard to trace end-to-end, so it is applied at coarse zoom (where flow matters) and relaxed at fine zoom (where a user follows specific prerequisite links). It complements aggregation — aggregation reduces node clutter, bundling reduces edge clutter — and both are how the overview stays legible within its LOD budget.",
      "group": "graphdraw",
      "dependencies": ["HIERARCHICAL_LAYOUT"],
      "competency_question_refs": ["CQ_10"],
      "scope_boundary": { "included": ["routing related edges along shared paths to show flow", "the readability-vs-traceability trade", "bundling at coarse zoom and relaxing at fine zoom"], "excluded": ["node-level aggregation (prior node)", "label decluttering (its own node)"] },
      "pros": [ { "claim": "Bundling turns a dense edge hairball into readable inter-region flows, improving the overview within its budget", "example": "hundreds of cross-cluster edges rendered as a few thick bundles between FK-area headings", "weight": 0.8 } ],
      "cons": [ { "claim": "Bundled edges are hard to trace individually and can imply connections between nodes that are merely routed together", "example": "a user unable to tell which specific prerequisite a bundle contains, or misreading two co-routed edges as related", "weight": 0.66 } ],
      "failure_modes": ["bundling at fine zoom where users need to trace single edges", "bundles implying adjacency that the underlying edges do not have"],
      "acceptance_tests": ["the node describes routing related edges along shared paths, states the readability-vs-traceability trade, and bundles at coarse zoom while relaxing at fine zoom, tagged ESTIMATE-to-source-verify"],
      "revisit_triggers": ["users cannot trace a needed edge", "bundles are read as false adjacency", "edge density at a level changes"],
      "base": { "criticality": 0.76, "business_value": 0.68, "user_value": 0.74, "technical_complexity": 0.58, "risk_if_wrong": 0.62, "cross_topic_coupling": 0.72, "irreversibility": 0.35, "confidence": 0.62, "node_conflict_pressure": 0.46, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.76, "evidence_confidence": 0.6, "failure_rate": 0.18, "downside_weight": 0.44, "uncertainty_interval": [0.3, 0.58], "update_signal": "an untraceable-edge report or false-adjacency reading changes the bundling policy" }
    },
    {
      "id": "LABEL_PLACEMENT_DECLUTTER",
      "topic": "label_collision_placement_and_decluttering",
      "definition": "Place labels without overlap: node label placement is the classic point-feature label-placement problem, which is NP-hard, so it is solved with a greedy priority heuristic — order nodes by importance, try candidate label positions around each, and place a label only if it clears already-placed labels and passes the current zoom threshold [ESTIMATE — NP-hard label placement and greedy/heuristic solvers, to source-verify]. Below a zoom level only high-importance nodes are labelled at all; as the user zooms in, the element budget grows and more labels appear. Decluttering combines with SELECTION_ELIMINATION (only surviving nodes compete for labels) so the two never fight, and it is what keeps text readable instead of an overlapping smear.",
      "group": "labels",
      "dependencies": ["LABEL_GENERALIZATION", "SELECTION_ELIMINATION"],
      "competency_question_refs": ["CQ_10"],
      "scope_boundary": { "included": ["greedy priority label placement with collision detection", "zoom-threshold gating so only important nodes are labelled at coarse zoom", "coupling with selection so labels and survivors agree"], "excluded": ["the per-level label text variants (prior node)", "edge bundling (prior node)"] },
      "pros": [ { "claim": "Priority placement with collision detection keeps labels readable and puts the scarce label slots on the most important nodes", "example": "at overview only the FK headings carry labels while minor concepts stay unlabelled until zoom-in", "weight": 0.82 } ],
      "cons": [ { "claim": "Greedy placement is only heuristic, so it can leave an important label unplaced or flicker labels on and off near thresholds", "example": "a label appearing and disappearing as the user hovers around a zoom threshold", "weight": 0.62 } ],
      "failure_modes": ["labelling every node and producing an overlapping smear", "greedy order dropping a high-importance label while placing trivial ones"],
      "acceptance_tests": ["the node states label placement is NP-hard, uses a greedy priority solver with collision detection and zoom-threshold gating, and couples with selection so labels and survivors agree, tagged ESTIMATE-to-source-verify"],
      "revisit_triggers": ["important labels are dropped or labels flicker at thresholds", "label density at a level is wrong", "a better placement solver is adopted"],
      "base": { "criticality": 0.78, "business_value": 0.7, "user_value": 0.8, "technical_complexity": 0.56, "risk_if_wrong": 0.64, "cross_topic_coupling": 0.74, "irreversibility": 0.35, "confidence": 0.64, "node_conflict_pressure": 0.44, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.78, "evidence_confidence": 0.62, "failure_rate": 0.18, "downside_weight": 0.44, "uncertainty_interval": [0.3, 0.58], "update_signal": "a dropped-label or flicker report changes the placement heuristic" }
    },
    {
      "id": "ZOOM_LOD_MAPPING",
      "topic": "concrete_lod_level_to_render_mapping",
      "definition": "Bind the abstractions to concrete render decisions: a explicit table mapping each LOD level to what is drawn. A workable mapping for this app [ESTIMATE — design synthesis over the FK/CC groupings and LOD budgets]: level 0 (farthest) = only FK-area / CC-section heading meta-nodes with short labels and bundled inter-heading flows; level 1 = headings expanded into concept clusters, top-importance concepts labelled; level 2 = individual concept nodes with concise names and their edges within the viewport; level 3 (closest) = full concept names, all local edges unbundled, and hover-ready detail. Each level obeys its LOD_HIERARCHY budget via the generalization operators, draws the LABEL_GENERALIZATION variant for that level, and defines exactly what appears/disappears at each threshold — the single source of truth that turns 'semantic zoom' into a specific, testable behavior.",
      "group": "aggregation",
      "dependencies": ["LOD_HIERARCHY", "AGGREGATION_META_NODES", "LABEL_GENERALIZATION"],
      "competency_question_refs": ["CQ_06", "CQ_12"],
      "scope_boundary": { "included": ["a concrete level-0-to-3 render map for this app", "each level obeying its budget, operators and label variant", "defining what appears/disappears at each threshold"], "excluded": ["viewport culling within a level (next node)", "the transition animation between levels (its own node)"] },
      "pros": [ { "claim": "A concrete level-to-render table makes semantic zoom testable — each level's exact contents can be asserted", "example": "a test that level 0 shows only heading meta-nodes and never an individual concept node", "weight": 0.84 } ],
      "cons": [ { "claim": "A fixed four-level map may not fit every graph density, needing per-graph tuning of the thresholds and contents", "example": "a sparse subject where level 1 and level 2 look nearly identical and one level is wasted", "weight": 0.6 } ],
      "failure_modes": ["levels whose contents overlap so zoom reveals nothing new", "a mapping that ignores its own LOD budget and over-draws a level"],
      "acceptance_tests": ["the node gives a concrete level-0-to-3 render map, has each level obey its budget/operators/label-variant, and defines what appears and disappears at each threshold"],
      "revisit_triggers": ["a level reveals nothing new versus its neighbor", "graph density makes the level count wrong", "budgets are re-set"],
      "base": { "criticality": 0.86, "business_value": 0.78, "user_value": 0.84, "technical_complexity": 0.54, "risk_if_wrong": 0.72, "cross_topic_coupling": 0.84, "irreversibility": 0.4, "confidence": 0.68, "node_conflict_pressure": 0.44, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.86, "evidence_confidence": 0.64, "failure_rate": 0.16, "downside_weight": 0.5, "uncertainty_interval": [0.26, 0.54], "update_signal": "overlapping levels or a density mismatch changes the LOD-render map" }
    },
    {
      "id": "VIEWPORT_CULLING",
      "topic": "viewport_culling_and_progressive_loading",
      "definition": "Render and lay out only what is needed: cull to the current viewport plus a neighborhood margin so nodes far off-screen are neither drawn nor force-simulated, and load graph data progressively (by cluster or by expanded region) rather than all at once. This is what keeps the interaction smooth at scale and it must respect the app's hard 500-row query caps, which silently truncate large result sets — so the view pages or clusters its data instead of assuming the whole graph is in memory [FACT-source: APP_RECON — 500-row caps with silent truncation]. Culling composes with the ZOOM_LOD_MAPPING (a coarse level already shows few meta-nodes) and with Barnes-Hut scaling (fewer active nodes per frame), together bounding the work per frame regardless of total graph size.",
      "group": "aggregation",
      "dependencies": ["ZOOM_LOD_MAPPING"],
      "competency_question_refs": ["CQ_12"],
      "scope_boundary": { "included": ["viewport-plus-margin culling of draw and simulation", "progressive/paged data loading respecting 500-row caps", "composing with LOD and Barnes-Hut to bound per-frame work"], "excluded": ["the level-to-render map itself (prior node)", "the SVG/canvas render-target choice (its own node)"] },
      "pros": [ { "claim": "Culling to the viewport bounds per-frame work so interaction stays smooth no matter how large the total graph is", "example": "panning a 2000-node graph while only the ~100 on-screen nodes are simulated and drawn", "weight": 0.82 } ],
      "cons": [ { "claim": "Off-screen culling means off-screen structure is unknown until loaded, and paged loading can hitch when new regions stream in", "example": "a brief pause as a newly panned-to cluster loads its concepts and edges past the 500-row cap", "weight": 0.6 } ],
      "failure_modes": ["assuming the whole graph fits in one query and hitting silent 500-row truncation", "culling so tightly that edges to just-off-screen nodes vanish"],
      "acceptance_tests": ["the node specifies viewport-plus-margin culling, progressive/paged loading respecting the 500-row caps (APP_RECON), and composition with LOD and Barnes-Hut to bound per-frame work"],
      "revisit_triggers": ["a large graph hits the 500-row cap", "paged loading hitches on pan", "off-screen edges disappear visibly"],
      "base": { "criticality": 0.8, "business_value": 0.72, "user_value": 0.76, "technical_complexity": 0.56, "risk_if_wrong": 0.68, "cross_topic_coupling": 0.76, "irreversibility": 0.35, "confidence": 0.66, "node_conflict_pressure": 0.4, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.8, "evidence_confidence": 0.64, "failure_rate": 0.18, "downside_weight": 0.46, "uncertainty_interval": [0.28, 0.56], "update_signal": "a 500-row truncation or a pan hitch changes the culling/loading policy" }
    },
    {
      "id": "RENDER_TARGET_SVG_CANVAS",
      "topic": "svg_versus_canvas_webgl_render_target",
      "definition": "Choose the render target by scale. SVG draws each node/edge/label as a DOM element: crisp text, easy hit-testing and CSS styling, but the DOM bogs down past roughly a few thousand elements. Canvas (or WebGL) draws to a bitmap: it scales to far more elements but loses DOM convenience, so labels and interaction must be handled manually [ESTIMATE — standard SVG-vs-canvas rendering trade, to source-verify]. The rule: use SVG at coarse LOD levels and small graphs where crisp labels matter most, and switch to canvas/WebGL above a node-count threshold or at fine zoom over dense regions. Because the app ships no graph renderer today, this choice is open and should be made against the actual element counts the ZOOM_LOD_MAPPING and viewport culling produce, not guessed [FACT-source: APP_RECON §5.5 — no graph library installed].",
      "group": "graphdraw",
      "dependencies": ["BARNES_HUT_SCALING", "VIEWPORT_CULLING"],
      "competency_question_refs": ["CQ_13"],
      "scope_boundary": { "included": ["SVG (crisp DOM, small scale) vs canvas/WebGL (bitmap, large scale) trade", "the node-count threshold to switch", "deciding against real culled element counts"], "excluded": ["the build-vs-adopt library decision (next node)", "the color styling of elements (neighboring KB)"] },
      "pros": [ { "claim": "Matching render target to element count keeps text crisp when it can and performance intact when it must scale", "example": "SVG headings at overview for sharp labels, canvas for a dense fine-zoom region of hundreds of nodes", "weight": 0.82 } ],
      "cons": [ { "claim": "Supporting both targets doubles the rendering path, and canvas loses the free hit-testing and styling SVG gives", "example": "reimplementing label placement and click detection by hand once a region switches to canvas", "weight": 0.64 } ],
      "failure_modes": ["forcing SVG at a scale that bogs the DOM", "jumping to canvas prematurely and rebuilding interaction for no benefit"],
      "acceptance_tests": ["the node states the SVG-vs-canvas/WebGL trade, gives a node-count switch threshold, decides against real culled element counts, and cites APP_RECON §5.5 that no renderer exists yet"],
      "revisit_triggers": ["the DOM bogs at a level still on SVG", "canvas interaction cost outweighs its benefit", "a WebGL renderer is considered"],
      "base": { "criticality": 0.78, "business_value": 0.72, "user_value": 0.74, "technical_complexity": 0.58, "risk_if_wrong": 0.64, "cross_topic_coupling": 0.72, "irreversibility": 0.4, "confidence": 0.66, "node_conflict_pressure": 0.42, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.78, "evidence_confidence": 0.62, "failure_rate": 0.16, "downside_weight": 0.44, "uncertainty_interval": [0.28, 0.56], "update_signal": "a DOM bog or an interaction-cost finding changes the render-target rule" }
    },
    {
      "id": "TRANSITION_ANIMATION",
      "topic": "animated_lod_transitions_object_constancy",
      "definition": "Animate the transitions between LOD levels and expand/collapse so the change is legible: when a heading expands, its members should visibly grow out of its position; when the user zooms out, concepts should visibly coalesce back into their heading. Smooth tweening of position, size and opacity preserves OBJECT CONSTANCY — the perceptual sense that the same things moved rather than a new screen replaced the old — which is what lets the user follow where a category became its concepts and back [ESTIMATE — animated-transition/object-constancy principles, to source-verify]. Animations must be brief and interruptible, and to stay performant they animate only viewport elements (leaning on culling) and cap the number of simultaneously tweened items; a heavy animation on a large graph is worse than an instant cut.",
      "group": "labels",
      "dependencies": ["LAYOUT_STABILITY", "ZOOM_LOD_MAPPING"],
      "competency_question_refs": ["CQ_11"],
      "scope_boundary": { "included": ["tweening position/size/opacity across LOD and expand/collapse", "object constancy so the user tracks the change", "keeping animation brief, interruptible and viewport-bounded"], "excluded": ["the underlying layout stability that supplies start/end positions (prior node)", "the render target that draws frames (prior node)"] },
      "pros": [ { "claim": "Animated transitions give object constancy so users track where a heading became its members instead of being jump-cut", "example": "an FK heading smoothly splitting into its concept nodes so the user sees the relationship", "weight": 0.82 } ],
      "cons": [ { "claim": "Animation is expensive on large graphs and, if slow, is worse than an instant transition", "example": "a janky multi-hundred-node tween that stutters and disorients rather than clarifies", "weight": 0.62 } ],
      "failure_modes": ["animating the whole graph instead of viewport elements and stuttering", "long non-interruptible animations that block interaction"],
      "acceptance_tests": ["the node tweens position/size/opacity for object constancy, keeps animations brief, interruptible and viewport-bounded, and prefers an instant cut over a janky animation, tagged ESTIMATE-to-source-verify"],
      "revisit_triggers": ["transitions stutter on large graphs", "users still lose track across a transition", "animation blocks interaction"],
      "base": { "criticality": 0.74, "business_value": 0.68, "user_value": 0.8, "technical_complexity": 0.52, "risk_if_wrong": 0.6, "cross_topic_coupling": 0.74, "irreversibility": 0.3, "confidence": 0.66, "node_conflict_pressure": 0.4, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.74, "evidence_confidence": 0.62, "failure_rate": 0.18, "downside_weight": 0.4, "uncertainty_interval": [0.28, 0.56], "update_signal": "a stutter or lost-track report changes the transition policy" }
    },
    {
      "id": "LIBRARY_SELECTION",
      "topic": "build_versus_adopt_graph_library_decision",
      "definition": "Decide how the drawn view is actually built, given the app installs no graph library today [FACT-source: APP_RECON §5.5]. The choice is build-custom versus adopt a proven library. Adopting a mature graph/layout library gives force-directed and often layered layout, canvas/WebGL rendering and interaction out of the box, at the cost of fitting its model and bundle size; building custom gives exact control over semantic-zoom LOD behavior at a large engineering cost and risk. The decision criteria: does a candidate library support semantic zoom / per-level LOD, stable incremental layout, the required scale, and canvas rendering — if yes, adopt and layer this KB's generalization on top; if its zoom model cannot express the LOD mapping, build the LOD/generalization layer custom over its primitives. Specific library names and any performance figures are a research-and-benchmark decision, not asserted here [UNKNOWN — candidate libraries and their benchmarks; METHOD: prototype the LOD mapping against 2-3 candidates and measure at the app's node counts].",
      "group": "synthesis",
      "dependencies": ["RENDER_TARGET_SVG_CANVAS", "HIERARCHICAL_LAYOUT"],
      "competency_question_refs": ["CQ_13"],
      "scope_boundary": { "included": ["the build-vs-adopt decision and its criteria", "requiring semantic-zoom/LOD, stable layout, scale and canvas support", "carrying specific libraries and benchmarks as UNKNOWN with a method"], "excluded": ["asserting a specific library or performance number (unknown)", "the final composed verdict (next node)"] },
      "pros": [ { "claim": "Explicit build-vs-adopt criteria stop both a doomed from-scratch build and a library that cannot express semantic zoom", "example": "adopting a library for layout and rendering but building the LOD/generalization layer if its zoom model is too rigid", "weight": 0.82 } ],
      "cons": [ { "claim": "Without a benchmark the decision stays open, and either path carries risk — library lock-in or custom engineering cost", "example": "committing to a library whose fixed zoom cannot host the four-level LOD mapping and having to wrap or replace it", "weight": 0.64 } ],
      "failure_modes": ["adopting a library whose zoom model cannot express the LOD mapping", "building everything custom when a library would have sufficed"],
      "acceptance_tests": ["the node states build-vs-adopt criteria (semantic-zoom/LOD, stable layout, scale, canvas), cites APP_RECON §5.5, and carries specific libraries/benchmarks as UNKNOWN with a prototype-and-measure method rather than inventing names or numbers"],
      "revisit_triggers": ["a candidate library is prototyped and benchmarked", "a library's zoom model blocks the LOD mapping", "bundle-size or licensing rules out a candidate"],
      "base": { "criticality": 0.8, "business_value": 0.74, "user_value": 0.72, "technical_complexity": 0.6, "risk_if_wrong": 0.68, "cross_topic_coupling": 0.78, "irreversibility": 0.5, "confidence": 0.6, "node_conflict_pressure": 0.46, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.8, "evidence_confidence": 0.56, "failure_rate": 0.2, "downside_weight": 0.5, "uncertainty_interval": [0.32, 0.6], "update_signal": "a library prototype/benchmark or a zoom-model blocker changes the build-vs-adopt call" }
    },
    {
      "id": "VIEW_SPEC_VERDICT",
      "topic": "composed_semantic_zoom_view_go_no_go_verdict",
      "definition": "Compose the drawn-view specification this KB builds toward, the go/no-go for how the concept graph is rendered. Go: SEMANTIC zoom (not geometric) structured as a small LOD hierarchy with per-level budgets; cartographic generalization (importance-ranked selection, FK/CC aggregation into expandable headings, typification, symbolization) meeting each budget; a HYBRID layout — force-directed for related edges, layered for prereq edges — kept stable/incremental for the mental map; edge bundling and greedy label decluttering to stay legible; honest abstraction (member counts, real roll-ups, no invented structure); per-level label variants (concise out, full in); animated object-constancy transitions; and viewport culling with a scale-driven SVG-or-canvas render target. No-go: do NOT ship geometric-only zoom and call it semantic; do NOT abstract dishonestly (cosmetic roll-ups, invented groupings); do NOT assert a specific graph library or performance number until prototyped and measured [UNKNOWN carried, METHOD given]. A choice earns a place only if it is legible within its LOD budget, honest about what it hides, and stable across zoom.",
      "group": "synthesis",
      "dependencies": ["ZOOM_LOD_MAPPING", "HONEST_ABSTRACTION", "LAYOUT_STABILITY", "EDGE_BUNDLING", "LABEL_PLACEMENT_DECLUTTER", "LIBRARY_SELECTION"],
      "competency_question_refs": ["CQ_13"],
      "scope_boundary": { "included": ["the go stack (semantic zoom + LOD generalization + hybrid stable layout + bundling + decluttered honest labels + transitions + culled render target)", "the no-go on geometric-only zoom, dishonest abstraction and invented library/benchmark claims", "the legible/honest/stable acceptance rule"], "excluded": ["the color/meaning encoding and analytics charts (neighboring KBs)", "the graph's ontology (kgraph specialist)"] },
      "pros": [ { "claim": "The merged verdict commits to a legible, honest, stable semantic-zoom view and explicitly refuses the cheap or dishonest shortcuts", "example": "shipping importance-ranked LOD generalization with real roll-ups while refusing geometric-only zoom and cosmetic aggregates", "weight": 0.86 } ],
      "cons": [ { "claim": "A single composed spec defers the library/benchmark call to future prototyping and may over-constrain a graph whose density wants a different level structure", "example": "the four-level mapping fitting most subjects but needing rework for an unusually dense or sparse one", "weight": 0.6 } ],
      "failure_modes": ["shipping geometric-only zoom or dishonest abstraction against the no-go", "asserting a library or performance number before prototyping"],
      "acceptance_tests": ["the verdict states the go stack (semantic zoom + LOD generalization + hybrid stable layout + bundling + decluttered honest labels + transitions + culled render target), the no-go on geometric-only zoom/dishonest abstraction/invented library claims, and the legible-honest-stable acceptance rule"],
      "revisit_triggers": ["a graph library is prototyped and benchmarked", "a density mismatch forces a level-structure change", "the neighboring color/chart KBs change what a node must encode"],
      "base": { "criticality": 0.9, "business_value": 0.82, "user_value": 0.86, "technical_complexity": 0.58, "risk_if_wrong": 0.8, "cross_topic_coupling": 0.88, "irreversibility": 0.5, "confidence": 0.66, "node_conflict_pressure": 0.5, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.8, "prior_importance": 0.9, "evidence_confidence": 0.62, "failure_rate": 0.18, "downside_weight": 0.56, "uncertainty_interval": [0.28, 0.58], "update_signal": "a library benchmark, a density mismatch, or a neighboring-KB change revises the verdict" }
    }
  ],
  "edges": [
    { "from": "VIEW_PROBLEM_FRAME", "to": "SEMANTIC_VS_GEOMETRIC_ZOOM", "edge_type": "dependency", "why_related": "framing the drawn map sets up the geometric-vs-semantic zoom choice", "relation_strength": 0.82 },
    { "from": "SEMANTIC_VS_GEOMETRIC_ZOOM", "to": "LOD_HIERARCHY", "edge_type": "dependency", "why_related": "semantic zoom is structured as the discrete level-of-detail hierarchy", "relation_strength": 0.82 },
    { "from": "LOD_HIERARCHY", "to": "CARTO_GENERALIZATION_OPERATORS", "edge_type": "dependency", "why_related": "the operators are applied to meet each LOD level's complexity budget", "relation_strength": 0.8 },
    { "from": "CARTO_GENERALIZATION_OPERATORS", "to": "SELECTION_ELIMINATION", "edge_type": "dependency", "why_related": "selection/elimination is the operator that decides which nodes survive", "relation_strength": 0.8 },
    { "from": "SELECTION_ELIMINATION", "to": "AGGREGATION_META_NODES", "edge_type": "dependency", "why_related": "eliminated/absorbed nodes are aggregated into meta-node headings", "relation_strength": 0.8 },
    { "from": "AGGREGATION_META_NODES", "to": "LABEL_GENERALIZATION", "edge_type": "dependency", "why_related": "meta-node headings and their members are what the per-level labels name", "relation_strength": 0.8 },
    { "from": "LABEL_GENERALIZATION", "to": "HONEST_ABSTRACTION", "edge_type": "dependency", "why_related": "honest abstraction constrains what the generalized labels and roll-ups may claim", "relation_strength": 0.78 },
    { "from": "AGGREGATION_META_NODES", "to": "HONEST_ABSTRACTION", "edge_type": "dependency", "why_related": "aggregation is what honest abstraction requires to faithfully summarize members", "relation_strength": 0.78 },
    { "from": "VIEW_PROBLEM_FRAME", "to": "FORCE_DIRECTED_LAYOUT", "edge_type": "dependency", "why_related": "the drawn view needs a layout, defaulting to force-directed for related edges", "relation_strength": 0.8 },
    { "from": "FORCE_DIRECTED_LAYOUT", "to": "BARNES_HUT_SCALING", "edge_type": "dependency", "why_related": "Barnes-Hut approximates the force-directed repulsion to scale it", "relation_strength": 0.8 },
    { "from": "VIEW_PROBLEM_FRAME", "to": "HIERARCHICAL_LAYOUT", "edge_type": "dependency", "why_related": "the prereq edges need a layered layout the frame's graph structure implies", "relation_strength": 0.78 },
    { "from": "FORCE_DIRECTED_LAYOUT", "to": "LAYOUT_STABILITY", "edge_type": "dependency", "why_related": "force-directed non-determinism is the instability the stability node must tame", "relation_strength": 0.8 },
    { "from": "HIERARCHICAL_LAYOUT", "to": "LAYOUT_STABILITY", "edge_type": "dependency", "why_related": "the layered layout's positions must also stay stable across changes", "relation_strength": 0.74 },
    { "from": "HIERARCHICAL_LAYOUT", "to": "EDGE_BUNDLING", "edge_type": "dependency", "why_related": "edge bundling routes the layered prereq edges to reduce clutter", "relation_strength": 0.76 },
    { "from": "LABEL_GENERALIZATION", "to": "LABEL_PLACEMENT_DECLUTTER", "edge_type": "dependency", "why_related": "the per-level label variants are what the placement/declutter step positions without overlap", "relation_strength": 0.8 },
    { "from": "SELECTION_ELIMINATION", "to": "LABEL_PLACEMENT_DECLUTTER", "edge_type": "dependency", "why_related": "only surviving nodes from selection compete for label slots", "relation_strength": 0.76 },
    { "from": "LOD_HIERARCHY", "to": "ZOOM_LOD_MAPPING", "edge_type": "dependency", "why_related": "the LOD levels and budgets are what the render map concretizes", "relation_strength": 0.8 },
    { "from": "AGGREGATION_META_NODES", "to": "ZOOM_LOD_MAPPING", "edge_type": "dependency", "why_related": "the meta-node headings are the coarse levels of the render map", "relation_strength": 0.78 },
    { "from": "LABEL_GENERALIZATION", "to": "ZOOM_LOD_MAPPING", "edge_type": "dependency", "why_related": "each mapped level draws its own label variant", "relation_strength": 0.76 },
    { "from": "ZOOM_LOD_MAPPING", "to": "VIEWPORT_CULLING", "edge_type": "dependency", "why_related": "culling bounds the per-frame work within each mapped level", "relation_strength": 0.78 },
    { "from": "BARNES_HUT_SCALING", "to": "RENDER_TARGET_SVG_CANVAS", "edge_type": "dependency", "why_related": "the scaled layout's element counts inform the SVG-vs-canvas choice", "relation_strength": 0.74 },
    { "from": "VIEWPORT_CULLING", "to": "RENDER_TARGET_SVG_CANVAS", "edge_type": "dependency", "why_related": "the culled element count is what the render target is chosen against", "relation_strength": 0.76 },
    { "from": "LAYOUT_STABILITY", "to": "TRANSITION_ANIMATION", "edge_type": "dependency", "why_related": "stable start/end positions are what the transitions tween between", "relation_strength": 0.8 },
    { "from": "ZOOM_LOD_MAPPING", "to": "TRANSITION_ANIMATION", "edge_type": "dependency", "why_related": "transitions animate the change between the mapped LOD levels", "relation_strength": 0.76 },
    { "from": "RENDER_TARGET_SVG_CANVAS", "to": "LIBRARY_SELECTION", "edge_type": "dependency", "why_related": "the render-target need is a criterion for the build-vs-adopt library choice", "relation_strength": 0.76 },
    { "from": "HIERARCHICAL_LAYOUT", "to": "LIBRARY_SELECTION", "edge_type": "dependency", "why_related": "whether a library supplies layered layout drives adopt-vs-build", "relation_strength": 0.72 },
    { "from": "ZOOM_LOD_MAPPING", "to": "VIEW_SPEC_VERDICT", "edge_type": "dependency", "why_related": "the verdict commits to the concrete LOD render map", "relation_strength": 0.8 },
    { "from": "HONEST_ABSTRACTION", "to": "VIEW_SPEC_VERDICT", "edge_type": "dependency", "why_related": "the verdict's no-go on dishonest abstraction comes from this node", "relation_strength": 0.78 },
    { "from": "LAYOUT_STABILITY", "to": "VIEW_SPEC_VERDICT", "edge_type": "dependency", "why_related": "the verdict requires stable layout for the mental map", "relation_strength": 0.76 },
    { "from": "EDGE_BUNDLING", "to": "VIEW_SPEC_VERDICT", "edge_type": "dependency", "why_related": "the verdict includes edge bundling for legibility", "relation_strength": 0.72 },
    { "from": "LABEL_PLACEMENT_DECLUTTER", "to": "VIEW_SPEC_VERDICT", "edge_type": "dependency", "why_related": "the verdict includes decluttered labels", "relation_strength": 0.74 },
    { "from": "LIBRARY_SELECTION", "to": "VIEW_SPEC_VERDICT", "edge_type": "dependency", "why_related": "the verdict carries the build-vs-adopt call with its UNKNOWN library/benchmark", "relation_strength": 0.76 },
    { "from": "SELECTION_ELIMINATION", "to": "HONEST_ABSTRACTION", "edge_type": "conflict", "why_related": "aggressive elimination for legibility can hide members the honest overview must represent", "relation_strength": 0.62, "resolution_rule": "eliminate for legibility only within honesty limits — always fold eliminated nodes into a counted, faithful aggregate so nothing vanishes without being represented" },
    { "from": "FORCE_DIRECTED_LAYOUT", "to": "HIERARCHICAL_LAYOUT", "edge_type": "conflict", "why_related": "an organic force-directed layout and a directional layered layout cannot both govern the same edges", "relation_strength": 0.6, "resolution_rule": "split by edge type — force-directed for 'related' edges, layered for 'prereq' edges — rather than forcing one algorithm to serve both" },
    { "from": "FORCE_DIRECTED_LAYOUT", "to": "LAYOUT_STABILITY", "edge_type": "conflict", "why_related": "re-running force-directed layout for the best result fights keeping positions stable for the mental map", "relation_strength": 0.6, "resolution_rule": "prefer stability — seed incrementally from previous positions and accept a slightly worse layout, resetting from scratch only on an explicit user action" },
    { "from": "RENDER_TARGET_SVG_CANVAS", "to": "LABEL_PLACEMENT_DECLUTTER", "edge_type": "conflict", "why_related": "canvas scales to more nodes but loses the crisp DOM text and easy hit-testing SVG gives label placement", "relation_strength": 0.58, "resolution_rule": "keep SVG (crisp labels) at coarse levels/small graphs and switch to canvas above the node-count threshold, reimplementing label placement manually only where scale demands it" },
    { "from": "AGGREGATION_META_NODES", "to": "EDGE_BUNDLING", "edge_type": "similarity", "why_related": "aggregation reduces node clutter and bundling reduces edge clutter — complementary generalization moves toward the same legibility budget", "relation_strength": 0.6 },
    { "from": "VIEWPORT_CULLING", "to": "TRANSITION_ANIMATION", "edge_type": "causal", "why_related": "culling to the viewport reduces the number of elements a transition must animate, keeping it smooth", "relation_strength": 0.68, "causal_confidence": 0.6 },
    { "from": "BARNES_HUT_SCALING", "to": "VIEW_SPEC_VERDICT", "edge_type": "constraint", "why_related": "the app's 2000+ scale and 500-row caps constrain the verdict toward approximate scaling and culling", "relation_strength": 0.66 },
    { "from": "LIBRARY_SELECTION", "to": "FORCE_DIRECTED_LAYOUT", "edge_type": "feedback", "why_related": "adopting a graph library would replace the hand-rolled force-directed implementation with the library's own", "relation_strength": 0.62 }
  ],
  "conflict_axes": [
    { "name": "geometric_zoom_vs_semantic_zoom", "description": "Scaling one fixed picture is cheap but cannot add or rename objects, while semantic zoom reveals and renames objects per level at far greater authoring cost.", "poles": ["geometric scale-only zoom", "semantic per-level zoom"], "resolution_hint": "the requirement is semantic — commit to per-level representations and never ship scale-only zoom as if it were semantic", "affected_nodes": ["SEMANTIC_VS_GEOMETRIC_ZOOM", "LOD_HIERARCHY", "ZOOM_LOD_MAPPING"] },
    { "name": "detail_richness_vs_overview_legibility", "description": "Showing more nodes, edges and labels conveys more but past a level's budget it becomes an unreadable hairball.", "poles": ["draw everything for completeness", "hold each level to its complexity budget"], "resolution_hint": "enforce a per-level element budget and use generalization operators to meet it", "affected_nodes": ["LOD_HIERARCHY", "CARTO_GENERALIZATION_OPERATORS", "EDGE_BUNDLING"] },
    { "name": "faithful_abstraction_vs_aggressive_simplification", "description": "Hiding more detail makes a cleaner overview but risks implying structure, completeness or mastery the data does not support.", "poles": ["simplify aggressively for cleanliness", "keep abstraction faithful with counts and real roll-ups"], "resolution_hint": "abstraction may say less but everything it says must be true — show member counts and real aggregates", "affected_nodes": ["HONEST_ABSTRACTION", "SELECTION_ELIMINATION", "AGGREGATION_META_NODES"] },
    { "name": "force_directed_organic_vs_hierarchical_structure", "description": "Force-directed layout clusters related concepts organically but hides direction, while layered layout shows prerequisite flow but not community structure.", "poles": ["force-directed organic clustering", "layered directional flow"], "resolution_hint": "split by edge type — force-directed for related edges, layered for prereq edges — in a hybrid view", "affected_nodes": ["FORCE_DIRECTED_LAYOUT", "HIERARCHICAL_LAYOUT", "VIEW_SPEC_VERDICT"] },
    { "name": "edge_bundling_clarity_vs_edge_traceability", "description": "Bundling edges exposes overall flow but makes any single edge hard to follow and can imply false adjacency.", "poles": ["bundle for readable flow", "keep edges separate for traceability"], "resolution_hint": "bundle at coarse zoom where flow matters and unbundle at fine zoom where users trace specific edges", "affected_nodes": ["EDGE_BUNDLING", "HIERARCHICAL_LAYOUT"] },
    { "name": "layout_stability_vs_optimal_layout", "description": "Re-optimizing the layout after every change gives the best arrangement but moves everything and destroys the mental map.", "poles": ["always re-optimize for the best layout", "keep positions stable for the mental map"], "resolution_hint": "seed incrementally and prefer stability, resetting from scratch only on explicit user request", "affected_nodes": ["LAYOUT_STABILITY", "FORCE_DIRECTED_LAYOUT", "TRANSITION_ANIMATION"] },
    { "name": "svg_crispness_vs_canvas_scale", "description": "SVG gives crisp text and free interaction but bogs past a few thousand DOM elements, while canvas/WebGL scales but loses DOM conveniences.", "poles": ["SVG for crisp labels and easy interaction", "canvas/WebGL for scale"], "resolution_hint": "use SVG at coarse levels and small graphs, switch to canvas above the node-count threshold", "affected_nodes": ["RENDER_TARGET_SVG_CANVAS", "LABEL_PLACEMENT_DECLUTTER", "VIEWPORT_CULLING"] },
    { "name": "build_custom_vs_adopt_library", "description": "A proven library gives layout and rendering cheaply but may not express semantic-zoom LOD, while a custom build gives full control at large cost.", "poles": ["adopt a mature graph library", "build the view custom"], "resolution_hint": "adopt if a library supports semantic-zoom/LOD, stable layout, scale and canvas; otherwise build the LOD layer over its primitives", "affected_nodes": ["LIBRARY_SELECTION", "RENDER_TARGET_SVG_CANVAS", "VIEW_SPEC_VERDICT"] },
    { "name": "label_completeness_vs_declutter", "description": "Labelling every node names everything but overlaps into an unreadable smear, while decluttering keeps text readable by hiding most labels.", "poles": ["label every node", "label only important, non-overlapping nodes"], "resolution_hint": "gate labels by importance and zoom threshold so only surviving nodes are labelled and none overlap", "affected_nodes": ["LABEL_PLACEMENT_DECLUTTER", "LABEL_GENERALIZATION", "SELECTION_ELIMINATION"] },
    { "name": "eager_full_render_vs_viewport_culling", "description": "Rendering the whole graph is simplest but does not scale, while culling to the viewport scales but hides off-screen structure until loaded.", "poles": ["render the whole graph eagerly", "cull and load progressively"], "resolution_hint": "cull to viewport-plus-margin and page data within the 500-row caps, bounding per-frame work", "affected_nodes": ["VIEWPORT_CULLING", "BARNES_HUT_SCALING", "ZOOM_LOD_MAPPING"] }
  ],
  "edge_cases": [
    { "description": "The overview degrades into an unreadable hairball of nodes and edges", "trigger": "aggregation and bundling are not applied so a level exceeds its complexity budget", "affected_nodes": ["LOD_HIERARCHY", "AGGREGATION_META_NODES", "EDGE_BUNDLING"], "mitigation": "enforce the per-level budget via importance-ranked selection, meta-node aggregation and edge bundling at coarse zoom", "severity": "high" },
    { "description": "Node labels overlap into an unreadable smear at a zoom level", "trigger": "every node is labelled without collision detection or zoom-threshold gating", "affected_nodes": ["LABEL_PLACEMENT_DECLUTTER", "LABEL_GENERALIZATION"], "mitigation": "place labels greedily by importance with collision detection and only above each label's zoom threshold", "severity": "medium" },
    { "description": "Nodes jump to new positions when the user zooms or expands a heading", "trigger": "the layout is recomputed from scratch on every change instead of incrementally", "affected_nodes": ["LAYOUT_STABILITY", "TRANSITION_ANIMATION"], "mitigation": "seed layout from previous positions, pin heading anchors, and animate transitions so the map stays recognizable", "severity": "high" },
    { "description": "A meta-node heading misrepresents its members (wrong grouping or flattering roll-up)", "trigger": "aggregation invents a grouping or shows a cosmetic rather than a real aggregate", "affected_nodes": ["HONEST_ABSTRACTION", "AGGREGATION_META_NODES"], "mitigation": "group only real members, show member count and coverage, and compute roll-ups as true aggregates", "severity": "high" },
    { "description": "Force-directed layout fails to converge or oscillates", "trigger": "no iteration cap or cooling schedule so the simulation never settles", "affected_nodes": ["FORCE_DIRECTED_LAYOUT", "BARNES_HUT_SCALING"], "mitigation": "cap iterations, apply a cooling schedule, and consider stress majorization for a stable minimum", "severity": "medium" },
    { "description": "The browser freezes rendering or simulating a large graph", "trigger": "naive O(n^2) layout and eager full render on a 2000+ node graph", "affected_nodes": ["BARNES_HUT_SCALING", "VIEWPORT_CULLING", "RENDER_TARGET_SVG_CANVAS"], "mitigation": "use Barnes-Hut, viewport culling and a canvas render target above the node-count threshold", "severity": "high" },
    { "description": "A prerequisite cycle breaks the layered layout's layer assignment", "trigger": "the prereq subgraph is not acyclic when hierarchical layout runs", "affected_nodes": ["HIERARCHICAL_LAYOUT"], "mitigation": "detect the cycle and temporarily reverse an edge for layout purposes only, leaving the data unchanged", "severity": "medium" },
    { "description": "A critical weak or high-yield concept is hidden by the zoomed-out abstraction", "trigger": "selection ranks by degree alone and drops a low-degree but important concept", "affected_nodes": ["SELECTION_ELIMINATION", "HONEST_ABSTRACTION"], "mitigation": "blend high-yield and mastery relevance into importance so critical concepts persist across levels", "severity": "high" },
    { "description": "A concise label is abbreviated so far it becomes ambiguous", "trigger": "naive truncation instead of authored per-level label variants", "affected_nodes": ["LABEL_GENERALIZATION", "LABEL_PLACEMENT_DECLUTTER"], "mitigation": "use authored concise/full variants and keep the full name available on hover rather than truncating", "severity": "medium" },
    { "description": "A large graph query is silently truncated at the 500-row cap", "trigger": "the view assumes the whole graph fits in one query past the app's row caps", "affected_nodes": ["VIEWPORT_CULLING", "BARNES_HUT_SCALING"], "mitigation": "page or cluster the data within the caps and surface truncation honestly rather than silently dropping nodes", "severity": "high" },
    { "description": "The LOD-level transition animation stutters on a dense graph", "trigger": "the transition animates the whole graph instead of only viewport elements", "affected_nodes": ["TRANSITION_ANIMATION", "VIEWPORT_CULLING"], "mitigation": "animate only viewport elements, cap the tweened count, and fall back to an instant cut when too many elements would move", "severity": "medium" },
    { "description": "Two adjacent LOD levels look nearly identical so zoom reveals nothing new", "trigger": "the level-to-render map overlaps levels or the graph density does not fit the level count", "affected_nodes": ["ZOOM_LOD_MAPPING", "LOD_HIERARCHY"], "mitigation": "tune the number of levels and their thresholds to the graph's density so each level adds distinct detail", "severity": "medium" }
  ],
  "workflow": [
    { "action": "Frame the drawn concept-graph view", "node_ref": "VIEW_PROBLEM_FRAME", "description": "State the semantic-zoom concept-map goal over the existing Concept/ConceptEdge data with no renderer yet", "artifact": "view problem-frame note", "gate": "the semantic-zoom goal, the cartography-plus-graph-drawing split, and the APP_RECON §5.5 no-graph-library fact are stated" },
    { "action": "Distinguish zoom types and define the LOD hierarchy", "node_ref": "LOD_HIERARCHY", "description": "Separate geometric from semantic zoom and structure it as discrete LOD levels each with an element budget", "artifact": "LOD-level and budget definition", "gate": "geometric-vs-semantic zoom is defined and discrete LOD levels each carry an element budget with threshold hysteresis" },
    { "action": "Choose the generalization operators", "node_ref": "CARTO_GENERALIZATION_OPERATORS", "description": "Adopt selection/aggregation/simplification/typification/displacement/symbolization and map them onto graph elements", "artifact": "generalization operator map", "gate": "the operator set is enumerated, mapped onto nodes/edges/labels, and tagged ESTIMATE-to-source-verify" },
    { "action": "Rank nodes and define selection/elimination", "node_ref": "SELECTION_ELIMINATION", "description": "Build an importance ranking blending centrality, high-yield and mastery relevance so the right nodes survive coarse zoom", "artifact": "importance ranking and survival rule", "gate": "an importance blend is defined, top-ranked nodes survive to coarse levels, and a pure degree cut is rejected" },
    { "action": "Aggregate clusters into expandable meta-nodes", "node_ref": "AGGREGATION_META_NODES", "description": "Collapse FK-area/CC-section clusters into heading meta-nodes that expand into members, with a fixed membership mapping", "artifact": "meta-node aggregation mapping", "gate": "collapse/expand on FK/CC groupings is specified with a fixed honest membership mapping (APP_RECON)" },
    { "action": "Author per-level labels and enforce honest abstraction", "node_ref": "HONEST_ABSTRACTION", "description": "Define concise-to-full label variants per level and require member-only groupings, visible counts and real roll-ups", "artifact": "label-variant and honest-abstraction rules", "gate": "per-level label variants are defined and abstraction requires member-only groupings, counts/coverage and real-aggregate roll-ups" },
    { "action": "Choose the hybrid layout and make it stable", "node_ref": "LAYOUT_STABILITY", "description": "Use force-directed for related edges and layered for prereq edges, scaled by Barnes-Hut and kept stable/incremental", "artifact": "hybrid stable-layout spec", "gate": "force-directed and layered layouts are assigned by edge type, scaled by Barnes-Hut, and kept stable via incremental seeding and pinned anchors" },
    { "action": "Bundle edges and declutter labels", "node_ref": "LABEL_PLACEMENT_DECLUTTER", "description": "Bundle related edges at coarse zoom and place labels greedily by importance with collision detection and zoom thresholds", "artifact": "bundling and label-declutter spec", "gate": "edge bundling at coarse zoom and greedy zoom-gated collision-checked label placement coupled to selection are specified" },
    { "action": "Map LOD levels to render decisions and cull the viewport", "node_ref": "VIEWPORT_CULLING", "description": "Write the concrete level-0-to-3 render map and cull/page data to the viewport within the 500-row caps", "artifact": "LOD render map + culling policy", "gate": "a concrete level-to-render map is given and viewport culling with progressive loading respecting the 500-row caps is specified" },
    { "action": "Pick render target and build-vs-adopt, add transitions", "node_ref": "LIBRARY_SELECTION", "description": "Choose SVG/canvas by scale, decide build-vs-adopt by criteria, and add object-constancy transitions", "artifact": "render-target + library decision", "gate": "the SVG/canvas threshold and build-vs-adopt criteria are stated, with specific libraries/benchmarks carried as UNKNOWN plus a method" },
    { "action": "Compose the drawn-view verdict", "node_ref": "VIEW_SPEC_VERDICT", "description": "Merge semantic zoom, LOD generalization, hybrid stable layout, bundling, honest decluttered labels, transitions and culled rendering into a go/no-go", "artifact": "drawn-view go/no-go verdict", "gate": "the go stack, the no-go on geometric-only zoom/dishonest abstraction/invented library claims, and the legible-honest-stable acceptance rule are stated" }
  ],
  "dominance_rules": [
    { "rule": "Semantic zoom dominates geometric-only zoom for the concept map", "rationale": "the few-headings-out / concise-names-in requirement needs objects to appear and rename, which geometric scaling cannot do", "trigger": "a zoom behavior is being implemented", "action": "implement per-level semantic zoom and never ship scale-only zoom as if it were semantic" },
    { "rule": "Honest abstraction dominates aggressive simplification when they conflict", "rationale": "a cleaner overview is worthless if it implies structure, completeness or mastery the data does not support", "trigger": "a simplification would hide members or show a cosmetic roll-up", "action": "keep member-only groupings, show counts/coverage, and use real aggregates even at some cost to cleanliness" },
    { "rule": "Blended importance dominates degree-only when selecting survivors", "rationale": "a low-degree but high-yield or weak concept is exactly what the learner needs to see", "trigger": "nodes are ranked for coarse-zoom survival", "action": "rank by a blend of centrality, high-yield and mastery relevance, not degree alone" },
    { "rule": "Layout stability dominates re-optimizing on every change", "rationale": "moving everything for a marginally better layout destroys the user's mental map", "trigger": "the graph changes or the user zooms", "action": "seed incrementally from previous positions and reset from scratch only on explicit request" },
    { "rule": "Aggregation and bundling dominate rendering every node and edge at overview", "rationale": "an un-generalized overview is an unreadable hairball that violates the LOD budget", "trigger": "a coarse level is drawn", "action": "aggregate clusters into headings and bundle edges to meet the level's budget" },
    { "rule": "Viewport culling with Barnes-Hut dominates eager full render at scale", "rationale": "naive full layout and render freeze the browser at the app's 2000+ node scale", "trigger": "the active graph exceeds the interactive element budget", "action": "cull to the viewport, page within the 500-row caps, and approximate repulsion with Barnes-Hut" },
    { "rule": "Layered layout dominates force-directed for the directed prerequisite substructure", "rationale": "prerequisite flow must read directionally, which force-directed layout cannot show", "trigger": "prereq edges are laid out", "action": "use layered layout for prereq edges and force-directed only for related edges" },
    { "rule": "Canvas dominates SVG above the node-count threshold", "rationale": "the SVG DOM bogs past a few thousand elements while canvas/WebGL scales further", "trigger": "a level's culled element count exceeds the SVG threshold", "action": "render that level on canvas/WebGL and reimplement label placement manually there" },
    { "rule": "A prototyped, measured library choice dominates an asserted one", "rationale": "specific graph libraries and their performance are unknown here and must not be fabricated", "trigger": "a library or performance number is about to be committed", "action": "prototype the LOD mapping against candidates and measure at the app's node counts before choosing" }
  ],
  "anti_rework_rules": [
    { "rule": "Fix the LOD levels and per-level element budgets before building any renderer", "prevents": "authoring render code against ad-hoc levels and reworking every level when a budget is added later" },
    { "rule": "Define the importance ranking before selection so survivors are consistent across zoom", "prevents": "a survivor set that changes unpredictably and forces re-tuning of every level's contents" },
    { "rule": "Choose the SVG-vs-canvas render target before building components", "prevents": "rebuilding interaction and label placement after a late switch to canvas" },
    { "rule": "Make the layout stable and incremental from the outset", "prevents": "a jarring from-scratch relayout habit that has to be retrofitted for mental-map preservation" },
    { "rule": "Break prerequisite cycles for layout before running the layered algorithm", "prevents": "a broken layer assignment discovered only when real cyclic data arrives" },
    { "rule": "Define the meta-node membership mapping before wiring labels and roll-ups", "prevents": "heading contents and their labels/aggregates drifting apart and being reconciled later" },
    { "rule": "Set the label collision and zoom-threshold policy before adding labels", "prevents": "an overlapping label smear that must be untangled after the fact" },
    { "rule": "Decide build-vs-adopt by prototyping before implementing the layout math", "prevents": "hand-rolling a layout engine a library would have supplied, or vice versa" }
  ],
  "iteration_protocol": [
    { "trigger": "a level's culled element count grows past the SVG DOM threshold", "action": "switch that level to a canvas/WebGL render target and re-verify label placement", "nodes": ["RENDER_TARGET_SVG_CANVAS", "LABEL_PLACEMENT_DECLUTTER"], "priority": 1 },
    { "trigger": "users lose track of nodes across a zoom or expand/collapse", "action": "tighten layout stability and tune the transition animation for object constancy", "nodes": ["LAYOUT_STABILITY", "TRANSITION_ANIMATION"], "priority": 1 },
    { "trigger": "an overview level still reads as a hairball", "action": "increase aggregation and tighten selection to meet the level's budget", "nodes": ["AGGREGATION_META_NODES", "SELECTION_ELIMINATION", "EDGE_BUNDLING"], "priority": 2 },
    { "trigger": "labels overlap or flicker at a zoom level", "action": "tighten the collision thresholds and zoom-threshold gating in the placement heuristic", "nodes": ["LABEL_PLACEMENT_DECLUTTER", "LABEL_GENERALIZATION"], "priority": 2 },
    { "trigger": "force-directed layout fails to converge or oscillates", "action": "cap iterations, add cooling, or switch to stress majorization", "nodes": ["FORCE_DIRECTED_LAYOUT", "BARNES_HUT_SCALING"], "priority": 2 },
    { "trigger": "a graph library is prototyped and benchmarked at the app's node counts", "action": "resolve the build-vs-adopt decision and, if adopted, re-verify the LOD mapping over its zoom model", "nodes": ["LIBRARY_SELECTION", "ZOOM_LOD_MAPPING", "VIEW_SPEC_VERDICT"], "priority": 1 },
    { "trigger": "a large graph hits the 500-row query cap", "action": "add clustered/paged loading and surface truncation honestly", "nodes": ["VIEWPORT_CULLING", "HONEST_ABSTRACTION"], "priority": 2 }
  ],
  "priority_rationale": "The view frame and the geometric-vs-semantic zoom distinction come first because they decide that the whole view is a semantic-zoom map, not a scalable picture; the LOD hierarchy, generalization operators, importance-ranked selection, meta-node aggregation, per-level labels and honest-abstraction rules come next as the cartographic layer that decides what is shown, hidden, merged and renamed at each level; the force-directed and layered layouts, their Barnes-Hut scaling, mental-map stability, edge bundling and label decluttering then supply the graph-drawing layer that decides where everything goes and how it stays legible; and the concrete LOD-to-render mapping, viewport culling, render-target and build-vs-adopt decisions, transitions and the final drawn-view verdict come last because they compose every upstream choice into a legible, honest, stable, scalable rendering with an explicit no-go on geometric-only zoom, dishonest abstraction and fabricated library claims.",
  "eval_objective": "verify_core_properties_of_study_system_viz_semantic_zoom_lod_graph_drawing_kb"
}
