{
  "domain": "study_system_kgraph_learning_science_graphs",
  "domain_label": "Study System — KG Architect: Learning Science of Knowledge Graphs",
  "purpose": "The learning-science slice of the knowledge-graph architect: how the theory of concept maps (Novak & Cañas 2008) and Knowledge Space Theory (Doignon & Falmagne 1985) dictate what a Concept node and a typed ConceptEdge should mean, which graph shapes actually aid learning and retrieval, how a prerequisite DAG and its learning fringe drive 'block new cards on a concept until its prerequisite is mastered', and when to graduate a flat prerequisite spine into a full cross-linked graph — kept honest by tagging every domain claim FACT/ESTIMATE-to-source-verify/UNKNOWN and tracing every app-behavior claim to APP_RECON.",
  "assumptions": [
    "no observed dataset supplied; all base scores are heuristic priors and every shape/threshold recommendation is an ESTIMATE synthesized from the cited learning-science literature, not a measured result on this deck",
    "the app's Concept and ConceptEdge entities are the fixed integration target: ConceptEdge carries a typed relation in {prereq, related, part_of} with src/dst concept ids and Concept is a per-exam named unit [FACT-source: APP_RECON entity schemas]",
    "the prerequisite-gating behaviour this KB reasons about ('block new cards until prereq mastery >= 0.6') exists as diagnosis-string text only and is enforced by no code today [FACT-source: APP_RECON §4a] — this KB specifies the learning-science logic a real gate would implement, not a claim that it is implemented",
    "the two other kgraph KBs (knowledge-representation/ontology-engineering and biomedical terminology entity-linking) own the schema-mechanics and term-normalization slices; this KB stays in the learning-science slice and only names those boundaries"
  ],
  "exclusions": [
    "the ontology-engineering mechanics (formal typed node/edge schema design, hub-node graph algorithms, RDF/property-graph choices) that a neighboring kgraph KB owns, beyond mapping learning-science relations onto the existing ConceptEdge enum",
    "medical/dental terminology recognition, synonym normalization and hover-definition attachment (the neighboring biomedical-entity-linking kgraph KB), beyond naming a Concept node's granularity",
    "the visual encoding, layout and color-palette rendering of the graph (the viz specialist), beyond specifying WHAT the color-coded meaning layer must express",
    "the per-concept mastery estimator itself (BKT/Elo, owned by the learner specialist), consumed here only as the mastery signal a prerequisite gate reads",
    "authoring or LLM-generating the actual edge instances for the INBDE/TOEFL decks (contenteng), beyond the graduation policy for when the flat spine becomes a full graph"
  ],
  "competency_questions": [
    { "id": "CQ_01", "question": "Why represent a study domain as a graph at all, and what is the 'meaning layer' this KB owns versus the raw edge table contenteng produces?", "must_be_answerable_from": ["GRAPH_LEARNING_FRAME"], "acceptance_condition": "states that a graph makes prerequisite and integrative structure explicit so sequencing and gating become computable, and separates the data layer (edge rows) from the learning-science meaning layer (what a node/edge should signify)", "covered_by": ["GRAPH_LEARNING_FRAME"] },
    { "id": "CQ_02", "question": "What are the defining features of a Novak & Cañas concept map, and which of them does the app's ConceptEdge keep or drop?", "must_be_answerable_from": ["CONCEPT_MAPPING_NOVAK", "PROPOSITIONAL_STRUCTURE"], "acceptance_condition": "names concepts-as-nodes, labeled linking phrases forming propositions, a focus question, hierarchical layout and cross-links, and notes ConceptEdge keeps typed direction but drops the free-text linking phrase", "covered_by": ["CONCEPT_MAPPING_NOVAK", "PROPOSITIONAL_STRUCTURE"] },
    { "id": "CQ_03", "question": "What is a knowledge structure in Knowledge Space Theory and what is a knowledge state?", "must_be_answerable_from": ["KST_KNOWLEDGE_STATES"], "acceptance_condition": "defines a knowledge structure as a family of knowledge states (subsets of domain items a learner can have mastered) containing the empty and full sets, with the Doignon & Falmagne 1985 citation", "covered_by": ["KST_KNOWLEDGE_STATES"] },
    { "id": "CQ_04", "question": "What extra axioms turn a knowledge space into a learning space, and what are the outer and inner fringes?", "must_be_answerable_from": ["KST_LEARNING_SPACE", "FRINGE_OUTER_INNER"], "acceptance_condition": "states learning-smoothness/accessibility and closure-under-union (well-gradedness) let every state be reached one item at a time, and defines the outer fringe as the items learnable next and the inner fringe as the most recently learnable", "covered_by": ["KST_LEARNING_SPACE", "FRINGE_OUTER_INNER"] },
    { "id": "CQ_05", "question": "How does the surmise relation formalize 'prerequisite', and why must prerequisite edges form a DAG?", "must_be_answerable_from": ["SURMISE_PREREQUISITE", "PREREQUISITE_DAG"], "acceptance_condition": "explains that mastering an item lets one surmise mastery of its prerequisites (a quasi-order underlying a quasi-ordinal knowledge space), so prerequisite edges must be acyclic or they assert a contradiction, and a topological order is a valid learning sequence", "covered_by": ["SURMISE_PREREQUISITE", "PREREQUISITE_DAG"] },
    { "id": "CQ_06", "question": "Which graph shapes actually aid learning and retrieval, and which harm them?", "must_be_answerable_from": ["GRAPH_SHAPES_FOR_LEARNING", "RETRIEVAL_STRUCTURE"], "acceptance_condition": "states that a sparse hierarchy with a few meaningful cross-links aids organization and retrieval while an over-dense hairball degrades both, tying this to organized/elaborated structure and spreading activation, all tagged ESTIMATE-to-source-verify", "covered_by": ["GRAPH_SHAPES_FOR_LEARNING", "RETRIEVAL_STRUCTURE"] },
    { "id": "CQ_07", "question": "What are hub/anchor concepts, what do they do for learning, and what is their risk?", "must_be_answerable_from": ["HUB_NODES", "HIERARCHY_CROSSLINKS"], "acceptance_condition": "defines hub nodes as high-degree integrative anchors (subsumption/cross-link concentrators) that organize a domain but risk becoming over-connected bottlenecks that gate too much", "covered_by": ["HUB_NODES", "HIERARCHY_CROSSLINKS"] },
    { "id": "CQ_08", "question": "How do prerequisite edges and the learning fringe drive 'block new cards on a concept until its prerequisite is mastered'?", "must_be_answerable_from": ["PREREQ_GATING_MECHANISM", "MASTERY_THRESHOLD_GATE"], "acceptance_condition": "describes gating a dependent concept's new cards until every prerequisite's mastery clears a threshold (the app's block-until->=0.6 rule off bkt_p_mastery), grounded in the outer fringe as the set of currently unlockable concepts, traced to APP_RECON", "covered_by": ["PREREQ_GATING_MECHANISM", "MASTERY_THRESHOLD_GATE"] },
    { "id": "CQ_09", "question": "What goes wrong with prerequisite gating and how is it bounded?", "must_be_answerable_from": ["GATING_RISKS"], "acceptance_condition": "names over-gating (frustration, deadlock from a mislabeled or cyclic DAG, orphaned concepts) versus under-gating, and prescribes an escape hatch, a hysteresis band and a DAG-acyclicity precheck", "covered_by": ["GATING_RISKS", "PREREQUISITE_DAG"] },
    { "id": "CQ_10", "question": "When should the graph graduate from a flat prerequisite spine to a full cross-linked graph?", "must_be_answerable_from": ["FLAT_SPINE_GRADUATION"], "acceptance_condition": "prescribes starting with a hierarchical tree/prerequisite spine and adding related/cross-link edges only as co-mastery or authoring evidence accrues, so early sparsity avoids a premature hairball", "covered_by": ["FLAT_SPINE_GRADUATION", "GRAPH_SHAPES_FOR_LEARNING"] },
    { "id": "CQ_11", "question": "How do the three ConceptEdge types map onto learning-science relations, and what should a Concept node's granularity be?", "must_be_answerable_from": ["EDGE_TYPE_SEMANTICS", "NODE_GRANULARITY"], "acceptance_condition": "maps prereq->surmise/DAG, part_of->hierarchical composition, related->associative cross-link, and sets a Concept node to a single masterable/testable unit (a KST item) rather than a whole chapter", "covered_by": ["EDGE_TYPE_SEMANTICS", "NODE_GRANULARITY"] },
    { "id": "CQ_12", "question": "What does KST add beyond the edge table contenteng deferred, and what is the recommended graph-model verdict for this app?", "must_be_answerable_from": ["KST_ASSESSMENT_VS_CONTENTENG", "GRAPH_MODEL_VERDICT"], "acceptance_condition": "states KST supplies knowledge states, the learning fringe and the surmise-based gate contenteng punted (KG_RECON §2a), and gives the verdict: hierarchical concept-map backbone + prerequisite DAG for gating + sparse cross-links, graduating flat-spine->graph, mapped onto Concept/ConceptEdge and feeding the color meaning layer", "covered_by": ["KST_ASSESSMENT_VS_CONTENTENG", "GRAPH_MODEL_VERDICT", "COLOR_MEANING_LAYER"] }
  ],
  "glossary": [
    { "term": "concept map (Novak & Cañas 2008)", "definition": "A graphical knowledge representation in which concepts are nodes and each edge carries a labeled linking phrase so that concept-link-concept reads as a proposition; maps are organized hierarchically around a focus question with cross-links joining separate branches [FACT — cited in contenteng KBs per SPECIALIST_LIST §C1]." },
    { "term": "proposition (linking phrase)", "definition": "A concept-link-concept triple whose edge label makes it a readable statement (e.g. 'caries -> is caused by -> acidogenic bacteria'); the unit of meaning in a concept map and the feature the app's unlabeled typed ConceptEdge drops." },
    { "term": "Knowledge Space Theory (KST)", "definition": "Doignon & Falmagne (1985) set-theoretic framework modelling a domain as a family of knowledge states — the subsets of items a learner could plausibly have mastered together — rather than a single ability score [FACT — cited in contenteng KBs per SPECIALIST_LIST §C1]." },
    { "term": "knowledge state", "definition": "A subset of domain items a learner has mastered; the family of all admissible states is the knowledge structure, which always contains the empty set (knows nothing) and the full domain (knows everything)." },
    { "term": "learning space", "definition": "A knowledge structure satisfying learning-smoothness/accessibility (any nonempty state can be reached by adding one item at a time) and closure under union (well-gradedness); its states form the reachable learning paths [ESTIMATE — to source-verify]." },
    { "term": "surmise relation / prerequisite", "definition": "The precedence relation in which mastering item b lets one surmise mastery of its prerequisites a; for a quasi-ordinal knowledge space this quasi-order (Birkhoff correspondence) IS the prerequisite structure and must be acyclic [ESTIMATE — to source-verify]." },
    { "term": "outer / inner fringe", "definition": "For a knowledge state, the outer fringe is the set of items the learner could learn next (the study frontier) and the inner fringe is the most recently learnable items; the outer fringe is exactly the set of currently unlockable concepts a gate exposes." },
    { "term": "hub / anchor concept", "definition": "A high-degree node that integrates many others — an Ausubelian subsumption anchor or a cross-link concentrator; it organizes a domain but risks becoming an over-connected bottleneck that gates too many downstream concepts [ESTIMATE — to source-verify for the subsumption framing]." },
    { "term": "prerequisite DAG", "definition": "The directed acyclic graph of ConceptEdge type='prereq' edges; acyclicity is required because a cycle asserts that a concept is its own prerequisite, and a topological ordering of the DAG is a valid study sequence." },
    { "term": "prerequisite gating (block-until-mastered)", "definition": "Withholding a dependent concept's new cards until every prerequisite concept's mastery clears a threshold; the app's diagnosis text is 'block new cards on this concept until prereq mastery >= 0.6', triggered when a prereq's bkt_p_mastery < 0.5, and is text-only with no enforcing code today [FACT-source: APP_RECON §4a]." },
    { "term": "flat-spine to full-graph graduation", "definition": "The staged buildout in which the graph begins as a hierarchical tree or a single prerequisite spine and gains related/cross-link edges only as co-mastery or authoring evidence justifies them, keeping early graphs sparse and legible." }
  ],
  "nodes": [
    {
      "id": "GRAPH_LEARNING_FRAME",
      "topic": "why_a_graph_and_the_meaning_layer",
      "definition": "Frame the learning-science reason to represent a study domain as a graph rather than a flat card list: a graph makes prerequisite precedence and integrative structure EXPLICIT and therefore computable — you can topologically sequence study, gate on prerequisites, and locate integrative anchors. This KB owns the MEANING layer (what a node and a typed edge should signify for learning) as opposed to the DATA layer (the raw ConceptEdge rows contenteng emits) and the RENDER layer (colors/layout, owned by viz). contenteng stops at an edge table and explicitly defers the full Knowledge Space Theory treatment [FACT-source: SPECIALIST_LIST §C1, KG_RECON §2a]; this KB supplies the learning science that gives those edges their instructional meaning [ESTIMATE — design synthesis over the cited theory].",
      "group": "foundations",
      "dependencies": [],
      "competency_question_refs": ["CQ_01"],
      "scope_boundary": { "included": ["the learning rationale for a graph representation", "the data-vs-meaning-vs-render layer split", "the boundary against contenteng's deferred KST"], "excluded": ["the formal edge schema mechanics (neighboring KB)", "the color/layout rendering (viz)"] },
      "pros": [ { "claim": "Separating the meaning layer from the raw edge table stops the graph from being a decorative overlay and makes sequencing and gating first-class", "example": "reading a prereq edge as a study-order constraint rather than just a drawn line between two Concept rows", "weight": 0.82 } ],
      "cons": [ { "claim": "The meaning layer is a synthesis over learning-science literature, not a measured result on this deck, so its recommendations are judgments data could revise", "example": "asserting a sparse hierarchy helps before any retention data on this app confirms it", "weight": 0.58 } ],
      "failure_modes": ["treating the graph as visual decoration with no instructional consequence", "conflating the meaning layer with the render layer and duplicating viz's job"],
      "acceptance_tests": ["the frame states the graph's sequencing/gating rationale, the data-vs-meaning-vs-render split, and the contenteng-defers-KST boundary with the source tag"],
      "revisit_triggers": ["contenteng changes what the edge table contains", "the viz specialist redefines the render-layer boundary", "retention data contradicts a meaning-layer recommendation"],
      "base": { "criticality": 0.86, "business_value": 0.78, "user_value": 0.78, "technical_complexity": 0.38, "risk_if_wrong": 0.68, "cross_topic_coupling": 0.84, "irreversibility": 0.35, "confidence": 0.7, "node_conflict_pressure": 0.34, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.85, "prior_importance": 0.86, "evidence_confidence": 0.66, "failure_rate": 0.16, "downside_weight": 0.45, "uncertainty_interval": [0.24, 0.52], "update_signal": "a change to the contenteng edge table or the viz boundary revises the frame" }
    },
    {
      "id": "CONCEPT_MAPPING_NOVAK",
      "topic": "novak_canas_concept_map_features",
      "definition": "Enumerate the defining features of a Novak & Cañas (2008) concept map: concepts (perceived regularities, usually noun-labeled) are the NODES; every link carries a LABELED linking phrase; a concept-link-concept triple is a PROPOSITION (the unit of meaning); the map is built around a FOCUS QUESTION; it is arranged HIERARCHICALLY with the most general concepts at the top; and CROSS-LINKS join otherwise-separate branches to show integration [FACT — cited in contenteng KBs per SPECIALIST_LIST §C1]. Concept mapping rests on Ausubelian assimilation theory: meaningful learning attaches new concepts to relevant existing ones, which is exactly what an anchor/hub node models [ESTIMATE — to source-verify for the Ausubel attribution]. This is the qualitative, human-readable pole of knowledge-graph theory.",
      "group": "concept_mapping",
      "dependencies": ["GRAPH_LEARNING_FRAME"],
      "competency_question_refs": ["CQ_02"],
      "scope_boundary": { "included": ["concepts-as-nodes, labeled links, propositions, focus question, hierarchy, cross-links", "the assimilation-theory grounding"], "excluded": ["the formal set-theoretic KST view (its own node)", "the ConceptEdge mapping (its own node)"] },
      "pros": [ { "claim": "The concept-map feature set gives a concrete, evidence-backed template for what a good node and a good edge look like to a learner", "example": "requiring each authored edge to read as a proposition rather than an unlabeled arrow", "weight": 0.82 } ],
      "cons": [ { "claim": "Full concept maps demand a free-text linking phrase per edge that the app's typed ConceptEdge cannot store, so fidelity is partial", "example": "collapsing 'is treated by' and 'is contraindicated in' both into a single 'related' type and losing the proposition", "weight": 0.62 } ],
      "failure_modes": ["dropping the focus question and letting the map sprawl without a purpose", "authoring unlabeled links so no proposition is expressed"],
      "acceptance_tests": ["the six concept-map features are listed with the Novak & Cañas citation and the assimilation-theory grounding is tagged ESTIMATE-to-source-verify"],
      "revisit_triggers": ["the ConceptEdge schema gains a linking-phrase field", "the focus-question framing changes for a deck", "the assimilation-theory attribution is source-verified or corrected"],
      "base": { "criticality": 0.84, "business_value": 0.76, "user_value": 0.78, "technical_complexity": 0.4, "risk_if_wrong": 0.66, "cross_topic_coupling": 0.78, "irreversibility": 0.35, "confidence": 0.74, "node_conflict_pressure": 0.4, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.84, "evidence_confidence": 0.72, "failure_rate": 0.14, "downside_weight": 0.42, "uncertainty_interval": [0.22, 0.48], "update_signal": "a schema change adding linking phrases or a source-verification of the Ausubel link updates the feature set" }
    },
    {
      "id": "PROPOSITIONAL_STRUCTURE",
      "topic": "labeled_propositions_vs_typed_edges",
      "definition": "Make the proposition the unit of meaning and reconcile it with the app's typed edges. In a concept map the edge label carries the semantics: 'periodontitis -> is characterized by -> attachment loss' is a proposition a learner can be tested on, and elaborative interrogation (asking WHY the link holds) is a known retrieval booster [ESTIMATE — to source-verify]. The app's ConceptEdge stores only a TYPE in {prereq, related, part_of} and a direction, not a free-text phrase [FACT-source: APP_RECON entity schema], so the propositional content is compressed into the type plus the two concept names. The design consequence: choose the three types so that the recovered proposition ('A is a prerequisite of B', 'A is part of B', 'A is related to B') is still instructionally meaningful, and keep richer linking phrases in card text where they can be authored freely.",
      "group": "concept_mapping",
      "dependencies": ["CONCEPT_MAPPING_NOVAK"],
      "competency_question_refs": ["CQ_02"],
      "scope_boundary": { "included": ["the proposition as unit of meaning", "elaborative interrogation as a retrieval lever", "how the typed ConceptEdge compresses a proposition"], "excluded": ["the full edge-type-to-relation mapping (its own node)", "term-level linking in card text (biomedical KB)"] },
      "pros": [ { "claim": "Treating each edge as a compressed proposition keeps the typed graph instructionally meaningful despite the missing linking phrase", "example": "reading a prereq edge as the testable claim 'you must know A before B makes sense'", "weight": 0.8 } ],
      "cons": [ { "claim": "Three fixed types cannot express the diversity of real linking phrases, so nuance lives only in card text and is invisible to the graph", "example": "'inhibits' and 'is a cofactor of' both flattening to 'related' at the graph level", "weight": 0.64 } ],
      "failure_modes": ["overloading 'related' as a catch-all so it carries no meaning", "expecting the graph type alone to convey a nuance that only card text holds"],
      "acceptance_tests": ["the node states the proposition-as-meaning principle, notes ConceptEdge stores only a type (source-tagged), and prescribes choosing types whose recovered proposition stays meaningful"],
      "revisit_triggers": ["a linking-phrase field is added to ConceptEdge", "the 'related' type is observed to be an unusable catch-all", "elaborative-interrogation evidence is source-verified"],
      "base": { "criticality": 0.8, "business_value": 0.72, "user_value": 0.76, "technical_complexity": 0.42, "risk_if_wrong": 0.64, "cross_topic_coupling": 0.76, "irreversibility": 0.35, "confidence": 0.72, "node_conflict_pressure": 0.42, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.8, "evidence_confidence": 0.68, "failure_rate": 0.15, "downside_weight": 0.42, "uncertainty_interval": [0.24, 0.5], "update_signal": "a linking-phrase field or a catch-all 'related' problem changes the propositional mapping" }
    },
    {
      "id": "HIERARCHY_CROSSLINKS",
      "topic": "hierarchy_and_cross_links",
      "definition": "Distinguish the two structural axes of a concept map: HIERARCHY (general concepts subsume specific ones top-to-bottom, the tree backbone) and CROSS-LINKS (edges joining separate branches, signalling integrative, often creative, understanding) [FACT — Novak & Cañas feature, cited per SPECIALIST_LIST §C1]. The hierarchy maps naturally onto part_of / prereq edges and gives the graph its spine; cross-links map onto 'related' edges and are the sparse connective tissue that turns a set of trees into a graph. Good maps have MANY hierarchical edges and FEW, high-value cross-links — cross-links are prized precisely because they are rare and integrative, which is the structural argument against a dense hairball [ESTIMATE — to source-verify for the cross-link-quality claim].",
      "group": "concept_mapping",
      "dependencies": ["CONCEPT_MAPPING_NOVAK"],
      "competency_question_refs": ["CQ_02", "CQ_07"],
      "scope_boundary": { "included": ["hierarchy as the tree backbone", "cross-links as sparse integrative edges", "the many-hierarchical/few-cross-link balance"], "excluded": ["hub-node degree analysis (its own node)", "the density thresholds for retrieval (its own node)"] },
      "pros": [ { "claim": "Naming the two axes tells the author which edges are structural spine (many) and which are integrative cross-links (few and precious)", "example": "building the INBDE tree first, then adding a handful of cross-links between pharmacology and pathology", "weight": 0.8 } ],
      "cons": [ { "claim": "The 'few cross-links' guidance is qualitative, so without a density target authors can either over-connect or omit the integrative links that make the map valuable", "example": "adding a cross-link between every co-occurring pair and recreating a hairball", "weight": 0.62 } ],
      "failure_modes": ["adding cross-links indiscriminately until the hierarchy is unreadable", "omitting cross-links entirely and leaving disconnected trees"],
      "acceptance_tests": ["the node separates hierarchy from cross-links, maps each onto edge types, and states the many-hierarchical/few-cross-link balance with the quality claim tagged ESTIMATE"],
      "revisit_triggers": ["a density target is set by the graph-shapes node", "cross-links are observed to over- or under-connect a deck", "the integrative-value claim is source-verified"],
      "base": { "criticality": 0.78, "business_value": 0.72, "user_value": 0.74, "technical_complexity": 0.42, "risk_if_wrong": 0.62, "cross_topic_coupling": 0.78, "irreversibility": 0.35, "confidence": 0.72, "node_conflict_pressure": 0.42, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.78, "evidence_confidence": 0.68, "failure_rate": 0.15, "downside_weight": 0.42, "uncertainty_interval": [0.24, 0.5], "update_signal": "a density target or an observed over/under-connection changes the hierarchy/cross-link balance" }
    },
    {
      "id": "KST_KNOWLEDGE_STATES",
      "topic": "kst_knowledge_structure_and_states",
      "definition": "State the formal, quantitative pole: Knowledge Space Theory (Doignon & Falmagne 1985) models a domain as a KNOWLEDGE STRUCTURE — a family of KNOWLEDGE STATES, where a state is the subset of domain items a learner could plausibly have mastered together — instead of a single scalar ability [FACT — cited in contenteng KBs per SPECIALIST_LIST §C1]. The structure always includes the empty set (knows nothing) and the full domain (knows everything). Not every subset is a valid state: which subsets are admissible is governed by the domain's prerequisite structure, so KST turns 'what does the learner know' into 'which state are they in', and the graph edges are what constrain the set of admissible states. This is the theory that gives a Concept node the identity of a masterable ITEM.",
      "group": "kst",
      "dependencies": ["GRAPH_LEARNING_FRAME"],
      "competency_question_refs": ["CQ_03"],
      "scope_boundary": { "included": ["knowledge structure as a family of states", "a state as a subset of mastered items", "empty/full states and prerequisite-constrained admissibility"], "excluded": ["the learning-space axioms (its own node)", "the surmise/prerequisite relation formalism (its own node)"] },
      "pros": [ { "claim": "Modelling knowledge as a state rather than a score captures that two learners with the same count can know different things and need different next items", "example": "distinguishing a learner who mastered anatomy-then-pharmacology from one who did the reverse at the same total", "weight": 0.82 } ],
      "cons": [ { "claim": "The full family of states is exponential in the number of items, so KST is only tractable when the prerequisite structure sharply restricts admissible states", "example": "a 500-concept domain having astronomically many subsets unless prereq edges prune them", "weight": 0.66 } ],
      "failure_modes": ["treating every subset as an admissible state and losing tractability", "collapsing a knowledge state back into a single mastery score"],
      "acceptance_tests": ["the node defines a knowledge structure and state, names the empty/full states, and ties admissibility to the prerequisite structure with the Doignon & Falmagne citation"],
      "revisit_triggers": ["the domain grows large enough that state enumeration matters", "the prerequisite structure is found too weak to prune states", "the item granularity changes what counts as a state element"],
      "base": { "criticality": 0.86, "business_value": 0.76, "user_value": 0.78, "technical_complexity": 0.55, "risk_if_wrong": 0.7, "cross_topic_coupling": 0.82, "irreversibility": 0.4, "confidence": 0.72, "node_conflict_pressure": 0.4, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.86, "evidence_confidence": 0.7, "failure_rate": 0.15, "downside_weight": 0.48, "uncertainty_interval": [0.24, 0.5], "update_signal": "domain growth or a weak prerequisite structure changes the state-space treatment" }
    },
    {
      "id": "KST_LEARNING_SPACE",
      "topic": "learning_space_axioms",
      "definition": "Add the two axioms that make a knowledge structure a LEARNING SPACE: (1) learning smoothness / accessibility — any nonempty admissible state can be reached from the empty state by adding one item at a time, each intermediate set also being a state; and (2) closure under union — the union of any two states is a state (well-gradedness) [ESTIMATE — to source-verify]. Together these guarantee that from any state there is always a single-item step forward, so the learner is never stuck and there is always a legitimate 'next thing to learn'. This is the theoretical justification for one-concept-at-a-time sequencing and for a study frontier that never dead-ends, and it is what a prerequisite DAG must preserve to be a valid learning space.",
      "group": "kst",
      "dependencies": ["KST_KNOWLEDGE_STATES"],
      "competency_question_refs": ["CQ_04"],
      "scope_boundary": { "included": ["learning-smoothness/accessibility", "closure under union / well-gradedness", "the always-a-next-step guarantee"], "excluded": ["the outer/inner fringe definitions (its own node)", "the DAG acyclicity requirement (its own node)"] },
      "pros": [ { "claim": "The learning-space axioms guarantee a stepwise, never-stuck study path, which is exactly the property a sequencer needs", "example": "always being able to hand the learner one unlockable concept regardless of their current state", "weight": 0.82 } ],
      "cons": [ { "claim": "Real authored prerequisite graphs may violate the axioms (a state with no single-item exit), producing dead-ends the theory forbids", "example": "a concept whose two prerequisites can only be satisfied simultaneously, breaking accessibility", "weight": 0.64 } ],
      "failure_modes": ["authoring a prerequisite graph that admits a dead-end state with no single-item step", "assuming well-gradedness holds without checking the authored edges"],
      "acceptance_tests": ["the node states both axioms, derives the always-a-next-step guarantee, and tags the axiom statements ESTIMATE-to-source-verify"],
      "revisit_triggers": ["an authored graph is found to violate accessibility", "a dead-end state is observed in practice", "the axioms are source-verified against Falmagne & Doignon"],
      "base": { "criticality": 0.82, "business_value": 0.72, "user_value": 0.78, "technical_complexity": 0.58, "risk_if_wrong": 0.68, "cross_topic_coupling": 0.8, "irreversibility": 0.4, "confidence": 0.64, "node_conflict_pressure": 0.42, "acceptance_test_pass_rate": 0.8, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.82, "evidence_confidence": 0.6, "failure_rate": 0.18, "downside_weight": 0.48, "uncertainty_interval": [0.3, 0.58], "update_signal": "an axiom violation in an authored graph or a source-verification changes the learning-space treatment" }
    },
    {
      "id": "SURMISE_PREREQUISITE",
      "topic": "surmise_relation_as_prerequisite",
      "definition": "Formalize 'prerequisite' as the SURMISE relation: if observing mastery of item b lets you surmise (infer) mastery of item a, then a precedes b. For a quasi-ordinal knowledge space the admissible states are exactly the down-sets of a quasi-order on items, and by the Birkhoff correspondence that quasi-order IS the prerequisite structure — so the prerequisite graph and the knowledge space are two views of one object [ESTIMATE — to source-verify for the Birkhoff/quasi-ordinal statement]. This is the bridge from concept-map hierarchy and KST states to the app's ConceptEdge type='prereq': a prereq edge asserts a surmise dependency, and the set of all such edges defines which knowledge states are reachable. It also means a prerequisite relation must be transitive-consistent and, crucially, acyclic.",
      "group": "kst",
      "dependencies": ["KST_KNOWLEDGE_STATES"],
      "competency_question_refs": ["CQ_05"],
      "scope_boundary": { "included": ["the surmise relation as prerequisite precedence", "the quasi-order / down-set correspondence to states", "prereq edges as surmise assertions"], "excluded": ["the DAG acyclicity mechanics and topological order (its own node)", "the gating behaviour (its own node)"] },
      "pros": [ { "claim": "Grounding prereq edges in the surmise relation gives the app's prereq type a precise, testable meaning tied to the state space", "example": "justifying a prereq edge only when mastering the successor genuinely implies mastering the predecessor", "weight": 0.82 } ],
      "cons": [ { "claim": "Real prerequisites are often soft or probabilistic, not the hard surmise the quasi-ordinal model assumes, so strict edges can overstate necessity", "example": "marking a helpful-but-not-required concept as a hard prerequisite and needlessly blocking study", "weight": 0.64 } ],
      "failure_modes": ["asserting a prereq edge where the dependency is merely helpful, not surmisable", "ignoring transitivity so redundant prereq edges pile up"],
      "acceptance_tests": ["the node defines the surmise relation, links it to the quasi-order/down-set view of states (tagged ESTIMATE), and maps it onto ConceptEdge type='prereq'"],
      "revisit_triggers": ["soft/probabilistic prerequisites are needed", "transitive-redundant edges accumulate", "the quasi-ordinal claim is source-verified"],
      "base": { "criticality": 0.86, "business_value": 0.76, "user_value": 0.78, "technical_complexity": 0.55, "risk_if_wrong": 0.72, "cross_topic_coupling": 0.82, "irreversibility": 0.42, "confidence": 0.66, "node_conflict_pressure": 0.44, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.86, "evidence_confidence": 0.62, "failure_rate": 0.16, "downside_weight": 0.5, "uncertainty_interval": [0.28, 0.56], "update_signal": "a need for soft prerequisites or a source-verification of the quasi-ordinal link changes the prerequisite formalism" }
    },
    {
      "id": "FRINGE_OUTER_INNER",
      "topic": "outer_inner_fringe_study_frontier",
      "definition": "Define the fringes of a knowledge state, the operational output of KST for sequencing: the OUTER FRINGE is the set of items the learner could learn NEXT — each is one item whose addition yields another admissible state — and the INNER FRINGE is the set of items most recently learnable (removing one still leaves a state) [ESTIMATE — to source-verify]. The outer fringe is precisely the set of currently UNLOCKABLE concepts: every prerequisite of an outer-fringe item is already mastered. This is the concrete bridge to gating — 'what should I study now' is answered by the outer fringe, and 'is this concept unlocked yet' is answered by whether it sits on the outer fringe of the learner's current state. The inner fringe flags what to review to consolidate a state.",
      "group": "kst",
      "dependencies": ["KST_LEARNING_SPACE"],
      "competency_question_refs": ["CQ_04", "CQ_08"],
      "scope_boundary": { "included": ["outer fringe as the next-learnable/unlockable set", "inner fringe as recently-learnable/review set", "the fringe as the sequencing output"], "excluded": ["the mastery-threshold mechanics of the gate (its own node)", "the gating risk analysis (its own node)"] },
      "pros": [ { "claim": "The outer fringe turns the abstract state space into a directly usable 'study these next' list a UI can render", "example": "surfacing exactly the concepts whose prerequisites are all mastered as the unlocked set", "weight": 0.82 } ],
      "cons": [ { "claim": "Computing the true fringe requires knowing the learner's exact state, which is only estimated (noisy BKT mastery), so the fringe is an estimate not a certainty", "example": "a concept flickering on and off the outer fringe as a borderline prerequisite mastery estimate wobbles", "weight": 0.62 } ],
      "failure_modes": ["computing the fringe from a point mastery estimate without accounting for its noise", "confusing the inner fringe (review) with the outer fringe (learn next)"],
      "acceptance_tests": ["the node defines outer and inner fringe, identifies the outer fringe as the unlockable set feeding the gate, and tags the definitions ESTIMATE-to-source-verify"],
      "revisit_triggers": ["mastery-estimate noise makes the fringe unstable", "the UI needs a different next-item ranking", "the fringe definitions are source-verified"],
      "base": { "criticality": 0.82, "business_value": 0.74, "user_value": 0.82, "technical_complexity": 0.5, "risk_if_wrong": 0.66, "cross_topic_coupling": 0.8, "irreversibility": 0.38, "confidence": 0.66, "node_conflict_pressure": 0.42, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.82, "evidence_confidence": 0.62, "failure_rate": 0.16, "downside_weight": 0.46, "uncertainty_interval": [0.28, 0.56], "update_signal": "fringe instability from estimate noise or a re-ranking need changes the frontier logic" }
    },
    {
      "id": "PREREQUISITE_DAG",
      "topic": "prerequisite_dag_acyclicity_topo_order",
      "definition": "Require the prerequisite edges to form a DIRECTED ACYCLIC GRAPH. A cycle in prereq edges asserts that a concept is (transitively) its own prerequisite — a contradiction that makes some concepts permanently unlockable and breaks the learning-space accessibility guarantee. So before the graph is used for gating it must pass an acyclicity check, and any TOPOLOGICAL ordering of the DAG is then a valid end-to-end study sequence [FACT — standard graph property; its application here is design synthesis]. The app's diagnosis walks prereq edges to find an unmastered prerequisite [FACT-source: APP_RECON §4a], which only terminates safely if the edges are acyclic; a cycle would let a naive walk loop. This node is the correctness gate on the whole prerequisite layer.",
      "group": "prerequisite_structure",
      "dependencies": ["SURMISE_PREREQUISITE"],
      "competency_question_refs": ["CQ_05", "CQ_09"],
      "scope_boundary": { "included": ["acyclicity as a hard requirement", "topological order as a valid study sequence", "the acyclicity precheck before gating"], "excluded": ["the gating mechanism that consumes the DAG (its own node)", "the surmise semantics of each edge (its own node)"] },
      "pros": [ { "claim": "An enforced acyclic prerequisite DAG guarantees a valid global study order and a terminating prerequisite walk", "example": "topologically sorting concepts so every concept appears after all its prerequisites", "weight": 0.86 } ],
      "cons": [ { "claim": "Authoring or LLM-generation can easily introduce a cycle, and without an explicit check the gate silently deadlocks or loops", "example": "an LLM adding A->B and B->A prereq edges that lock both concepts forever", "weight": 0.68 } ],
      "failure_modes": ["shipping a prereq graph with an undetected cycle that permanently blocks concepts", "walking prereq edges without a visited-set and looping on a cycle"],
      "acceptance_tests": ["the node requires an acyclicity precheck, states topological order as a valid study sequence, and ties a cycle to permanent unlockability and walk non-termination"],
      "revisit_triggers": ["a cycle is detected in an authored or generated graph", "the prerequisite walk is observed to loop or stall", "the acyclicity check is added or changed"],
      "base": { "criticality": 0.9, "business_value": 0.8, "user_value": 0.82, "technical_complexity": 0.5, "risk_if_wrong": 0.82, "cross_topic_coupling": 0.82, "irreversibility": 0.48, "confidence": 0.76, "node_conflict_pressure": 0.42, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.9, "evidence_confidence": 0.74, "failure_rate": 0.16, "downside_weight": 0.55, "uncertainty_interval": [0.22, 0.48], "update_signal": "a detected cycle or a looping prerequisite walk changes the acyclicity handling" }
    },
    {
      "id": "GRAPH_SHAPES_FOR_LEARNING",
      "topic": "which_graph_shapes_aid_learning",
      "definition": "State which graph SHAPES actually aid learning and retrieval and which harm them: a SPARSE, mostly HIERARCHICAL graph with a small number of high-value cross-links supports organized encoding and legible study paths, whereas an OVER-DENSE 'hairball' (near-complete connectivity) degrades both — it offers no sequencing signal, overwhelms the learner, and makes any gate block almost everything [ESTIMATE — to source-verify; grounded in concept-map hierarchy evidence and network-organization arguments]. Moderate, meaningful connectivity is the target: enough edges to express real prerequisites and integrative links, few enough that the structure remains a readable tree-plus-cross-links rather than a mesh. Concretely, favour low average out-degree on prereq edges and reserve 'related' edges for genuinely integrative pairs.",
      "group": "graph_shapes",
      "dependencies": ["HIERARCHY_CROSSLINKS"],
      "competency_question_refs": ["CQ_06", "CQ_10"],
      "scope_boundary": { "included": ["sparse-hierarchy-plus-few-cross-links as the target shape", "the hairball as the anti-pattern", "low-out-degree/reserved-related-edge heuristics"], "excluded": ["retrieval-mechanism specifics (its own node)", "hub-node degree analysis (its own node)"] },
      "pros": [ { "claim": "A shape target lets an author or generator be told concretely to keep the graph sparse and hierarchical rather than maximally connected", "example": "capping related-edge density so the graph stays a tree-plus-cross-links", "weight": 0.82 } ],
      "cons": [ { "claim": "The 'sparse is better' guidance is an ESTIMATE without a deck-specific density threshold, so the exact cutoff is a tunable judgment", "example": "not knowing whether 1 or 3 cross-links per branch is the right budget for INBDE", "weight": 0.64 } ],
      "failure_modes": ["letting connectivity grow into a hairball with no sequencing signal", "over-pruning until the graph is disconnected trees with no integration"],
      "acceptance_tests": ["the node names the sparse-hierarchy target and the hairball anti-pattern, gives the out-degree/related-reservation heuristics, and tags the shape claims ESTIMATE-to-source-verify"],
      "revisit_triggers": ["a deck-specific density threshold is measured", "the graph is observed to trend toward a hairball", "disconnection harms integration"],
      "base": { "criticality": 0.8, "business_value": 0.74, "user_value": 0.78, "technical_complexity": 0.48, "risk_if_wrong": 0.66, "cross_topic_coupling": 0.82, "irreversibility": 0.38, "confidence": 0.64, "node_conflict_pressure": 0.46, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.8, "evidence_confidence": 0.6, "failure_rate": 0.18, "downside_weight": 0.46, "uncertainty_interval": [0.3, 0.58], "update_signal": "a measured density threshold or an observed hairball trend changes the shape target" }
    },
    {
      "id": "HUB_NODES",
      "topic": "hub_anchor_concepts_and_their_risk",
      "definition": "Characterize HUB / ANCHOR concepts: high-degree nodes that many others depend on or link to. In assimilation terms a hub is a subsumption anchor — a general concept new material attaches to — so hubs are where integrative understanding concentrates and are natural entry points for a domain [ESTIMATE — to source-verify for the subsumption framing]. But a hub carries a specific graph-shape RISK: because a prerequisite hub sits upstream of many concepts, if it is mislabeled or set as a hard prerequisite it gates a large downstream subgraph, and an over-connected hub can make the graph brittle. The design rule: allow a few deliberate hubs as organizing anchors, watch their out-degree on prereq edges, and never let an accidental hub silently become a global bottleneck.",
      "group": "graph_shapes",
      "dependencies": ["GRAPH_SHAPES_FOR_LEARNING", "HIERARCHY_CROSSLINKS"],
      "competency_question_refs": ["CQ_07"],
      "scope_boundary": { "included": ["hubs as high-degree subsumption anchors", "the downstream-gating and brittleness risk of prerequisite hubs", "the deliberate-few-hubs design rule"], "excluded": ["the general density target (its own node)", "hub graph-algorithm computation (neighboring ontology KB)"] },
      "pros": [ { "claim": "Recognizing hubs lets the design use a few of them as intentional anchors while guarding against accidental bottlenecks", "example": "designating 'cell metabolism' a deliberate anchor while flagging an accidental 20-child prereq hub for review", "weight": 0.8 } ],
      "cons": [ { "claim": "The line between a useful anchor and a harmful bottleneck is a judgment, and over-caution can strip the graph of the anchors that aid organization", "example": "removing a legitimate foundational hub because its degree looked high", "weight": 0.62 } ],
      "failure_modes": ["an accidental prerequisite hub gating a huge downstream subgraph", "stripping legitimate anchors in the name of sparsity"],
      "acceptance_tests": ["the node defines hubs as high-degree anchors, states the downstream-gating/brittleness risk of prerequisite hubs, and gives the deliberate-few-hubs rule with degree-watching"],
      "revisit_triggers": ["an accidental hub is found gating a large subgraph", "an anchor is over-pruned", "a hub-degree threshold is set"],
      "base": { "criticality": 0.78, "business_value": 0.72, "user_value": 0.74, "technical_complexity": 0.5, "risk_if_wrong": 0.68, "cross_topic_coupling": 0.82, "irreversibility": 0.4, "confidence": 0.66, "node_conflict_pressure": 0.48, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.78, "evidence_confidence": 0.62, "failure_rate": 0.18, "downside_weight": 0.48, "uncertainty_interval": [0.28, 0.56], "update_signal": "an accidental-hub bottleneck or an over-prune of anchors changes the hub policy" }
    },
    {
      "id": "RETRIEVAL_STRUCTURE",
      "topic": "structure_that_aids_retrieval",
      "definition": "Explain WHY the right structure aids retrieval, not just encoding. Organized, hierarchically structured material is recalled better than a flat list, and elaborated links (knowing WHY A connects to B) give more retrieval routes — the concept-map linking phrase and the 'related' cross-link are the graph's carriers of that elaboration [ESTIMATE — to source-verify; spreading-activation and organized-recall arguments]. Spreading activation predicts that retrieving one concept partially primes its graph neighbours, so a moderately connected graph creates helpful retrieval cues while a hairball creates interference. The design consequence for this app: the graph is not only a sequencing device but a retrieval scaffold, so edges should encode real, elaborable relationships, and this is a further argument for meaningful-but-sparse connectivity over maximal connectivity.",
      "group": "graph_shapes",
      "dependencies": ["PROPOSITIONAL_STRUCTURE", "GRAPH_SHAPES_FOR_LEARNING"],
      "competency_question_refs": ["CQ_06"],
      "scope_boundary": { "included": ["organized-structure and elaboration as retrieval boosters", "spreading activation as the neighbour-priming mechanism", "the graph as a retrieval scaffold argument"], "excluded": ["the sequencing/gating use of the graph (other nodes)", "the mastery estimator (learner KB)"] },
      "pros": [ { "claim": "Framing the graph as a retrieval scaffold gives a learning reason (not just a sequencing reason) to keep edges meaningful and sparse", "example": "justifying a cross-link because it adds a genuine retrieval route between two branches", "weight": 0.8 } ],
      "cons": [ { "claim": "The retrieval benefits are cited from general memory research, not measured on this graph, so they are ESTIMATE-level motivation rather than a guarantee for this deck", "example": "assuming a cross-link improves recall here without an A/B retention test", "weight": 0.64 } ],
      "failure_modes": ["adding edges that carry no elaborable relationship and create interference not cues", "over-claiming a retrieval benefit the app has not measured"],
      "acceptance_tests": ["the node ties organized structure and elaboration to better retrieval, names spreading activation as the mechanism, frames the graph as a retrieval scaffold, and tags the claims ESTIMATE-to-source-verify"],
      "revisit_triggers": ["a retention test measures a cross-link's retrieval effect", "interference from over-connection is observed", "the memory-research claims are source-verified"],
      "base": { "criticality": 0.76, "business_value": 0.7, "user_value": 0.78, "technical_complexity": 0.45, "risk_if_wrong": 0.6, "cross_topic_coupling": 0.78, "irreversibility": 0.35, "confidence": 0.62, "node_conflict_pressure": 0.42, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.76, "evidence_confidence": 0.58, "failure_rate": 0.18, "downside_weight": 0.42, "uncertainty_interval": [0.32, 0.6], "update_signal": "a retention measurement or observed interference changes the retrieval-scaffold claim" }
    },
    {
      "id": "FLAT_SPINE_GRADUATION",
      "topic": "flat_spine_to_full_graph_graduation",
      "definition": "Give the graduation policy from a FLAT prerequisite SPINE to a FULL cross-linked graph. Early, with little data and few concepts, model the domain as a hierarchical TREE or a single linear prerequisite spine — this is legible, trivially acyclic, and enough to sequence study. Add 'related' cross-link edges and additional prereq branches only as EVIDENCE accrues: co-mastery patterns (concepts learned together), authoring signals (a card explicitly bridges two concepts), or observed confusions [ESTIMATE — design synthesis over the graph-shape and concept-map evidence]. The rule is deliberate sparsity first, connectivity earned later, so the graph never starts as a hairball and each added edge has a justification. This is the structural counterpart of the learner-model 'start simple, add complexity only when data supports it' discipline.",
      "group": "graph_shapes",
      "dependencies": ["PREREQUISITE_DAG", "GRAPH_SHAPES_FOR_LEARNING"],
      "competency_question_refs": ["CQ_10"],
      "scope_boundary": { "included": ["start-as-tree/spine then earn connectivity", "the evidence signals that justify a new edge", "deliberate-sparsity-first discipline"], "excluded": ["the specific co-mastery statistics (learner KB)", "the final verdict composition (its own node)"] },
      "pros": [ { "claim": "A graduation policy prevents a premature hairball and makes every added edge accountable to evidence", "example": "adding a cross-link only after two concepts are repeatedly co-mastered rather than up front", "weight": 0.82 } ],
      "cons": [ { "claim": "Waiting for evidence delays useful integrative links, so a genuinely important cross-link may be missing early on", "example": "an obvious pharmacology-pathology bridge absent until enough co-mastery data accrues", "weight": 0.6 } ],
      "failure_modes": ["building the full dense graph up front before any evidence justifies the edges", "never graduating so the graph stays a bare spine with no integration"],
      "acceptance_tests": ["the node prescribes starting as a tree/spine, lists the evidence signals that earn a new edge, and states the sparsity-first-connectivity-earned discipline"],
      "revisit_triggers": ["a needed cross-link is found missing too long", "the graph fails to graduate past the spine", "the evidence signals for adding edges are refined"],
      "base": { "criticality": 0.82, "business_value": 0.76, "user_value": 0.78, "technical_complexity": 0.5, "risk_if_wrong": 0.66, "cross_topic_coupling": 0.82, "irreversibility": 0.42, "confidence": 0.66, "node_conflict_pressure": 0.44, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.82, "evidence_confidence": 0.62, "failure_rate": 0.16, "downside_weight": 0.46, "uncertainty_interval": [0.28, 0.56], "update_signal": "a missing cross-link or a failure to graduate changes the graduation policy" }
    },
    {
      "id": "PREREQ_GATING_MECHANISM",
      "topic": "prerequisite_gating_block_until_mastered",
      "definition": "Specify how prerequisite edges and the learning fringe drive 'BLOCK new cards on a concept until its prerequisite is mastered'. The mechanism: a dependent concept is UNLOCKED (its new cards may be introduced) only when every concept upstream of it via prereq edges has mastery clearing a threshold — equivalently, only when the concept lies on the learner's OUTER FRINGE. The app already encodes this as a diagnosis rule: when a missed concept has any prereq concept with bkt_p_mastery < 0.5 it emits 'missing_prereq' and the fix string 'Schedule the prerequisite concept first; block new cards on this concept until prereq mastery >= 0.6' — but this blocking is TEXT ONLY, enforced by no code today [FACT-source: APP_RECON §4a]. This node supplies the learning-science logic a real enforcing gate would implement: walk prereq edges, check each prerequisite's mastery, and withhold new cards until all clear.",
      "group": "gating",
      "dependencies": ["PREREQUISITE_DAG", "FRINGE_OUTER_INNER"],
      "competency_question_refs": ["CQ_08"],
      "scope_boundary": { "included": ["unlock-when-all-prereqs-mastered logic", "the outer-fringe equivalence", "the app's existing text-only missing_prereq rule as the integration target"], "excluded": ["the specific threshold and hysteresis values (its own node)", "the over/under-gating risk analysis (its own node)"] },
      "pros": [ { "claim": "A prerequisite gate stops a learner drilling a concept whose foundations they lack, directing effort to the prerequisite first", "example": "withholding advanced periodontics cards until basic periodontal anatomy clears mastery", "weight": 0.84 } ],
      "cons": [ { "claim": "The gate is only as correct as the prereq edges and the mastery estimate, and today it is unenforced text, so specifying it does not make it real", "example": "a wrong prereq edge blocking a concept the learner is actually ready for", "weight": 0.66 } ],
      "failure_modes": ["blocking on a mislabeled prerequisite and starving a ready concept", "reading 'block until mastered' as implemented when it is diagnosis text only"],
      "acceptance_tests": ["the node states the unlock-when-all-prereqs-clear logic, equates the unlocked set with the outer fringe, and cites the app's text-only missing_prereq rule as the target with the source tag"],
      "revisit_triggers": ["a real enforcing gate is implemented", "a prereq edge is found to block a ready concept", "the mastery signal feeding the gate changes"],
      "base": { "criticality": 0.9, "business_value": 0.82, "user_value": 0.84, "technical_complexity": 0.52, "risk_if_wrong": 0.82, "cross_topic_coupling": 0.86, "irreversibility": 0.48, "confidence": 0.7, "node_conflict_pressure": 0.5, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.9, "evidence_confidence": 0.7, "failure_rate": 0.18, "downside_weight": 0.55, "uncertainty_interval": [0.24, 0.52], "update_signal": "an enforcing gate implementation or a mislabeled-prereq block changes the gating mechanism" }
    },
    {
      "id": "MASTERY_THRESHOLD_GATE",
      "topic": "gate_threshold_and_hysteresis",
      "definition": "Set the mastery threshold logic for the gate. The app uses an asymmetric band: a prerequisite counts as WEAK (triggering the missing_prereq diagnosis) when its bkt_p_mastery < 0.5, but the unblock condition is stated as mastery >= 0.6 [FACT-source: APP_RECON §4a]. Read as a HYSTERESIS band, this gap (0.5 to unlock-fail, 0.6 to unlock-pass) is exactly the guard against a concept flickering unlocked/locked as a borderline mastery estimate wobbles across a single threshold [ESTIMATE — the hysteresis interpretation is design synthesis; the two numbers are FACT]. The design rule: gate on a band, not a point; require the prerequisite to reach the higher bound to unlock and only re-lock if it falls below the lower bound; and choose the band wide enough to absorb mastery-estimate noise but narrow enough not to over-block.",
      "group": "gating",
      "dependencies": ["PREREQ_GATING_MECHANISM"],
      "competency_question_refs": ["CQ_08"],
      "scope_boundary": { "included": ["the 0.5-weak / 0.6-unlock band from the app", "the hysteresis interpretation against threshold flicker", "the gate-on-a-band-not-a-point rule"], "excluded": ["how mastery itself is estimated (learner KB)", "the broader gating risks (its own node)"] },
      "pros": [ { "claim": "A hysteresis band stops a concept oscillating between locked and unlocked as a borderline mastery estimate jitters", "example": "not re-locking a concept whose prereq mastery dips from 0.61 to 0.59 after one slip", "weight": 0.82 } ],
      "cons": [ { "claim": "The specific 0.5/0.6 values are the app's choice, not a validated optimum, so the band width is a heuristic that may over- or under-block", "example": "a 0.6 bar being too high for a concept the learner could handle at 0.55", "weight": 0.62 } ],
      "failure_modes": ["gating on a single point threshold and flickering on noise", "treating 0.5/0.6 as validated optima rather than tunable heuristics"],
      "acceptance_tests": ["the node states the 0.5/0.6 band from APP_RECON, interprets it as hysteresis against flicker (tagged), and prescribes gating on a band not a point"],
      "revisit_triggers": ["threshold flicker is still observed", "the 0.5/0.6 values are re-tuned from retention data", "the mastery estimator's scale changes"],
      "base": { "criticality": 0.8, "business_value": 0.74, "user_value": 0.78, "technical_complexity": 0.45, "risk_if_wrong": 0.66, "cross_topic_coupling": 0.74, "irreversibility": 0.38, "confidence": 0.7, "node_conflict_pressure": 0.44, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.8, "evidence_confidence": 0.68, "failure_rate": 0.16, "downside_weight": 0.45, "uncertainty_interval": [0.26, 0.52], "update_signal": "persistent flicker or a re-tune of the 0.5/0.6 values changes the threshold band" }
    },
    {
      "id": "GATING_RISKS",
      "topic": "over_and_under_gating_bounds",
      "definition": "Bound the failure space of prerequisite gating. OVER-GATING harms: a mislabeled or too-strict prereq edge, or a cyclic DAG, can DEADLOCK a concept (permanently unlockable), orphan concepts behind an unreachable prerequisite, or frustrate a learner who is actually ready. UNDER-GATING harms: too few or too-weak prerequisites let a learner drill advanced material with no foundation, wasting effort. The mitigations: run the acyclicity precheck (from the DAG node) before gating; provide an explicit ESCAPE HATCH so a learner or author can override a block; keep the hysteresis band so borderline mastery does not thrash; and cap prerequisite chain depth so no concept sits behind an unreasonably long unlock path [ESTIMATE — design synthesis]. A gate with no override is more dangerous than no gate, because a single bad edge silently removes content.",
      "group": "gating",
      "dependencies": ["PREREQ_GATING_MECHANISM"],
      "competency_question_refs": ["CQ_09"],
      "scope_boundary": { "included": ["over-gating deadlock/orphan/frustration harms", "under-gating no-foundation harm", "escape hatch, acyclicity precheck, hysteresis, chain-depth cap mitigations"], "excluded": ["the acyclicity mechanics themselves (DAG node)", "the threshold values (threshold node)"] },
      "pros": [ { "claim": "Naming both failure directions and the escape hatch keeps a single bad prereq edge from silently starving the learner of content", "example": "letting a learner override a block on a concept a wrong edge locked", "weight": 0.82 } ],
      "cons": [ { "claim": "An escape hatch that is too easy to hit dissolves the gate's value, so the override must be deliberate not default", "example": "a one-tap skip that learners use to bypass every legitimate prerequisite", "weight": 0.62 } ],
      "failure_modes": ["shipping a gate with no override so a bad edge permanently hides content", "making the override so easy the gate never actually gates"],
      "acceptance_tests": ["the node names over-gating and under-gating harms and prescribes the escape hatch, acyclicity precheck, hysteresis, and chain-depth cap as mitigations"],
      "revisit_triggers": ["a deadlock or orphaned concept is reported", "the override is over- or under-used", "a prerequisite chain is found too deep"],
      "base": { "criticality": 0.84, "business_value": 0.76, "user_value": 0.82, "technical_complexity": 0.48, "risk_if_wrong": 0.8, "cross_topic_coupling": 0.8, "irreversibility": 0.5, "confidence": 0.68, "node_conflict_pressure": 0.5, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.84, "evidence_confidence": 0.64, "failure_rate": 0.2, "downside_weight": 0.55, "uncertainty_interval": [0.26, 0.54], "update_signal": "a deadlock report or override misuse changes the gating-risk mitigations" }
    },
    {
      "id": "EDGE_TYPE_SEMANTICS",
      "topic": "conceptedge_type_to_relation_mapping",
      "definition": "Map the three ConceptEdge types onto learning-science relations so authors and generators use them consistently. PREREQ = the surmise/precedence relation forming the acyclic study-order DAG (must be mastered before). PART_OF = hierarchical/mereological composition (this concept is a component of that broader one) — the concept-map hierarchy backbone, also directional and acyclic. RELATED = an associative cross-link between concepts with no precedence — the integrative connective tissue, kept sparse [FACT for the enum values, source: APP_RECON entity schema; ESTIMATE for the learning-science mapping]. The critical discipline: keep prereq for genuine study-order dependencies only (it is the type that gates), keep part_of for composition, and do NOT let 'related' become a dumping ground — an over-used 'related' edge set is how a clean graph degrades into a hairball.",
      "group": "integration",
      "dependencies": ["PROPOSITIONAL_STRUCTURE", "SURMISE_PREREQUISITE", "PREREQUISITE_DAG"],
      "competency_question_refs": ["CQ_11"],
      "scope_boundary": { "included": ["prereq->surmise/DAG, part_of->composition, related->associative mapping", "the keep-prereq-strict and don't-dump-into-related discipline"], "excluded": ["adding new edge types (out of scope; schema is fixed)", "the color meaning layer that reads the types (its own node)"] },
      "pros": [ { "claim": "A fixed semantics per edge type keeps authoring and LLM-generation consistent and keeps prereq (the gating type) clean", "example": "an author using part_of for 'incisor is part of dentition' and prereq only for real study-order needs", "weight": 0.84 } ],
      "cons": [ { "claim": "Only three types cannot capture every relation, so some real relationships are forced into an imperfect bucket or left off the graph", "example": "a causal 'A causes B' relation with no exact type, mapped approximately to related", "weight": 0.64 } ],
      "failure_modes": ["using prereq for merely-helpful relations and over-gating", "dumping every association into related until it is meaningless"],
      "acceptance_tests": ["the node maps each of prereq/part_of/related to its learning-science relation (enum source-tagged), and states the keep-prereq-strict and don't-dump-related discipline"],
      "revisit_triggers": ["the ConceptEdge enum is extended", "'related' is observed as an unusable catch-all", "a needed relation has no good type"],
      "base": { "criticality": 0.86, "business_value": 0.8, "user_value": 0.8, "technical_complexity": 0.48, "risk_if_wrong": 0.74, "cross_topic_coupling": 0.84, "irreversibility": 0.45, "confidence": 0.72, "node_conflict_pressure": 0.46, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.86, "evidence_confidence": 0.7, "failure_rate": 0.16, "downside_weight": 0.5, "uncertainty_interval": [0.24, 0.5], "update_signal": "an enum extension or a catch-all 'related' problem changes the type semantics" }
    },
    {
      "id": "NODE_GRANULARITY",
      "topic": "concept_node_granularity_masterable_unit",
      "definition": "Fix what a Concept NODE should be: a single MASTERABLE, TESTABLE unit — a KST item — not a whole book chapter or a broad topic. The 'books -> fundamental units -> graph' vision means decomposing source material until each node is something a learner can be said to have mastered and a card can directly assess, which is the grain at which prerequisites, mastery estimates, and gating are meaningful [ESTIMATE — design synthesis over KST item semantics and concept-map concept-as-regularity]. Too COARSE (a node = 'Pharmacology') makes mastery and prerequisites meaningless; too FINE (a node per fact) explodes the graph and buries structure. The app's Concept is a per-exam named unit tied to cards [FACT-source: APP_RECON]; this node sets the authoring target grain: roughly one testable proposition-cluster per Concept, aligned to how cards tag concepts.",
      "group": "integration",
      "dependencies": ["KST_KNOWLEDGE_STATES", "CONCEPT_MAPPING_NOVAK"],
      "competency_question_refs": ["CQ_11"],
      "scope_boundary": { "included": ["a Concept node as a masterable/testable KST item", "the too-coarse/too-fine failure bounds", "aligning node grain to card concept-tags"], "excluded": ["terminology-level entities within card text (biomedical KB)", "the exam-specific taxonomy fields on Concept (contenteng)"] },
      "pros": [ { "claim": "Setting the node grain to a masterable unit makes prerequisites, mastery, and gating all meaningful at the same level", "example": "a Concept 'local anesthetic mechanism' that a card can test and a gate can require, versus a whole 'Anesthesia' chapter", "weight": 0.82 } ],
      "cons": [ { "claim": "The right grain is a judgment with no crisp rule, so different authors can decompose the same material inconsistently", "example": "one author making 'caries' one node and another splitting it into five", "weight": 0.62 } ],
      "failure_modes": ["nodes so coarse that mastery and prerequisites are meaningless", "nodes so fine the graph explodes and structure is lost"],
      "acceptance_tests": ["the node defines a Concept as a masterable/testable KST item, states the too-coarse/too-fine bounds, and aligns the grain to card concept-tags with the source tag"],
      "revisit_triggers": ["authors decompose material inconsistently", "the graph explodes or collapses at the chosen grain", "the card-tagging convention changes"],
      "base": { "criticality": 0.82, "business_value": 0.76, "user_value": 0.78, "technical_complexity": 0.46, "risk_if_wrong": 0.68, "cross_topic_coupling": 0.8, "irreversibility": 0.44, "confidence": 0.68, "node_conflict_pressure": 0.44, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.82, "evidence_confidence": 0.66, "failure_rate": 0.16, "downside_weight": 0.48, "uncertainty_interval": [0.26, 0.54], "update_signal": "inconsistent decomposition or a card-tagging change updates the granularity target" }
    },
    {
      "id": "KST_ASSESSMENT_VS_CONTENTENG",
      "topic": "what_kst_adds_beyond_contenteng_edge_table",
      "definition": "Pin the boundary with contenteng and state what THIS KB's KST treatment adds. contenteng produces the edge table and explicitly DEFERS the full Knowledge Space Theory treatment [FACT-source: SPECIALIST_LIST §C1, KG_RECON §2a]. What KST adds on top of the raw edges: (1) knowledge STATES — a principled model of which combinations of mastered concepts are admissible, not just isolated mastery scores; (2) the learning FRINGE — a computed 'what to study next / what is unlocked' set; and (3) the surmise-based GATE — the prerequisite logic that turns edges into 'block until mastered'. KST-based adaptive assessment (efficiently locating a learner's state by choosing maximally informative items, the approach popularized by ALEKS) is the natural extension [ESTIMATE — to source-verify for the ALEKS attribution] but is beyond this app today; this KB claims the states/fringe/gate layer, not a full adaptive-assessment engine.",
      "group": "integration",
      "dependencies": ["KST_LEARNING_SPACE", "SURMISE_PREREQUISITE"],
      "competency_question_refs": ["CQ_12"],
      "scope_boundary": { "included": ["contenteng defers KST (source-tagged)", "the states/fringe/gate layer this KB adds", "adaptive assessment (ALEKS-style) named as the deferred extension"], "excluded": ["building a full adaptive-assessment item-selection engine", "the contenteng edge-authoring process"] },
      "pros": [ { "claim": "Naming exactly what KST adds beyond the edge table keeps this KB's contribution crisp and non-overlapping with contenteng", "example": "claiming the fringe and gate while leaving edge authoring to contenteng", "weight": 0.82 } ],
      "cons": [ { "claim": "Stopping short of full adaptive assessment leaves the most powerful KST capability on the table for now", "example": "not selecting maximally informative diagnostic items to pinpoint the learner's state", "weight": 0.6 } ],
      "failure_modes": ["re-implementing contenteng's edge authoring instead of the KST layer", "over-claiming a full ALEKS-style assessment engine this app lacks"],
      "acceptance_tests": ["the node cites contenteng's KST deferral, lists the states/fringe/gate additions, and marks adaptive assessment as a deferred extension with the ALEKS attribution tagged ESTIMATE"],
      "revisit_triggers": ["adaptive assessment is scoped in", "contenteng changes its deferral boundary", "the ALEKS attribution is source-verified"],
      "base": { "criticality": 0.82, "business_value": 0.78, "user_value": 0.78, "technical_complexity": 0.5, "risk_if_wrong": 0.68, "cross_topic_coupling": 0.82, "irreversibility": 0.42, "confidence": 0.7, "node_conflict_pressure": 0.44, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.82, "evidence_confidence": 0.66, "failure_rate": 0.16, "downside_weight": 0.48, "uncertainty_interval": [0.26, 0.52], "update_signal": "scoping in adaptive assessment or a contenteng boundary change updates this delineation" }
    },
    {
      "id": "COLOR_MEANING_LAYER",
      "topic": "color_coded_data_and_meaning_layer",
      "definition": "Specify WHAT the color-coded data-and-meaning layer must express (leaving HOW to render it to the viz specialist). Each Concept node carries a MEANING to surface: its knowledge STATE membership (mastered / on the outer fringe / locked behind a prerequisite / not yet reachable) and its mastery level, and each edge carries its TYPE (prereq / part_of / related). The color layer's job is to make those two encodings legible at a glance — for example distinguishing a mastered concept from an unlocked-but-unstudied one from a still-locked one, and distinguishing a gating prereq edge from an associative related edge [ESTIMATE — design synthesis; the state categories derive from the KST nodes, the edge types from APP_RECON]. This node defines the semantic CONTRACT the viz layer colors against; it does not choose palettes, hues, or layout.",
      "group": "integration",
      "dependencies": ["KST_KNOWLEDGE_STATES", "EDGE_TYPE_SEMANTICS"],
      "competency_question_refs": ["CQ_12"],
      "scope_boundary": { "included": ["the node-state categories (mastered/fringe/locked/unreachable) and edge-type distinctions the colors must express", "the meaning-contract-not-render-choice framing"], "excluded": ["palette, hue, contrast and layout choices (viz specialist)", "the mastery estimator itself (learner KB)"] },
      "pros": [ { "claim": "Defining the meaning contract lets viz choose colors freely while guaranteeing the encodings carry the right learning-science distinctions", "example": "specifying that locked, unlocked-unstudied, and mastered must be visually separable without dictating which colors", "weight": 0.8 } ],
      "cons": [ { "claim": "Splitting meaning from render risks a mismatch if viz encodes categories the meaning layer did not intend or drops ones it did", "example": "viz collapsing 'locked' and 'unreachable' into one color and losing a distinction the gate relies on", "weight": 0.62 } ],
      "failure_modes": ["specifying render details that belong to viz and duplicating its job", "leaving a state category undefined so viz invents its own semantics"],
      "acceptance_tests": ["the node lists the node-state categories and edge-type distinctions the color layer must express and frames itself as the meaning contract, deferring palette/layout to viz"],
      "revisit_triggers": ["viz reports a category it cannot render distinctly", "a new state category is added by the KST layer", "the mastery scale changes what levels to show"],
      "base": { "criticality": 0.78, "business_value": 0.76, "user_value": 0.8, "technical_complexity": 0.42, "risk_if_wrong": 0.64, "cross_topic_coupling": 0.82, "irreversibility": 0.38, "confidence": 0.68, "node_conflict_pressure": 0.44, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.78, "evidence_confidence": 0.66, "failure_rate": 0.15, "downside_weight": 0.44, "uncertainty_interval": [0.26, 0.52], "update_signal": "a viz-render mismatch or a new state category changes the meaning contract" }
    },
    {
      "id": "GRAPH_MODEL_VERDICT",
      "topic": "recommended_graph_model_verdict",
      "definition": "Compose the recommended learning-science graph model, the go/no-go this KB builds toward. GO: a HIERARCHICAL concept-map backbone (part_of/hierarchy edges) as the spine; an acyclic PREREQUISITE DAG (prereq edges as surmise relations) driving the outer-fringe study frontier and the block-until-mastered gate with a hysteresis band; a SPARSE set of high-value 'related' cross-links for integration and retrieval; Concept nodes at masterable-unit grain; graduating FLAT-SPINE -> full graph only as evidence accrues; all mapped onto the existing Concept/ConceptEdge entities and feeding the color meaning layer and the prereq gate [ESTIMATE — design synthesis over the cited theory]. NO-GO: do NOT build a dense hairball, do NOT let 'related' become a catch-all, do NOT gate on a raw single-point threshold, and do NOT ship a prereq graph without an acyclicity check. An edge earns a place only when it expresses a real, evidence-justified learning relationship.",
      "group": "synthesis",
      "dependencies": ["FLAT_SPINE_GRADUATION", "PREREQ_GATING_MECHANISM", "EDGE_TYPE_SEMANTICS", "KST_ASSESSMENT_VS_CONTENTENG", "HUB_NODES"],
      "competency_question_refs": ["CQ_12"],
      "scope_boundary": { "included": ["the go model (hierarchy backbone + acyclic prereq DAG + sparse cross-links + masterable-grain nodes + staged graduation) mapped onto Concept/ConceptEdge", "the no-go list (no hairball, no catch-all related, no point-threshold gate, no unchecked cycles)"], "excluded": ["the ontology-mechanics and terminology-linking verdicts (neighboring KBs)", "the render/palette verdict (viz)"] },
      "pros": [ { "claim": "The verdict commits to a sparse, hierarchical, acyclic, evidence-earned graph and explicitly refuses the anti-patterns that degrade it", "example": "shipping a spine-first prereq DAG with a few cross-links rather than a fully connected mesh", "weight": 0.86 } ],
      "cons": [ { "claim": "A single recommended model can under-serve a domain whose structure genuinely needs denser or richer typing than three edge types allow", "example": "a heavily causal subdomain forced into prereq/related when a causal type would fit better", "weight": 0.6 } ],
      "failure_modes": ["building the dense hairball or catch-all 'related' the no-go forbids", "gating on a point threshold or shipping an unchecked cycle against the verdict"],
      "acceptance_tests": ["the verdict states the go model (hierarchy backbone + acyclic prereq DAG + sparse cross-links + masterable nodes + staged graduation) on Concept/ConceptEdge and the no-go list, and requires every edge to express a real evidence-justified relationship"],
      "revisit_triggers": ["a domain needs richer edge typing than the enum allows", "an anti-pattern is found shipped", "a neighboring KB's verdict changes the shared boundary"],
      "base": { "criticality": 0.9, "business_value": 0.84, "user_value": 0.84, "technical_complexity": 0.52, "risk_if_wrong": 0.82, "cross_topic_coupling": 0.88, "irreversibility": 0.5, "confidence": 0.68, "node_conflict_pressure": 0.5, "acceptance_test_pass_rate": 0.8, "dependency_gate_pass_rate": 0.8, "prior_importance": 0.9, "evidence_confidence": 0.66, "failure_rate": 0.18, "downside_weight": 0.55, "uncertainty_interval": [0.26, 0.56], "update_signal": "a need for richer edge typing or a shipped anti-pattern changes the verdict" }
    }
  ],
  "edges": [
    { "from": "GRAPH_LEARNING_FRAME", "to": "CONCEPT_MAPPING_NOVAK", "edge_type": "dependency", "why_related": "the meaning-layer frame sets up the qualitative concept-map view of what a node and edge signify", "relation_strength": 0.8 },
    { "from": "CONCEPT_MAPPING_NOVAK", "to": "PROPOSITIONAL_STRUCTURE", "edge_type": "dependency", "why_related": "the proposition is the concept-map unit of meaning the linking phrase carries", "relation_strength": 0.82 },
    { "from": "CONCEPT_MAPPING_NOVAK", "to": "HIERARCHY_CROSSLINKS", "edge_type": "dependency", "why_related": "hierarchy and cross-links are two of the defining concept-map features", "relation_strength": 0.8 },
    { "from": "GRAPH_LEARNING_FRAME", "to": "KST_KNOWLEDGE_STATES", "edge_type": "dependency", "why_related": "the meaning-layer frame sets up the formal KST state view as the quantitative pole", "relation_strength": 0.8 },
    { "from": "KST_KNOWLEDGE_STATES", "to": "KST_LEARNING_SPACE", "edge_type": "dependency", "why_related": "the learning-space axioms are added on top of the knowledge-structure definition", "relation_strength": 0.82 },
    { "from": "KST_KNOWLEDGE_STATES", "to": "SURMISE_PREREQUISITE", "edge_type": "dependency", "why_related": "the surmise relation is what constrains which subsets are admissible states", "relation_strength": 0.82 },
    { "from": "KST_LEARNING_SPACE", "to": "FRINGE_OUTER_INNER", "edge_type": "dependency", "why_related": "the fringes are defined relative to a state in a learning space", "relation_strength": 0.8 },
    { "from": "KST_LEARNING_SPACE", "to": "KST_ASSESSMENT_VS_CONTENTENG", "edge_type": "dependency", "why_related": "the states/fringe layer KST adds is what distinguishes this KB from contenteng's edge table", "relation_strength": 0.74 },
    { "from": "SURMISE_PREREQUISITE", "to": "KST_ASSESSMENT_VS_CONTENTENG", "edge_type": "dependency", "why_related": "the surmise-based gate is one of the additions KST makes over the raw edges", "relation_strength": 0.72 },
    { "from": "SURMISE_PREREQUISITE", "to": "PREREQUISITE_DAG", "edge_type": "dependency", "why_related": "the surmise precedence relation is the structure that must be acyclic", "relation_strength": 0.84 },
    { "from": "HIERARCHY_CROSSLINKS", "to": "GRAPH_SHAPES_FOR_LEARNING", "edge_type": "dependency", "why_related": "the hierarchy/cross-link balance is the basis for which shapes aid learning", "relation_strength": 0.8 },
    { "from": "GRAPH_SHAPES_FOR_LEARNING", "to": "HUB_NODES", "edge_type": "dependency", "why_related": "hub nodes are a specific shape feature analyzed within the shape target", "relation_strength": 0.78 },
    { "from": "HIERARCHY_CROSSLINKS", "to": "HUB_NODES", "edge_type": "dependency", "why_related": "hubs are the high-degree anchors the hierarchy and cross-links concentrate at", "relation_strength": 0.76 },
    { "from": "PROPOSITIONAL_STRUCTURE", "to": "RETRIEVAL_STRUCTURE", "edge_type": "dependency", "why_related": "elaborable propositions are the linking-phrase carriers of retrieval routes", "relation_strength": 0.76 },
    { "from": "GRAPH_SHAPES_FOR_LEARNING", "to": "RETRIEVAL_STRUCTURE", "edge_type": "dependency", "why_related": "the sparse-connectivity shape is what makes retrieval cues rather than interference", "relation_strength": 0.78 },
    { "from": "PREREQUISITE_DAG", "to": "FLAT_SPINE_GRADUATION", "edge_type": "dependency", "why_related": "the acyclic spine is the starting structure the graduation policy grows from", "relation_strength": 0.8 },
    { "from": "GRAPH_SHAPES_FOR_LEARNING", "to": "FLAT_SPINE_GRADUATION", "edge_type": "dependency", "why_related": "the sparsity-first shape target is what the graduation policy operationalizes over time", "relation_strength": 0.78 },
    { "from": "PREREQUISITE_DAG", "to": "PREREQ_GATING_MECHANISM", "edge_type": "dependency", "why_related": "the acyclic prereq DAG is what the gate walks to check prerequisites", "relation_strength": 0.84 },
    { "from": "FRINGE_OUTER_INNER", "to": "PREREQ_GATING_MECHANISM", "edge_type": "dependency", "why_related": "the outer fringe is exactly the unlocked set the gate exposes", "relation_strength": 0.8 },
    { "from": "PREREQ_GATING_MECHANISM", "to": "MASTERY_THRESHOLD_GATE", "edge_type": "dependency", "why_related": "the threshold band is the numeric condition the gating mechanism applies", "relation_strength": 0.8 },
    { "from": "PREREQ_GATING_MECHANISM", "to": "GATING_RISKS", "edge_type": "dependency", "why_related": "the gating risks are the failure space of the gating mechanism", "relation_strength": 0.78 },
    { "from": "PROPOSITIONAL_STRUCTURE", "to": "EDGE_TYPE_SEMANTICS", "edge_type": "dependency", "why_related": "the typed edge is the compressed proposition the type mapping formalizes", "relation_strength": 0.78 },
    { "from": "SURMISE_PREREQUISITE", "to": "EDGE_TYPE_SEMANTICS", "edge_type": "dependency", "why_related": "the prereq type is mapped to the surmise relation", "relation_strength": 0.8 },
    { "from": "PREREQUISITE_DAG", "to": "EDGE_TYPE_SEMANTICS", "edge_type": "dependency", "why_related": "the prereq type is the one that forms the acyclic gating DAG", "relation_strength": 0.76 },
    { "from": "KST_KNOWLEDGE_STATES", "to": "NODE_GRANULARITY", "edge_type": "dependency", "why_related": "a Concept node's grain is set to a KST masterable item", "relation_strength": 0.78 },
    { "from": "CONCEPT_MAPPING_NOVAK", "to": "NODE_GRANULARITY", "edge_type": "dependency", "why_related": "the concept-as-regularity view informs what one node should represent", "relation_strength": 0.74 },
    { "from": "KST_KNOWLEDGE_STATES", "to": "COLOR_MEANING_LAYER", "edge_type": "dependency", "why_related": "the node-state categories the colors express derive from the KST states", "relation_strength": 0.76 },
    { "from": "EDGE_TYPE_SEMANTICS", "to": "COLOR_MEANING_LAYER", "edge_type": "dependency", "why_related": "the edge-type distinctions the colors express come from the type semantics", "relation_strength": 0.76 },
    { "from": "FLAT_SPINE_GRADUATION", "to": "GRAPH_MODEL_VERDICT", "edge_type": "dependency", "why_related": "the verdict adopts the staged flat-spine-to-graph graduation", "relation_strength": 0.8 },
    { "from": "PREREQ_GATING_MECHANISM", "to": "GRAPH_MODEL_VERDICT", "edge_type": "dependency", "why_related": "the verdict includes the block-until-mastered prerequisite gate", "relation_strength": 0.8 },
    { "from": "EDGE_TYPE_SEMANTICS", "to": "GRAPH_MODEL_VERDICT", "edge_type": "dependency", "why_related": "the verdict maps the model onto the typed ConceptEdge semantics", "relation_strength": 0.78 },
    { "from": "KST_ASSESSMENT_VS_CONTENTENG", "to": "GRAPH_MODEL_VERDICT", "edge_type": "dependency", "why_related": "the verdict claims exactly the KST states/fringe/gate layer this node delineates", "relation_strength": 0.74 },
    { "from": "HUB_NODES", "to": "GRAPH_MODEL_VERDICT", "edge_type": "dependency", "why_related": "the verdict allows a few deliberate hubs while guarding against bottlenecks", "relation_strength": 0.72 },
    { "from": "CONCEPT_MAPPING_NOVAK", "to": "KST_KNOWLEDGE_STATES", "edge_type": "conflict", "why_related": "concept mapping is a qualitative labeled-proposition view while KST is a formal set-theoretic state view of the same domain knowledge", "relation_strength": 0.6, "resolution_rule": "use both at distinct layers — the concept-map hierarchy authors the human-meaningful backbone, KST formalizes the states and prerequisites that make it computable — rather than forcing one to serve both roles" },
    { "from": "HUB_NODES", "to": "GRAPH_SHAPES_FOR_LEARNING", "edge_type": "conflict", "why_related": "hubs aid integration and organization while high-degree nodes push toward the dense hairball the shape target warns against", "relation_strength": 0.6, "resolution_rule": "allow a few deliberate anchor hubs but cap their prereq out-degree and keep overall connectivity sparse, so integration is served without a hairball" },
    { "from": "MASTERY_THRESHOLD_GATE", "to": "GATING_RISKS", "edge_type": "conflict", "why_related": "a strict high unlock threshold protects foundations but raises over-gating and frustration risk", "relation_strength": 0.6, "resolution_rule": "use the hysteresis band plus an explicit escape hatch so the threshold protects foundations while the override bounds over-gating" },
    { "from": "GATING_RISKS", "to": "PREREQUISITE_DAG", "edge_type": "constraint", "why_related": "the acyclicity requirement is the precheck that prevents the deadlock failure mode the gating risks describe", "relation_strength": 0.72 },
    { "from": "RETRIEVAL_STRUCTURE", "to": "GRAPH_MODEL_VERDICT", "edge_type": "causal", "why_related": "the retrieval-scaffold benefit of meaningful-but-sparse connectivity causally motivates the verdict's preference for a sparse graph over a dense one", "relation_strength": 0.66, "causal_confidence": 0.6 },
    { "from": "FLAT_SPINE_GRADUATION", "to": "PREREQUISITE_DAG", "edge_type": "feedback", "why_related": "graduating the graph by adding edges feeds new prereq edges back into the DAG that must be re-checked for acyclicity", "relation_strength": 0.66 }
  ],
  "conflict_axes": [
    { "name": "qualitative_map_vs_formal_state_space", "description": "Concept mapping models knowledge as human-readable labeled propositions while KST models it as a formal family of states, and the two do not reduce to each other.", "poles": ["qualitative concept-map propositions (Novak)", "formal set-theoretic knowledge states (KST)"], "resolution_hint": "author the human-meaningful backbone as a concept-map hierarchy and let KST formalize its states and prerequisites for computation", "affected_nodes": ["CONCEPT_MAPPING_NOVAK", "KST_KNOWLEDGE_STATES", "GRAPH_MODEL_VERDICT"] },
    { "name": "sparse_legible_vs_richly_connected", "description": "A sparse hierarchical graph is legible and gives sequencing signal while a richly connected graph captures more relationships but trends toward an unusable hairball.", "poles": ["sparse hierarchy plus few cross-links", "densely connected graph"], "resolution_hint": "keep the graph a tree-plus-few-cross-links, add connectivity only when evidence justifies each edge", "affected_nodes": ["GRAPH_SHAPES_FOR_LEARNING", "HIERARCHY_CROSSLINKS", "FLAT_SPINE_GRADUATION"] },
    { "name": "anchor_hub_vs_bottleneck_hub", "description": "A high-degree hub is a valuable integrative anchor but a prerequisite hub gates a large downstream subgraph and can become a bottleneck.", "poles": ["hub as organizing anchor", "hub as gating bottleneck"], "resolution_hint": "allow a few deliberate anchors but cap prereq out-degree and flag accidental high-degree hubs", "affected_nodes": ["HUB_NODES", "GRAPH_SHAPES_FOR_LEARNING", "PREREQ_GATING_MECHANISM"] },
    { "name": "strict_gating_vs_learner_autonomy", "description": "Strictly blocking new cards until prerequisites are mastered protects foundations but removes learner choice and can frustrate a ready learner.", "poles": ["hard block until mastered", "learner override / soft gate"], "resolution_hint": "gate on a hysteresis band with an explicit deliberate escape hatch, not a hard immovable block", "affected_nodes": ["PREREQ_GATING_MECHANISM", "MASTERY_THRESHOLD_GATE", "GATING_RISKS"] },
    { "name": "hard_surmise_vs_soft_prerequisite", "description": "The quasi-ordinal surmise relation treats prerequisites as hard implications, but real prerequisites are often merely helpful, so strict edges overstate necessity.", "poles": ["hard surmise prerequisite", "soft/helpful prerequisite"], "resolution_hint": "reserve prereq edges for genuine study-order necessity and route merely-helpful links to related", "affected_nodes": ["SURMISE_PREREQUISITE", "EDGE_TYPE_SEMANTICS", "PREREQ_GATING_MECHANISM"] },
    { "name": "point_threshold_vs_hysteresis_band", "description": "Gating on a single mastery threshold is simple but flickers on estimate noise, while a hysteresis band is stable but adds a second parameter.", "poles": ["single-point unlock threshold", "asymmetric hysteresis band"], "resolution_hint": "use the app's 0.5/0.6 band as a hysteresis guard against threshold flicker", "affected_nodes": ["MASTERY_THRESHOLD_GATE", "FRINGE_OUTER_INNER", "GATING_RISKS"] },
    { "name": "coarse_vs_fine_node_granularity", "description": "Coarse Concept nodes make mastery and prerequisites meaningless while overly fine nodes explode the graph and bury structure.", "poles": ["coarse topic-level nodes", "fine fact-level nodes"], "resolution_hint": "target one masterable/testable unit per node, aligned to how cards tag concepts", "affected_nodes": ["NODE_GRANULARITY", "KST_KNOWLEDGE_STATES", "CONCEPT_MAPPING_NOVAK"] },
    { "name": "three_types_vs_relational_richness", "description": "The fixed prereq/part_of/related enum keeps authoring consistent but cannot express every real relationship, tempting overload of 'related'.", "poles": ["fixed three-type enum", "richer relation typing"], "resolution_hint": "keep prereq strict, part_of for composition, and resist dumping every association into related; propose an enum extension only when a relation recurs", "affected_nodes": ["EDGE_TYPE_SEMANTICS", "PROPOSITIONAL_STRUCTURE", "GRAPH_MODEL_VERDICT"] },
    { "name": "build_full_graph_now_vs_graduate", "description": "Building the full cross-linked graph up front captures integration immediately but risks a premature hairball, while graduating delays useful links.", "poles": ["author the dense graph up front", "graduate flat-spine to graph on evidence"], "resolution_hint": "start as a tree/spine and earn each cross-link with co-mastery or authoring evidence", "affected_nodes": ["FLAT_SPINE_GRADUATION", "GRAPH_SHAPES_FOR_LEARNING", "GRAPH_MODEL_VERDICT"] }
  ],
  "edge_cases": [
    { "description": "An LLM-generated prereq graph contains a cycle that permanently locks concepts", "trigger": "generation adds A->B and B->A prereq edges with no acyclicity check", "affected_nodes": ["PREREQUISITE_DAG", "GATING_RISKS"], "mitigation": "run an acyclicity precheck before the graph is used for gating and reject or break any cycle", "severity": "high" },
    { "description": "A borderline prerequisite mastery estimate makes a concept flicker locked/unlocked", "trigger": "a single mastery threshold is used and the estimate wobbles across it", "affected_nodes": ["MASTERY_THRESHOLD_GATE", "FRINGE_OUTER_INNER"], "mitigation": "gate on the 0.5/0.6 hysteresis band so unlock and re-lock use different bounds", "severity": "medium" },
    { "description": "The 'related' edge type becomes a dumping ground and the graph degrades into a hairball", "trigger": "authors or generators use related for every association", "affected_nodes": ["EDGE_TYPE_SEMANTICS", "GRAPH_SHAPES_FOR_LEARNING"], "mitigation": "reserve related for genuinely integrative pairs and cap related-edge density", "severity": "high" },
    { "description": "An accidental prerequisite hub gates a large downstream subgraph", "trigger": "a high-out-degree concept is set as a hard prerequisite of many others", "affected_nodes": ["HUB_NODES", "PREREQ_GATING_MECHANISM"], "mitigation": "watch prereq out-degree, flag accidental hubs, and keep deliberate anchors few", "severity": "high" },
    { "description": "A mislabeled prerequisite blocks a concept the learner is actually ready for", "trigger": "a merely-helpful link is authored as a hard prereq edge", "affected_nodes": ["SURMISE_PREREQUISITE", "GATING_RISKS"], "mitigation": "reserve prereq for genuine surmise necessity and provide an escape hatch to override", "severity": "high" },
    { "description": "A dead-end knowledge state exists with no single-item step forward", "trigger": "an authored graph violates learning-space accessibility", "affected_nodes": ["KST_LEARNING_SPACE", "FRINGE_OUTER_INNER"], "mitigation": "check that every reachable state has a nonempty outer fringe and add a bridging item where it does not", "severity": "medium" },
    { "description": "Concept nodes are authored at inconsistent grain across a deck", "trigger": "one author uses topic-level nodes and another fact-level nodes", "affected_nodes": ["NODE_GRANULARITY", "KST_KNOWLEDGE_STATES"], "mitigation": "hold nodes to one masterable/testable unit aligned to card concept-tags", "severity": "medium" },
    { "description": "The block-until-mastered rule is assumed enforced when it is only diagnosis text", "trigger": "an implementer reads the missing_prereq fix string as working code", "affected_nodes": ["PREREQ_GATING_MECHANISM", "MASTERY_THRESHOLD_GATE"], "mitigation": "treat the app rule as text-only per APP_RECON and specify the enforcing gate explicitly before relying on it", "severity": "high" },
    { "description": "A gate with no override permanently hides content behind a single bad edge", "trigger": "the gate is shipped without an escape hatch", "affected_nodes": ["GATING_RISKS", "PREREQ_GATING_MECHANISM"], "mitigation": "always ship a deliberate learner/author override alongside the gate", "severity": "high" },
    { "description": "The full dense graph is authored up front before any evidence justifies the edges", "trigger": "generation emits every plausible cross-link at once", "affected_nodes": ["FLAT_SPINE_GRADUATION", "GRAPH_SHAPES_FOR_LEARNING"], "mitigation": "start as a tree/spine and add cross-links only as co-mastery or authoring evidence accrues", "severity": "medium" },
    { "description": "The viz layer collapses two distinct node-state categories into one color", "trigger": "the meaning contract leaves a state category ambiguous", "affected_nodes": ["COLOR_MEANING_LAYER", "EDGE_TYPE_SEMANTICS"], "mitigation": "enumerate every node-state and edge-type distinction the colors must keep separable in the meaning contract", "severity": "medium" }
  ],
  "workflow": [
    { "action": "Frame the graph's meaning layer and its boundaries", "node_ref": "GRAPH_LEARNING_FRAME", "description": "State why a graph aids learning and separate the data, meaning and render layers", "artifact": "meaning-layer frame note", "gate": "the sequencing/gating rationale, the layer split, and the contenteng-defers-KST boundary are stated with source tags" },
    { "action": "Lay out the concept-map features and propositional structure", "node_ref": "CONCEPT_MAPPING_NOVAK", "description": "Enumerate Novak & Cañas features and reconcile propositions with the typed ConceptEdge", "artifact": "concept-map feature note", "gate": "the six features are listed with citation and the proposition-vs-typed-edge compression is explained" },
    { "action": "Define knowledge states, learning space and the fringe", "node_ref": "KST_KNOWLEDGE_STATES", "description": "Give the KST structure/state definitions, the learning-space axioms and the outer/inner fringe", "artifact": "KST states-and-fringe note", "gate": "states, the two axioms, and the outer fringe as the unlockable set are defined with the Doignon & Falmagne citation and ESTIMATE tags" },
    { "action": "Formalize prerequisites as surmise and require an acyclic DAG", "node_ref": "SURMISE_PREREQUISITE", "description": "Map prereq edges to the surmise relation and require acyclicity with a topological study order", "artifact": "prerequisite-DAG note", "gate": "the surmise relation is defined, acyclicity is required, and topological order is stated as a valid sequence" },
    { "action": "State which graph shapes aid learning and retrieval", "node_ref": "GRAPH_SHAPES_FOR_LEARNING", "description": "Prescribe sparse-hierarchy-plus-few-cross-links, name the hairball anti-pattern, and tie structure to retrieval", "artifact": "graph-shape and retrieval note", "gate": "the sparse-hierarchy target, the hairball anti-pattern, and the retrieval-scaffold argument are stated with ESTIMATE tags" },
    { "action": "Analyze hub nodes and their bottleneck risk", "node_ref": "HUB_NODES", "description": "Characterize hubs as anchors and guard against accidental prerequisite bottlenecks", "artifact": "hub-node policy", "gate": "hubs are defined as high-degree anchors, the downstream-gating risk is stated, and the deliberate-few-hubs rule is given" },
    { "action": "Specify the prerequisite gate and its threshold band", "node_ref": "PREREQ_GATING_MECHANISM", "description": "Define block-until-all-prereqs-clear off the outer fringe and the 0.5/0.6 hysteresis band", "artifact": "prerequisite-gate spec", "gate": "the unlock-when-all-prereqs-clear logic, the outer-fringe equivalence, and the hysteresis band are stated with the APP_RECON source" },
    { "action": "Bound the gating risks and mitigations", "node_ref": "GATING_RISKS", "description": "Name over/under-gating harms and prescribe the escape hatch, acyclicity precheck, hysteresis and chain-depth cap", "artifact": "gating-risk note", "gate": "both failure directions and all four mitigations are stated, including the mandatory override" },
    { "action": "Map edge types and node granularity onto the app entities", "node_ref": "EDGE_TYPE_SEMANTICS", "description": "Map prereq/part_of/related to relations and set the Concept node to a masterable unit", "artifact": "entity-mapping note", "gate": "each edge type is mapped to its relation (enum source-tagged) and the masterable-unit node grain is set" },
    { "action": "Define the color meaning contract and issue the graph-model verdict", "node_ref": "GRAPH_MODEL_VERDICT", "description": "Specify the node-state/edge-type meaning the colors must express and commit to the go/no-go graph model", "artifact": "meaning contract + graph-model verdict", "gate": "the color meaning contract and the go model (hierarchy backbone + acyclic prereq DAG + sparse cross-links + masterable nodes + staged graduation) plus the no-go list are stated on Concept/ConceptEdge" }
  ],
  "dominance_rules": [
    { "rule": "Acyclicity of the prerequisite DAG dominates any convenience of adding an edge", "rationale": "a cycle permanently locks concepts and breaks the learning-space accessibility guarantee", "trigger": "a prereq edge is about to be added or a graph is about to gate", "action": "run an acyclicity precheck and reject or break any cycle before using the graph" },
    { "rule": "A masterable-unit node grain dominates chapter- or fact-level granularity", "rationale": "prerequisites, mastery and gating are only meaningful when a node is a single testable unit", "trigger": "a Concept node is authored", "action": "decompose to one masterable/testable unit aligned to card concept-tags" },
    { "rule": "Sparse hierarchy dominates dense connectivity when they conflict", "rationale": "a hairball offers no sequencing signal and makes gates block almost everything", "trigger": "connectivity is about to grow past a tree-plus-few-cross-links", "action": "keep the graph sparse and add each cross-link only on evidence" },
    { "rule": "A deliberate escape hatch dominates a hard immovable block", "rationale": "a gate with no override lets one bad edge silently remove content", "trigger": "a prerequisite gate is shipped", "action": "always ship a deliberate learner/author override with the gate" },
    { "rule": "The hysteresis band dominates a single-point unlock threshold", "rationale": "a point threshold flickers on mastery-estimate noise", "trigger": "the gate evaluates a borderline prerequisite mastery", "action": "unlock at the higher bound and only re-lock below the lower bound (0.6/0.5)" },
    { "rule": "Strict prereq typing dominates convenience use of prereq for helpful links", "rationale": "prereq is the gating type, so a merely-helpful link marked prereq over-blocks", "trigger": "a relationship is about to be typed prereq", "action": "use prereq only for genuine surmise necessity and route helpful links to related" },
    { "rule": "The KST states/fringe/gate layer dominates re-implementing contenteng's edge authoring", "rationale": "contenteng owns edge authoring and defers KST; this KB adds the states/fringe/gate", "trigger": "this KB's scope is about to overlap edge authoring", "action": "claim only the KST states/fringe/gate layer and leave edge authoring to contenteng" },
    { "rule": "The meaning contract dominates render choices in the color layer", "rationale": "mixing palette/layout decisions into the meaning layer duplicates the viz specialist", "trigger": "the color layer is being specified", "action": "define only the node-state/edge-type distinctions the colors must express and defer palette/layout to viz" },
    { "rule": "Text-only app rules are treated as unimplemented until code exists", "rationale": "the block-until-mastered rule is diagnosis text with no enforcing code per APP_RECON", "trigger": "an app behavior is cited as working", "action": "verify enforcement against APP_RECON and specify the real gate before relying on the behavior" }
  ],
  "anti_rework_rules": [
    { "rule": "Run the acyclicity precheck before any prerequisite gating is wired", "prevents": "a shipped cyclic graph that permanently locks concepts and must be untangled after the gate is live" },
    { "rule": "Fix the masterable-unit node grain before authoring nodes at scale", "prevents": "re-decomposing a whole deck when coarse or fine nodes make mastery and prerequisites meaningless" },
    { "rule": "Set the prereq/part_of/related type discipline before edges are authored", "prevents": "a catch-all 'related' hairball that has to be re-typed edge by edge later" },
    { "rule": "Start as a flat spine and graduate on evidence rather than building the dense graph up front", "prevents": "authoring a premature hairball whose unjustified edges must be pruned back" },
    { "rule": "Specify the escape hatch when the gate is first designed, not after complaints", "prevents": "retrofitting an override into a live gate that has been silently hiding content" },
    { "rule": "Adopt the hysteresis band from the outset instead of a single threshold", "prevents": "reworking a flickering point-threshold gate after learners see concepts toggle" },
    { "rule": "Verify the block-until-mastered rule's enforcement against APP_RECON before building on it", "prevents": "building features atop a rule that is only diagnosis text and does not actually gate" },
    { "rule": "Define the color meaning contract before viz picks a palette", "prevents": "viz encoding the wrong or missing state categories and needing a re-color pass" }
  ],
  "iteration_protocol": [
    { "trigger": "an acyclicity check or a live gate reveals a cycle in the prereq DAG", "action": "break the cycle, re-run the check, and audit the affected concepts' unlockability", "nodes": ["PREREQUISITE_DAG", "GATING_RISKS"], "priority": 1 },
    { "trigger": "concepts are observed flickering locked/unlocked", "action": "widen or confirm the hysteresis band and re-check the fringe computation", "nodes": ["MASTERY_THRESHOLD_GATE", "FRINGE_OUTER_INNER"], "priority": 1 },
    { "trigger": "the 'related' edge set trends toward a hairball", "action": "re-audit related edges against the integrative-value bar and prune non-justified ones", "nodes": ["EDGE_TYPE_SEMANTICS", "GRAPH_SHAPES_FOR_LEARNING"], "priority": 2 },
    { "trigger": "an accidental prerequisite hub is found gating a large subgraph", "action": "review the hub's prereq out-degree and reroute or soften over-strict edges", "nodes": ["HUB_NODES", "PREREQ_GATING_MECHANISM"], "priority": 2 },
    { "trigger": "co-mastery or authoring evidence justifies new integrative links", "action": "graduate the flat spine by adding the justified cross-links and re-checking acyclicity", "nodes": ["FLAT_SPINE_GRADUATION", "PREREQUISITE_DAG"], "priority": 2 },
    { "trigger": "an ESTIMATE-tagged learning-science claim (learning-space axioms, surmise/Birkhoff, ALEKS, Ausubel) is source-verified or refuted", "action": "update the affected node's tags and any recommendation resting on it", "nodes": ["KST_LEARNING_SPACE", "SURMISE_PREREQUISITE", "KST_ASSESSMENT_VS_CONTENTENG"], "priority": 2 },
    { "trigger": "the viz layer reports a node-state or edge-type category it cannot render distinctly", "action": "revise the color meaning contract to make the required distinctions explicit", "nodes": ["COLOR_MEANING_LAYER", "EDGE_TYPE_SEMANTICS"], "priority": 3 }
  ],
  "priority_rationale": "The meaning-layer frame comes first because it decides which layer (meaning vs data vs render) every downstream decision belongs to; the concept-map features and the KST states/learning-space/fringe come next as the two theoretical poles that give nodes and edges their instructional meaning; the surmise relation and its acyclic prerequisite DAG then formalize the study-order structure; the graph-shape, hub, and retrieval nodes prescribe which shapes actually help; the gating mechanism, threshold band and gating risks operationalize block-until-mastered; and the edge-type semantics, node granularity, KST-vs-contenteng boundary, color meaning contract and final verdict come last because they compose every upstream idea into a concrete mapping onto the app's Concept/ConceptEdge entities with an explicit no-go on hairballs, catch-all edges, point-threshold gates and unchecked cycles.",
  "eval_objective": "verify_core_properties_of_study_system_kgraph_learning_science_graphs_kb"
}
