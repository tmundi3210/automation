{
  "domain": "director_production_ops",
  "domain_label": "Movie Studio Director — Production Operations: Asset Registry, Provenance, Checklist and Owner Escalation",
  "purpose": "A method for the operational backbone of an AI reel studio: naming and versioning every asset (prompts, keyframes, clips, Souls, Reference Elements), designing the persona-asset registry from a workspace that is currently empty, recording per-clip provenance (which model, params, prompt version produced each kept clip), running the per-reel checklist from brief to posted, and escalating to the owner on credit-spend thresholds, likeness/policy flags, identity drift beyond tolerance, or any real-person identifiability — every platform capability sourced from the live Higgsfield MCP surface enumeration of 2026-07-03, never from memory.",
  "assumptions": [
    "no production reels have been generated yet; all scores are heuristic priors and every platform-capability claim (model ids, Soul/Element mechanics, credit balance 1,010 on plan 'plus', the get_cost:true zero-spend preflight, count 1-4 per call, media value = media_id or prior job_id never URL) is quoted from the live Higgsfield MCP surface enumeration of 2026-07-03, not re-derived or remembered",
    "the workspace persona surfaces are empty as of the enumeration — show_characters and show_reference_elements both returned zero items — so the persona-asset registry is designed from zero, not migrated",
    "per-generation credit costs, Soul-training credit cost, and rate limits are all [UNKNOWN] on the read surface; the surface's own sanctioned probe is get_cost:true on a generate_* call, so every cost figure in this KB is a knob to preflight, never a stated price",
    "escalation thresholds (per-call, per-reel, balance-floor) are policy proposals tagged [ESTIMATE] for the owner to ratify, because with costs [UNKNOWN] no threshold can be grounded in a platform fact yet",
    "the likeness lane is closed by policy: any real-person identifiability halts work and escalates to the owner; this KB carries no technique for evoking a specific real person"
  ],
  "exclusions": [
    "shot design, transition choice, cinematography and prompt craft — the sibling shot-pipeline KB owns brief-to-shot-list creative planning; this KB owns the bookkeeping, registry and governance around it",
    "the visual QC rubric's artifact taxonomy content (text/hands/contact-physics tells) — the realism research owns what to look for; this KB owns where the checkpoints sit and what gets recorded and escalated",
    "editing, grading and cover craft detail — owned by the editing/grading KB; this KB records only the stage gates and artifacts those stages must produce",
    "posting cadence, analytics interpretation and audience strategy beyond the release-packaging gate",
    "any guidance for evoking a specific real person while hiding or altering the face — the likeness lane stays closed; identifiability triggers escalation, never a workaround",
    "inventing credit prices, rate limits, engagement statistics or platform policies the grounding research did not carry"
  ],
  "competency_questions": [
    { "id": "CQ_01", "question": "What id scheme names every production asset, and how do local ids relate to platform ids?",
      "must_be_answerable_from": ["ASSET_NAMING_SCHEME", "PLATFORM_HANDLE_REGISTRY"], "acceptance_condition": "states a deterministic local id embedding reel/shot/role/version for prompts, keyframes, clips, Souls and Elements, and states that platform handles (media_id, job_id, soul_id, element_id) are foreign keys stored alongside — never renamed, never used as the local key", "covered_by": ["ASSET_NAMING_SCHEME", "PLATFORM_HANDLE_REGISTRY"] },
    { "id": "CQ_02", "question": "How are prompts versioned, and how does a generation reference the exact prompt text that produced it?",
      "must_be_answerable_from": ["PROMPT_VERSION_CONTROL"], "acceptance_condition": "states prompts are immutable versioned artifacts (edit = new version id, never in-place mutation) and that every generation call logs the exact prompt version id it used, including embedded element tokens", "covered_by": ["PROMPT_VERSION_CONTROL"] },
    { "id": "CQ_03", "question": "What does the persona-asset registry hold, and why must it be designed from zero before any generation?",
      "must_be_answerable_from": ["PERSONA_REGISTRY_BOOTSTRAP"], "acceptance_condition": "states show_characters and show_reference_elements returned empty lists on 2026-07-03, and enumerates the planned registry rows: one Soul per persona, one Element per persona, wardrobe modules as prop/character Elements, locations as environment Elements, each with reference-image lineage", "covered_by": ["PERSONA_REGISTRY_BOOTSTRAP"] },
    { "id": "CQ_04", "question": "What is the Soul asset lifecycle and which constraints bind its use?",
      "must_be_answerable_from": ["SOUL_ASSET_LIFECYCLE"], "acceptance_condition": "states train = name + 5-20 reference images, ~10 min, non-blocking; usable ONLY with soul_2 and soul_cinematic; ONE soul_id per generation; training credit cost [UNKNOWN]; and the registry row fields a Soul must carry", "covered_by": ["SOUL_ASSET_LIFECYCLE"] },
    { "id": "CQ_05", "question": "What is the Reference Element lifecycle, and how do wardrobe modules and location refs ride on it?",
      "must_be_answerable_from": ["ELEMENT_ASSET_LIFECYCLE"], "acceptance_condition": "states Elements are instant (no training), category character|environment|prop, embedded as <<<element_id>>> tokens with multiple per prompt allowed, NOT usable with Soul models, with the element-compatible video-model list partly [UNKNOWN]; and maps wardrobe to prop/character Elements and locations to environment Elements", "covered_by": ["ELEMENT_ASSET_LIFECYCLE"] },
    { "id": "CQ_06", "question": "What provenance fields must every kept clip carry before it may enter a timeline?",
      "must_be_answerable_from": ["SHOT_PROVENANCE_RECORD"], "acceptance_condition": "enumerates at minimum: exact model id string, full params (resolution, duration, aspect, genre/speedramp/cfg_scale/mode where the model exposes them), prompt version id, soul_id or element ids used, start/end image handles, job_id, QC scores per checkpoint, and actual credit cost", "covered_by": ["SHOT_PROVENANCE_RECORD"] },
    { "id": "CQ_07", "question": "How are keyframe chains across clip seams recorded so a re-roll cannot silently break a stitched reel?",
      "must_be_answerable_from": ["KEYFRAME_CHAIN_PROVENANCE"], "acceptance_condition": "states clip N's end_image job_id may feed clip N+1's start_image (media value accepts a prior job_id), that each seam row records transition type plus both frame handles, and that re-rolling an upstream clip marks every downstream chained clip stale", "covered_by": ["KEYFRAME_CHAIN_PROVENANCE"] },
    { "id": "CQ_08", "question": "How is credit spend tracked, preflighted and budgeted when per-generation prices are unknown?",
      "must_be_answerable_from": ["CREDIT_SPEND_ACCOUNTING"], "acceptance_condition": "states balance 1,010 on plan 'plus' at enumeration, that per-model cost is [UNKNOWN] and get_cost:true is the sanctioned zero-spend preflight, that transactions/balance surfaces reconcile actuals, and that every reel gets a spend cap before its first call", "covered_by": ["CREDIT_SPEND_ACCOUNTING"] },
    { "id": "CQ_09", "question": "What is the per-reel checklist from brief to posted, and which gate ends each stage?",
      "must_be_answerable_from": ["REEL_BRIEF_INTAKE", "RELEASE_PACKAGING"], "acceptance_condition": "orders the stages brief -> budget preflight -> keyframe stills -> still QC -> video generation -> video QC -> assembly/grade -> phone-screen QC -> cover -> post, with the stage artifact and gate named for each, and no stage starting before its predecessor's gate passes", "covered_by": ["REEL_BRIEF_INTAKE", "RELEASE_PACKAGING"] },
    { "id": "CQ_10", "question": "Where do the QC checkpoints sit and what re-roll rules do they enforce?",
      "must_be_answerable_from": ["QC_GATE_CHECKPOINTS"], "acceptance_condition": "names the three checkpoints (start-frame image, raw video, post-edit reel at phone resolution), the rubric gate rules (any single item scored 2 = re-roll; total >= 4 = re-roll), and the budget rule QC-before-upscale/post", "covered_by": ["QC_GATE_CHECKPOINTS"] },
    { "id": "CQ_11", "question": "What triggers an owner escalation, and which trigger dominates when several fire?",
      "must_be_answerable_from": ["ESCALATION_MATRIX", "LIKENESS_POLICY_GATE"], "acceptance_condition": "lists the four trigger families (credit-spend threshold, likeness/policy flag, identity drift beyond tolerance, any real-person identifiability), states likeness/identifiability outranks all others and halts work rather than queuing, and states escalations carry the ledger evidence", "covered_by": ["ESCALATION_MATRIX", "LIKENESS_POLICY_GATE"] },
    { "id": "CQ_12", "question": "How is identity drift detected and tolerated, and what happens beyond tolerance?",
      "must_be_answerable_from": ["IDENTITY_DRIFT_TOLERANCE"], "acceptance_condition": "states drift is checked against the persona reference sheet at every QC checkpoint (identity-lock rubric item), that the quantitative Souls-vs-Elements fidelity gap is [UNKNOWN] until A/B runs, and that beyond-tolerance drift quarantines the lineage and escalates with a retrain/re-create decision", "covered_by": ["IDENTITY_DRIFT_TOLERANCE"] },
    { "id": "CQ_13", "question": "What does the per-model failure ledger record and how does it drive model choice and grading?",
      "must_be_answerable_from": ["MODEL_FAILURE_LEDGER"], "acceptance_condition": "states the ledger keys on exact model id and shot type, records failure classes, keep-rate and the per-model color trim preset, and that repeat shot types default to the ledger winner while A/B exploration is reserved for new shot types", "covered_by": ["MODEL_FAILURE_LEDGER"] },
    { "id": "CQ_14", "question": "What is the source of truth — the local production ledger or the platform's own surfaces?",
      "must_be_answerable_from": ["PRODUCTION_LEDGER", "PLATFORM_HANDLE_REGISTRY"], "acceptance_condition": "states the local ledger is canonical because it carries fields the platform does not (prompt versions, QC scores, seam plans), while show_generations/show_medias/job_display/transactions are reconciliation surfaces whose retention and query semantics are [UNKNOWN] (names only were enumerated)", "covered_by": ["PRODUCTION_LEDGER", "PLATFORM_HANDLE_REGISTRY"] }
  ],
  "glossary": [
    { "term": "production ledger", "definition": "The studio's canonical append-only record: one row per asset (prompt version, keyframe, clip, Soul, Element, reel) written at creation time, carrying local id, platform handles, provenance fields, QC scores and credit cost. Canonical because the platform surfaces (show_generations, show_medias, job_display, transactions) were enumerated by name only — their retention and query semantics are [UNKNOWN] — and none of them can carry prompt versions or QC verdicts." },
    { "term": "soul_id", "definition": "Handle of a trained Higgsfield Soul character (train = name + 5-20 reference images, ~10 min, non-blocking); usable ONLY with the soul_2 and soul_cinematic image models, ONE soul_id per generation — a two-persona shot cannot carry two Souls [FACT — live tool schemas 2026-07-03]. Training credit cost [UNKNOWN]." },
    { "term": "element token", "definition": "A <<<element_id>>> marker embedded in a generate_image/generate_video prompt referencing a Reference Element (instant, no training; category character|environment|prop); multiple tokens per prompt are allowed, making Elements the surface-designated path for two-persona shots; Elements are NOT usable with Soul models [FACT — live tool descriptions 2026-07-03]." },
    { "term": "platform handle", "definition": "Any id minted by Higgsfield rather than the studio: media_id (from media_confirm), job_id (from a generation), soul_id, element_id. Stored as foreign keys on ledger rows; generate_* media value accepts a media_id or a prior job_id, never a URL [FACT — generate_* schemas]." },
    { "term": "get_cost preflight", "definition": "Passing get_cost:true on a generate_* call returns the credit cost WITHOUT submitting the job — the surface's sanctioned zero-spend price probe [FACT — generate_* schemas]. Mandatory before any untested model x resolution x duration cell because per-generation prices are [UNKNOWN] on the read surface." },
    { "term": "prompt version", "definition": "An immutable, id-carrying snapshot of exact prompt text (including embedded element tokens). Any edit mints a new version id; generations reference the version id, so every kept clip's wording is reconstructable and A/B results stay attributable." },
    { "term": "keyframe chain", "definition": "The provenance linkage of a stitched reel: clip N's end_image handle feeds clip N+1's start_image (structurally supported because media value accepts a prior generation's job_id [FACT — generate_video schema]); each seam row records the transition type plus both frame handles, so a re-roll upstream marks downstream clips stale instead of silently breaking the stitch." },
    { "term": "identity drift", "definition": "A persona's face geometry, eye color, hair or wardrobe changing across frames or shots — an industry-named AI-video failure mode [FACT — via search excerpts in the realism research]. Checked as the identity-lock rubric item against the persona reference sheet at every QC checkpoint; the quantitative fidelity gap between Souls and Elements is [UNKNOWN] until A/B generation runs." },
    { "term": "likeness closed lane", "definition": "The standing policy that any real-person identifiability — a generated face resembling an identifiable real individual, or a request to evoke one — halts the affected work and escalates to the owner. No technique for approximating a specific real person while obscuring the face is recorded anywhere in this system; the lane is closed, not narrow." },
    { "term": "model failure ledger", "definition": "A per-model, per-shot-type record of failure classes, keep-rate, and the learned color trim preset (each model batch arrives with its own color signature, so a per-model trim nudge toward the house target is maintained before hero-clip matching — process design from the editing research, actual signatures [UNKNOWN until generation runs])." },
    { "term": "escalation packet", "definition": "What an owner escalation must contain: the trigger, the ledger rows evidencing it (asset ids, provenance, spend), the halted-or-continuing state of affected work, and the specific decision requested — so the owner rules on evidence, not on a summary." }
  ],
  "nodes": [
    {
      "id": "ASSET_NAMING_SCHEME",
      "topic": "deterministic_local_ids_for_all_production_assets",
      "definition": "Fix the naming and versioning convention before anything is generated, because it keys every other record. Local ids are deterministic and human-parseable, embedding reel, shot/seam, asset role and version — e.g. a still keyframe for reel 003, shot 02, end-frame role, version 4 is one string that sorts and greps — covering all five asset classes: prompts, keyframes (start/end stills), clips, Souls and Reference Elements, plus reels themselves. Versions are monotone integers minted on any change; nothing is edited in place. Platform-minted handles (media_id, job_id, soul_id, element_id) are never used as local keys and never renamed — they are foreign keys carried on the row (their mechanics are the platform-handle node). The scheme is designed from zero honestly: no naming convention exists in the workspace yet, so this node is a design commitment [ESTIMATE — standard asset-management practice applied to this pipeline; the asset classes themselves are FACT from the live surface].",
      "group": "foundations",
      "dependencies": [],
      "competency_question_refs": ["CQ_01"],
      "scope_boundary": { "included": ["the local id grammar (reel/shot/role/version) for prompts, keyframes, clips, Souls, Elements, reels", "monotone versioning with no in-place edits", "the rule that platform handles are foreign keys, never local keys"], "excluded": ["the ledger row schema built on these ids (ledger node)", "platform handle semantics (platform-handle node)"] },
      "pros": [ { "claim": "A deterministic id grammar makes every asset findable and every provenance row joinable without a lookup service", "example": "recovering all four versions of reel 003 shot 02's end keyframe by prefix match alone when diagnosing a seam mismatch", "weight": 0.82 } ],
      "cons": [ { "claim": "A grammar fixed too early can under-represent later asset classes and force awkward retrofits", "example": "adding wardrobe-module Elements after the grammar only encoded persona/location roles, forcing an escaped role field", "weight": 0.55 } ],
      "failure_modes": ["naming assets after platform job ids so a platform-side change orphans the archive", "editing an asset in place so two generations silently reference different content under one id"],
      "acceptance_tests": ["the scheme names all five asset classes plus reels, embeds reel/shot/role/version, mints a new version on any change, and stores platform handles only as foreign-key fields"],
      "revisit_triggers": ["a new asset class appears (e.g. voice assets, cover templates) that the grammar cannot express", "two assets collide under one id", "a platform handle format change breaks a foreign-key field"],
      "base": { "criticality": 0.88, "business_value": 0.78, "user_value": 0.72, "technical_complexity": 0.35, "risk_if_wrong": 0.72, "cross_topic_coupling": 0.88, "irreversibility": 0.62, "confidence": 0.8, "node_conflict_pressure": 0.3, "acceptance_test_pass_rate": 0.86, "dependency_gate_pass_rate": 0.88, "prior_importance": 0.88, "evidence_confidence": 0.72, "failure_rate": 0.12, "downside_weight": 0.55, "uncertainty_interval": [0.14, 0.36], "update_signal": "a new asset class, an id collision, or a platform handle format change forces a grammar revision" }
    },
    {
      "id": "PRODUCTION_LEDGER",
      "topic": "local_append_only_ledger_as_source_of_truth",
      "definition": "Commit to a local, append-only production ledger as the canonical record, written at creation time for every asset. The platform exposes its own history surfaces — show_generations, show_medias, job_display, transactions — but these were enumerated by NAME ONLY on 2026-07-03; their retention windows, filters and query semantics are [UNKNOWN — the surfaces were listed but not exercised this session], and no platform surface can carry the studio-side fields that matter most: prompt version ids, QC verdicts per checkpoint, seam/transition plans, and escalation states. So the ledger is canonical and the platform surfaces are reconciliation inputs: job_display confirms a job's terminal state, transactions confirms actual spend, show_medias confirms a handle still resolves. One row per asset, keyed by the local id, carrying the platform foreign keys. Rows are never deleted; kept/killed/stale/quarantined are states, not deletions, so failed generations remain evidence for the model failure ledger.",
      "group": "foundations",
      "dependencies": ["ASSET_NAMING_SCHEME"],
      "competency_question_refs": ["CQ_14"],
      "scope_boundary": { "included": ["ledger-as-canonical with platform surfaces as reconciliation inputs", "write-at-creation-time discipline", "row states (kept/killed/stale/quarantined) instead of deletion"], "excluded": ["the specific provenance fields per clip (provenance node)", "credit reconciliation mechanics (credit node)"] },
      "pros": [ { "claim": "A write-at-creation ledger preserves fields no platform surface holds, keeping every kept clip reconstructable", "example": "answering 'which prompt version and model produced this clip' months later from the ledger row when show_generations' retention is unknown", "weight": 0.84 } ],
      "cons": [ { "claim": "Dual bookkeeping (local plus platform) costs discipline and drifts if reconciliation is skipped", "example": "a ledger row marked kept while job_display shows the job actually failed, poisoning downstream counts", "weight": 0.6 } ],
      "failure_modes": ["relying on show_generations as the archive and losing prompt-version linkage it never had", "backfilling rows from memory days later instead of writing them at creation time"],
      "acceptance_tests": ["the ledger design states write-at-creation, one row per asset keyed by local id with platform foreign keys, non-deleting states, and names the four platform surfaces as reconciliation-only with their semantics tagged [UNKNOWN]"],
      "revisit_triggers": ["a platform surface's retention or query semantics get exercised and documented, changing the reconciliation design", "a ledger/platform mismatch is found in an audit", "the ledger grows past what flat files handle and needs a storage upgrade"],
      "base": { "criticality": 0.9, "business_value": 0.82, "user_value": 0.74, "technical_complexity": 0.45, "risk_if_wrong": 0.78, "cross_topic_coupling": 0.86, "irreversibility": 0.55, "confidence": 0.78, "node_conflict_pressure": 0.38, "acceptance_test_pass_rate": 0.84, "dependency_gate_pass_rate": 0.86, "prior_importance": 0.9, "evidence_confidence": 0.74, "failure_rate": 0.15, "downside_weight": 0.6, "uncertainty_interval": [0.16, 0.38], "update_signal": "exercising the platform history surfaces resolves their [UNKNOWN] semantics and may shrink or reshape the local schema" }
    },
    {
      "id": "PROMPT_VERSION_CONTROL",
      "topic": "immutable_versioned_prompts_referenced_by_generations",
      "definition": "Treat every prompt as an immutable versioned artifact. A prompt row carries: version id (per the naming grammar), exact text including embedded <<<element_id>>> tokens, the shot/seam it serves, and its lineage (which version it derives from and why — a QC failure class, a transition change, a model swap). Any edit mints a new version; the old version stays because kept clips reference it. Every generation call records the prompt version id it used, which is what makes A/B results attributable: when two batches differ, the ledger can prove whether the prompt, the model, or the params moved. This discipline is a design commitment [ESTIMATE — version-control practice applied to prompt assets], but the payload it protects is factual: prompts carry the element tokens and craft vocabulary that the platform surfaces do not archive with the job.",
      "group": "foundations",
      "dependencies": ["ASSET_NAMING_SCHEME"],
      "competency_question_refs": ["CQ_02"],
      "scope_boundary": { "included": ["immutability and version minting on any edit", "the prompt row fields (text, tokens, shot served, lineage, derivation reason)", "generations referencing the exact version id"], "excluded": ["prompt craft itself (shot-pipeline KB)", "which models accept element tokens (element node)"] },
      "pros": [ { "claim": "Version-referenced prompts make every A/B result attributable and every kept clip's wording reconstructable", "example": "proving a keep-rate jump came from prompt v7's motivated-lighting line and not from the model change shipped the same day", "weight": 0.8 } ],
      "cons": [ { "claim": "Version ceremony slows rapid-fire iteration when a director wants to nudge one adjective per roll", "example": "ten micro-versions minted in an hour of whip-pan seam tuning, cluttering the lineage graph", "weight": 0.55 } ],
      "failure_modes": ["mutating a prompt in place so two generations under one id used different text", "logging only the prompt 'name' without the version so A/B attribution collapses"],
      "acceptance_tests": ["the design states edit-mints-new-version, the prompt row fields including element tokens and derivation reason, and that every generation call logs the exact version id"],
      "revisit_triggers": ["a QC-driven prompt revision loop starts producing versions faster than the lineage stays readable", "an unattributable batch difference is traced to an untracked prompt tweak", "a new prompt-bearing surface (e.g. preset_id routing) needs version linkage"],
      "base": { "criticality": 0.84, "business_value": 0.76, "user_value": 0.7, "technical_complexity": 0.35, "risk_if_wrong": 0.7, "cross_topic_coupling": 0.8, "irreversibility": 0.4, "confidence": 0.8, "node_conflict_pressure": 0.42, "acceptance_test_pass_rate": 0.86, "dependency_gate_pass_rate": 0.86, "prior_importance": 0.82, "evidence_confidence": 0.72, "failure_rate": 0.14, "downside_weight": 0.5, "uncertainty_interval": [0.14, 0.36], "update_signal": "an attribution failure or a new prompt-bearing surface changes what a prompt row must carry" }
    },
    {
      "id": "PLATFORM_HANDLE_REGISTRY",
      "topic": "media_id_job_id_soul_id_element_id_foreign_keys",
      "definition": "Model the platform's handle economy precisely, because every ledger row joins on it. Facts from the live surface [FACT — generate_* schemas and tool descriptions, 2026-07-03]: media inputs pass a `value` that is a media_id (minted by media_confirm after upload) or a PRIOR GENERATION'S job_id — never a URL; count is 1-4 per generate_image/generate_video call (audio fixed at 1), so one call can mint up to four job outputs that need rows; soul_id and element_id are minted by their creation surfaces and referenced by parameter (soul_id) or in-prompt token (element). The registry therefore maps local id <-> platform handle bidirectionally, records which handle KIND each row holds, and records handle provenance (uploaded vs generated). Unresolved surface facts carried honestly: rate limits and concurrency caps are [UNKNOWN — nothing on any read surface states them], and duplicate catalog ids exist (soul_2 and soul_v2 are the same model under two ids [FACT]), so the registry canonicalizes model references too.",
      "group": "foundations",
      "dependencies": ["PRODUCTION_LEDGER"],
      "competency_question_refs": ["CQ_01", "CQ_14"],
      "scope_boundary": { "included": ["media_id/job_id/soul_id/element_id semantics and the never-URL rule", "count 1-4 multi-output bookkeeping", "bidirectional local<->platform mapping with handle kind and provenance"], "excluded": ["Soul/Element lifecycle rules (their own nodes)", "the naming grammar of local ids (naming node)"] },
      "pros": [ { "claim": "Precise handle bookkeeping lets chained generations and re-uploads be traced without guessing", "example": "tracing a video clip back through its start_image job_id to the still generation and its prompt version in two joins", "weight": 0.82 } ],
      "cons": [ { "claim": "The handle economy has unverified corners (rate limits, retention) that the registry cannot paper over", "example": "a job_id referenced weeks later failing to resolve because retention semantics were never documented, stranding a planned re-render", "weight": 0.58 } ],
      "failure_modes": ["storing URLs where the schema demands media_id/job_id and burning a failed call", "logging one row for a count:4 call and losing three outputs' lineage"],
      "acceptance_tests": ["the registry states value = media_id or prior job_id never URL, one row per output on multi-count calls, bidirectional mapping with handle kind, and carries rate-limit/retention semantics as [UNKNOWN]"],
      "revisit_triggers": ["a handle fails to resolve, revealing retention behavior", "rate limits or concurrency caps surface during run-phase batches", "a new handle kind appears (e.g. voice ids) needing registry support"],
      "base": { "criticality": 0.86, "business_value": 0.76, "user_value": 0.7, "technical_complexity": 0.5, "risk_if_wrong": 0.76, "cross_topic_coupling": 0.85, "irreversibility": 0.45, "confidence": 0.8, "node_conflict_pressure": 0.32, "acceptance_test_pass_rate": 0.84, "dependency_gate_pass_rate": 0.84, "prior_importance": 0.86, "evidence_confidence": 0.8, "failure_rate": 0.14, "downside_weight": 0.55, "uncertainty_interval": [0.14, 0.36], "update_signal": "run-phase resolves handle retention/rate-limit unknowns or a schema change alters the value rules" }
    },
    {
      "id": "PERSONA_REGISTRY_BOOTSTRAP",
      "topic": "designing_the_persona_asset_registry_from_an_empty_workspace",
      "definition": "Design the persona-asset registry from zero, because the workspace is verifiably empty: show_characters and show_reference_elements both returned empty item lists on 2026-07-03 [FACT — live MCP surface]. The bootstrap plan, grounded in the surface's two persistence mechanisms: (1) one trained Soul per persona (male, female) for solo hero stills on the Soul-only models; (2) one Reference Element per persona so two-persona shots and non-Soul models can hold identity; (3) wardrobe modules as prop/character Elements and location refs as environment Elements (the category enum is character|environment|prop [FACT]) — this Soul-plus-Element dual mapping is the surface-constraint-driven program [ESTIMATE — medium-high confidence, from the one-Soul-per-generation and Elements-not-with-Soul-models constraints]. Each registry row is authored BEFORE the asset exists: intended name, category, reference-image lineage (which uploads/generations will seed it), target models, and creation status — so creation calls execute a plan instead of improvising, and the first likeness review happens on the reference sheet before any credits are spent.",
      "group": "registry",
      "dependencies": ["ASSET_NAMING_SCHEME"],
      "competency_question_refs": ["CQ_03"],
      "scope_boundary": { "included": ["the empty-workspace fact and design-before-create discipline", "the Soul-plus-Element dual mapping per persona", "wardrobe-as-prop/character and location-as-environment rows"], "excluded": ["Soul training mechanics (soul node)", "Element creation mechanics (element node)", "persona visual design itself (shot-pipeline/character scope)"] },
      "pros": [ { "claim": "Planning registry rows before creation forces the likeness review and model-compatibility check to the cheapest point", "example": "catching that a planned wardrobe module needs an element-compatible model before any Soul training or generation spend", "weight": 0.8 } ],
      "cons": [ { "claim": "Upfront registry design delays the first visible output, which tempts skipping it under owner enthusiasm", "example": "spending a session authoring rows while the owner asks where the first reel is", "weight": 0.52 } ],
      "failure_modes": ["generating persona content before any registry row exists, orphaning identity lineage from day one", "bootstrapping only Souls and hitting the one-Soul limit at the first two-persona shot"],
      "acceptance_tests": ["the bootstrap plan cites the empty show_characters/show_reference_elements lists, assigns each persona one Soul row and one Element row, and models wardrobe/locations as Elements with authored-before-created status fields"],
      "revisit_triggers": ["the first fidelity A/B (Souls vs Elements) lands and shifts the dual-mapping policy", "a third persona or a recurring guest persona is commissioned", "the workspace stops being empty via any out-of-band creation"],
      "base": { "criticality": 0.86, "business_value": 0.8, "user_value": 0.78, "technical_complexity": 0.42, "risk_if_wrong": 0.74, "cross_topic_coupling": 0.82, "irreversibility": 0.5, "confidence": 0.78, "node_conflict_pressure": 0.4, "acceptance_test_pass_rate": 0.84, "dependency_gate_pass_rate": 0.86, "prior_importance": 0.86, "evidence_confidence": 0.78, "failure_rate": 0.15, "downside_weight": 0.55, "uncertainty_interval": [0.16, 0.38], "update_signal": "first creation runs or a fidelity A/B reshapes the per-persona asset mapping" }
    },
    {
      "id": "SOUL_ASSET_LIFECYCLE",
      "topic": "soul_training_constraints_and_registry_row",
      "definition": "Operate the Soul lifecycle inside its hard surface constraints [FACT — live tool schemas and printed constraints, 2026-07-03]: training takes a name plus 5-20 reference images (media_id UUIDs from media_confirm, completed image-job ids, or https URLs), runs ~10 minutes, non-blocking; types are soul (legacy), soul_2, soul_cinematic; a trained Soul is usable ONLY with the soul_2 and soul_cinematic image models; and ONE soul_id per generation — a two-persona shot cannot carry two Souls. Training credit cost and exact training-image quality requirements beyond '5-20 images, ~10 min' are [UNKNOWN — not exposed on the read surface], so the FIRST Soul training is itself an escalation item (spend of unknown size). The registry row per Soul: local id, soul_id once minted, persona served, the exact reference-image handles used (the persona reference sheet doubles as the identity-drift baseline), training date, type, and status. Retraining mints a NEW row and soul_id lineage — a retrained Soul is a new asset whose clips must not silently mix with the old one's.",
      "group": "registry",
      "dependencies": ["PERSONA_REGISTRY_BOOTSTRAP", "PLATFORM_HANDLE_REGISTRY"],
      "competency_question_refs": ["CQ_04"],
      "scope_boundary": { "included": ["the 5-20 image / ~10 min / non-blocking training facts", "the soul_2+soul_cinematic-only and one-Soul-per-generation constraints", "the Soul registry row and retrain-as-new-asset rule"], "excluded": ["two-persona routing (element node)", "reference-image visual design (character scope)"] },
      "pros": [ { "claim": "Recording the exact reference-image handles per Soul gives identity drift a fixed baseline to be measured against", "example": "comparing a suspect clip's persona against the very images that trained the Soul rather than against another generation", "weight": 0.82 } ],
      "cons": [ { "claim": "Soul reach is narrow — two image models only, one Soul per generation — so Souls alone cannot carry the production", "example": "a planned two-persona terrace still discovered unproducible on Soul models, forcing an Element re-plan mid-reel", "weight": 0.62 } ],
      "failure_modes": ["training a Soul before the training cost is preflighted or escalated, spending an unknown sum", "reusing one registry row across a retrain so old and new soul_id clips mix"],
      "acceptance_tests": ["the lifecycle states 5-20 images/~10 min/non-blocking, soul_2+soul_cinematic-only usage, one soul_id per generation, [UNKNOWN] training cost handled by escalate-before-first-train, and retrain-mints-new-row"],
      "revisit_triggers": ["the Soul training cost becomes known via a first train or a surface change", "the usable-model list for Souls changes", "identity drift traces back to reference-sheet weaknesses requiring retrain policy changes"],
      "base": { "criticality": 0.85, "business_value": 0.8, "user_value": 0.78, "technical_complexity": 0.5, "risk_if_wrong": 0.78, "cross_topic_coupling": 0.8, "irreversibility": 0.6, "confidence": 0.78, "node_conflict_pressure": 0.45, "acceptance_test_pass_rate": 0.84, "dependency_gate_pass_rate": 0.84, "prior_importance": 0.85, "evidence_confidence": 0.8, "failure_rate": 0.16, "downside_weight": 0.6, "uncertainty_interval": [0.16, 0.4], "update_signal": "first training run resolves the cost/quality unknowns or the Soul-compatible model list changes" }
    },
    {
      "id": "ELEMENT_ASSET_LIFECYCLE",
      "topic": "reference_elements_wardrobe_modules_location_refs",
      "definition": "Operate the Reference Element lifecycle [FACT — live tool descriptions, 2026-07-03]: creation is instant (single or few images, no training); category enum character|environment|prop; usage is embedding <<<element_id>>> tokens inside the prompt of generate_image/generate_video with MULTIPLE tokens per prompt allowed — the surface-designated path for shots containing both personas; Elements are NOT usable with the Soul models. Named element-compatible image models: nano_banana_2, nano_banana_flash, gpt_image_2, seedream_v4_5, seedream_v5_lite, cinematic_studio_2_5; the video list names Cinema Studio Video 2/3.0, Seedance 2.0, Kling 3.0 but was truncated in transmission [UNKNOWN — whether more video models are element-compatible]. Registry mapping: wardrobe modules ride as prop/character Elements, locations as environment Elements, each row carrying local id, element_id, category, source-image handles, and the compatible-model list it was last verified against. Whether Elements degrade identity fidelity versus Souls is [UNKNOWN — surface says Souls are identity-faithful and Elements are instant references; the gap is only testable by generation], so early reels must A/B and record before wardrobe libraries are built out.",
      "group": "registry",
      "dependencies": ["PERSONA_REGISTRY_BOOTSTRAP", "PLATFORM_HANDLE_REGISTRY"],
      "competency_question_refs": ["CQ_05"],
      "scope_boundary": { "included": ["instant creation, the category enum, multi-token prompts, the not-with-Soul-models rule", "wardrobe-as-prop/character and location-as-environment registry rows", "the compatible-model lists with their truncation [UNKNOWN]"], "excluded": ["Soul mechanics (soul node)", "which model to pick per shot (shot-pipeline KB)"] },
      "pros": [ { "claim": "Elements are the only surface path to two-persona shots and to identity on non-Soul models, so registering them per persona unlocks most of the catalog", "example": "a two-persona dinner shot carrying both persona Elements plus an evening-wardrobe Element as three tokens in one Seedance prompt", "weight": 0.84 } ],
      "cons": [ { "claim": "Element identity fidelity is unproven and the video compatibility list is incomplete, so Element-heavy plans carry untested risk", "example": "building a twelve-module wardrobe library before the first A/B shows Elements hold the male persona's face acceptably", "weight": 0.62 } ],
      "failure_modes": ["embedding element tokens in a Soul-model prompt where Elements are unusable", "building the wardrobe library before the fidelity A/B, then discovering the gap and rebuilding on Souls"],
      "acceptance_tests": ["the lifecycle states instant creation, category enum, multiple <<<element_id>>> tokens per prompt, not-with-Soul-models, the named compatible-model lists with the video-list truncation tagged [UNKNOWN], and the wardrobe/location registry mapping"],
      "revisit_triggers": ["the element-compatible video-model list is resolved or extended on a later surface read", "the Souls-vs-Elements fidelity A/B lands", "a wardrobe or location module fails identity or style consistency in QC"],
      "base": { "criticality": 0.85, "business_value": 0.8, "user_value": 0.78, "technical_complexity": 0.5, "risk_if_wrong": 0.76, "cross_topic_coupling": 0.82, "irreversibility": 0.42, "confidence": 0.76, "node_conflict_pressure": 0.48, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.84, "prior_importance": 0.85, "evidence_confidence": 0.78, "failure_rate": 0.17, "downside_weight": 0.55, "uncertainty_interval": [0.18, 0.42], "update_signal": "fidelity A/B results or a resolved compatibility list change how much production weight Elements carry" }
    },
    {
      "id": "MODEL_ID_COLLISION_GUARD",
      "topic": "canonical_model_id_discipline_under_naming_collisions",
      "definition": "Guard the ledger against the surface's own naming collisions, which are documented facts: the Elements tool maps `nano_banana_2` to the display name '(Nano Banana Pro)' and `nano_banana_flash` to '(Nano Banana 2)', while the model catalog has `nano_banana_2`='Nano Banana 2' and `nano_banana_pro`='Nano Banana Pro'; `nano_banana_flash` is absent from the catalog entirely; `nano_banana_2_shots` carries the display name 'Nano Banana Pro' with a blank provider; and `soul_2`/`soul_v2` are duplicate ids with identical schemas [FACT — live surface discrepancies, 2026-07-03]. Which id the Elements pipeline actually routes to is [UNKNOWN — unresolved on the read surface; blocker: only a run-phase generation with ledger comparison can resolve routing]. Discipline: the ledger records the EXACT model id string passed on every call, never a display name; duplicate ids are canonicalized to one ledger id (soul_2, with soul_v2 recorded as alias); and any Elements-routed generation records both the id requested and, once observable, the id billed/reported — the first element-model generation is deliberately instrumented to resolve the routing question.",
      "group": "registry",
      "dependencies": ["ELEMENT_ASSET_LIFECYCLE"],
      "competency_question_refs": ["CQ_01"],
      "scope_boundary": { "included": ["the documented nano_banana and soul_2/soul_v2 collisions", "exact-id-string logging and alias canonicalization", "instrumenting the first element-routed generation to resolve routing"], "excluded": ["model quality choice (failure-ledger node and shot-pipeline KB)", "element usage mechanics (element node)"] },
      "pros": [ { "claim": "Exact-id logging keeps the failure ledger and cost accounting truthful even while display names contradict each other", "example": "attributing a bad batch to nano_banana_2 the catalog id, not to whichever 'Nano Banana Pro' a display name suggested", "weight": 0.78 } ],
      "cons": [ { "claim": "Until routing is resolved, per-model conclusions drawn from element-routed batches may be attributed to the wrong engine", "example": "recording an identity failure against nano_banana_2 when the Elements pipeline actually routed to the catalog-absent nano_banana_flash", "weight": 0.6 } ],
      "failure_modes": ["logging display names so the collision silently corrupts per-model statistics", "treating soul_2 and soul_v2 as different models and splitting their history"],
      "acceptance_tests": ["the guard cites the specific collisions, mandates exact-id logging with alias canonicalization, tags element routing [UNKNOWN] with the run-phase resolution plan, and blocks display names from the ledger"],
      "revisit_triggers": ["the first instrumented element-routed generation resolves the routing question", "a catalog refresh renames or merges the colliding ids", "a new duplicate-id pair appears on a later enumeration"],
      "base": { "criticality": 0.72, "business_value": 0.62, "user_value": 0.58, "technical_complexity": 0.42, "risk_if_wrong": 0.66, "cross_topic_coupling": 0.7, "irreversibility": 0.35, "confidence": 0.76, "node_conflict_pressure": 0.35, "acceptance_test_pass_rate": 0.84, "dependency_gate_pass_rate": 0.84, "prior_importance": 0.7, "evidence_confidence": 0.8, "failure_rate": 0.15, "downside_weight": 0.45, "uncertainty_interval": [0.18, 0.42], "update_signal": "run-phase routing evidence or a catalog change resolves or reshapes the collision set" }
    },
    {
      "id": "SHOT_PROVENANCE_RECORD",
      "topic": "full_provenance_row_per_kept_clip",
      "definition": "Define the provenance row every KEPT clip must carry before it may enter a timeline — the answer to 'which model, params, prompt version produced this'. Minimum fields: exact model id string (per the collision guard); full parameter set as called — resolution, duration, aspect ratio, and the model-specific knobs the surface exposes (genre, speedramp, cfg_scale, mode, quality, generate_audio/sound) [FACT — these params exist per the enumerated schemas]; prompt version id; identity refs used (soul_id or element ids); start_image/end_image handles; the job_id(s) returned; QC scores at each checkpoint passed; actual credit cost once reconciled; and the reel/shot slot it serves. The row is written when the generation is submitted and completed as results land — never reconstructed later, because the platform surfaces cannot supply prompt versions or QC verdicts. A clip with any missing provenance field is not 'kept' — it is quarantined until backfilled from ledger evidence or regenerated.",
      "group": "provenance",
      "dependencies": ["PROMPT_VERSION_CONTROL", "PLATFORM_HANDLE_REGISTRY"],
      "competency_question_refs": ["CQ_06"],
      "scope_boundary": { "included": ["the minimum field set for a kept clip", "write-at-submit, complete-at-result timing", "the quarantine rule for incomplete rows"], "excluded": ["seam chaining across clips (chain node)", "QC scoring content (QC node)"] },
      "pros": [ { "claim": "A complete provenance row makes every kept clip reproducible and every failure attributable", "example": "re-rendering a hero clip at 4k by replaying its exact model, params and prompt version after the owner asks for an upgrade", "weight": 0.86 } ],
      "cons": [ { "claim": "Field completeness costs per-generation effort exactly when batches are moving fastest", "example": "a four-output batch landing while the operator is mid-edit, tempting a 'fill it in later' that never happens", "weight": 0.6 } ],
      "failure_modes": ["keeping a clip whose prompt version was never logged, making its look unreproducible", "recording the params intended rather than the params actually sent after a clamp (durations are clamped to nearest allowed [FACT])"],
      "acceptance_tests": ["the row spec enumerates model id, full called params, prompt version, identity refs, frame handles, job_id, per-checkpoint QC scores, reconciled credit cost and reel slot, with write-at-submit timing and the quarantine rule for gaps"],
      "revisit_triggers": ["a new model exposes params the row schema lacks", "a quarantined clip reveals a systematic logging gap", "duration clamping or param rewriting is observed and needs an as-sent vs as-executed distinction"],
      "base": { "criticality": 0.9, "business_value": 0.84, "user_value": 0.76, "technical_complexity": 0.5, "risk_if_wrong": 0.82, "cross_topic_coupling": 0.85, "irreversibility": 0.5, "confidence": 0.8, "node_conflict_pressure": 0.4, "acceptance_test_pass_rate": 0.84, "dependency_gate_pass_rate": 0.84, "prior_importance": 0.9, "evidence_confidence": 0.78, "failure_rate": 0.15, "downside_weight": 0.62, "uncertainty_interval": [0.14, 0.36], "update_signal": "new model params, observed clamping behavior, or a quarantine incident extends the row schema" }
    },
    {
      "id": "KEYFRAME_CHAIN_PROVENANCE",
      "topic": "seam_records_and_job_id_chaining_across_clips",
      "definition": "Record the stitched reel's seam structure as first-class provenance. The platform structurally supports chaining: a generate_video media value accepts a prior generation's job_id, so clip N's end frame can seed clip N+1's start frame [FACT — generate_video schema]. Each seam row records: the transition type chosen for that seam (the shot-pipeline KB chooses it; this KB records it), the end-keyframe handle of clip N, the start-keyframe handle of clip N+1, whether the pair was chained by job_id or generated as separate stills, and the QC match verdict on the pair. The chain is what makes re-rolls safe: when any upstream clip or keyframe is re-rolled, every downstream row that references its handles is automatically marked STALE, forcing a deliberate re-link or regeneration instead of a silently broken stitch. Without seam rows, a reel is just a folder of clips and the most expensive property of the format — engineered continuity — is untracked.",
      "group": "provenance",
      "dependencies": ["SHOT_PROVENANCE_RECORD"],
      "competency_question_refs": ["CQ_07"],
      "scope_boundary": { "included": ["seam rows (transition type, both frame handles, chain method, match verdict)", "job_id chaining as the platform mechanism", "stale-marking of downstream rows on upstream re-roll"], "excluded": ["choosing the transition (shot-pipeline KB)", "editor-side seam execution (editing KB)"] },
      "pros": [ { "claim": "Stale-marking on re-roll prevents the classic stitched-reel failure of one regenerated clip orphaning every seam after it", "example": "re-rolling clip 2 for a hand artifact and having seams 2-3 and 3-4 flagged for re-link before assembly instead of at the phone-QC pass", "weight": 0.8 } ],
      "cons": [ { "claim": "Seam bookkeeping doubles the row count per reel and only pays off when re-rolls happen", "example": "a five-clip reel carrying four seam rows that are never touched because every clip passed QC first try", "weight": 0.5 } ],
      "failure_modes": ["chaining by job_id without recording it, so a re-roll invalidates downstream clips invisibly", "recording the transition plan in the edit notes but not on the seam row, losing it when the editor changes"],
      "acceptance_tests": ["the seam row spec carries transition type, both frame handles, chain method and match verdict, and the stale-marking rule fires on any upstream re-roll"],
      "revisit_triggers": ["a stitched reel ships with a broken seam, indicating the stale rule failed", "a new chaining mechanism appears (e.g. multi-frame conditioning)", "seam rows prove too coarse for loop-construction reels where last-to-first also chains"],
      "base": { "criticality": 0.82, "business_value": 0.76, "user_value": 0.74, "technical_complexity": 0.52, "risk_if_wrong": 0.74, "cross_topic_coupling": 0.78, "irreversibility": 0.4, "confidence": 0.78, "node_conflict_pressure": 0.35, "acceptance_test_pass_rate": 0.84, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.8, "evidence_confidence": 0.78, "failure_rate": 0.16, "downside_weight": 0.55, "uncertainty_interval": [0.16, 0.4], "update_signal": "a broken-seam incident or a new conditioning mechanism changes the seam row design" }
    },
    {
      "id": "MODEL_FAILURE_LEDGER",
      "topic": "per_model_per_shot_type_failure_and_trim_ledger",
      "definition": "Maintain the per-model failure ledger the owner's own directive implies ('try different, which gives better result' — A/B per shot-type and keep a per-model failure ledger, per the realism research's model-choice mitigation [FACT that physics adherence varies hugely across models per the VideoPhy spreads; the ledger design is the research file's stated policy]). Keyed by exact model id and shot type, each entry records: failure classes observed (mapped to the QC rubric items — text garble, hand morphing, contact-physics, identity drift, texture boiling), keep-rate (kept clips over attempts), typical re-roll count, reconciled cost per keep once known, and the per-model color trim preset — the editing research directs maintaining a small per-model trim (offset/temp/sat nudge toward the house target) learned from first batches, because per-model color signatures are expected but [UNKNOWN until generation runs]. The ledger is what converts spend into knowledge: every killed generation is a data point, which is why ledger rows are never deleted.",
      "group": "provenance",
      "dependencies": ["SHOT_PROVENANCE_RECORD"],
      "competency_question_refs": ["CQ_13"],
      "scope_boundary": { "included": ["the model x shot-type key and failure-class/keep-rate/trim fields", "killed generations as retained evidence", "the ledger driving default model choice for repeat shot types"], "excluded": ["which artifact classes exist (realism research scope)", "grading execution (editing KB)"] },
      "pros": [ { "claim": "A failure ledger turns re-roll spend into a routing table so repeat shot types stop paying the exploration tax", "example": "defaulting all future two-persona interiors to the model whose ledger keep-rate is highest instead of re-running the A/B", "weight": 0.82 } ],
      "cons": [ { "claim": "Early ledger entries rest on tiny samples and can freeze a premature winner", "example": "crowning a model after three keeps that were really carried by an unusually easy shot type", "weight": 0.58 } ],
      "failure_modes": ["recording failures against display names so the collision corrupts the routing table", "deleting killed-generation rows and losing the denominator of every keep-rate"],
      "acceptance_tests": ["the ledger spec keys on exact model id x shot type, records failure classes mapped to rubric items, keep-rate, cost-per-keep and trim preset, and retains killed generations as evidence"],
      "revisit_triggers": ["a model update ships and invalidates that model's accumulated entries", "keep-rates shift materially over a batch, suggesting drift in the model or the prompts", "the per-model color signatures become measured, replacing the [UNKNOWN] trim guesses"],
      "base": { "criticality": 0.8, "business_value": 0.82, "user_value": 0.72, "technical_complexity": 0.48, "risk_if_wrong": 0.68, "cross_topic_coupling": 0.78, "irreversibility": 0.35, "confidence": 0.76, "node_conflict_pressure": 0.5, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.8, "evidence_confidence": 0.74, "failure_rate": 0.18, "downside_weight": 0.5, "uncertainty_interval": [0.18, 0.42], "update_signal": "model updates, measured color signatures, or keep-rate shifts re-weight the ledger's routing advice" }
    },
    {
      "id": "CREDIT_SPEND_ACCOUNTING",
      "topic": "budget_caps_get_cost_preflight_and_reconciliation",
      "definition": "Account for credits under honest uncertainty. Known facts [FACT — live surface, 2026-07-03]: balance 1,010 credits on plan 'plus'; get_cost:true on a generate_* call returns the credit cost WITHOUT submitting (the sanctioned zero-spend preflight); a transactions surface exists (name enumerated, semantics unexercised); count is 1-4 per call. Unknown and carried as such: per-generation cost per model x resolution x duration cell, Soul training cost, and any plan-dependence of the catalog [UNKNOWN — none exposed on the read surface]. Discipline: every reel receives a spend cap before its first call; every untested cost cell is preflighted with get_cost before first use and the result cached in the ledger; actual spend per asset is reconciled against balance/transactions after each batch; and cost-per-keep (not cost-per-call) is the reported efficiency number because re-rolls are part of the true price. Proposed default thresholds for escalation — before any Soul training, before any single call whose preflighted cost exceeds ~5% of current balance, when a reel exceeds its cap, and when balance falls below ~25% of its period-start value — are [ESTIMATE — policy design for the owner to ratify; no platform fact grounds a specific number].",
      "group": "provenance",
      "dependencies": ["PRODUCTION_LEDGER"],
      "competency_question_refs": ["CQ_08"],
      "scope_boundary": { "included": ["the balance/plan facts and get_cost preflight", "per-reel caps, cost-cell caching, reconciliation, cost-per-keep reporting", "the [ESTIMATE] threshold proposals awaiting owner ratification"], "excluded": ["what to spend on (shot-pipeline KB)", "the escalation transaction itself (escalation node)"] },
      "pros": [ { "claim": "get_cost preflighting converts unknown prices into cached facts at zero credit cost", "example": "pricing the cinematic_studio_3_0 4k x 10s cell before a hero-shot batch instead of discovering it from a shrunken balance", "weight": 0.86 } ],
      "cons": [ { "claim": "Preflight ceremony adds a call per new cost cell and can be skipped under batch momentum exactly when models are being swapped most", "example": "an A/B sweep across four video models submitted without preflights because the operator assumed similar prices", "weight": 0.6 } ],
      "failure_modes": ["submitting untested cost cells unpreflighted and blowing the reel cap in one batch", "reporting cost-per-call and hiding that a shot needed six re-rolls per keep"],
      "acceptance_tests": ["the accounting states balance 1,010/plan plus at enumeration, mandates get_cost preflight per new cell with ledger caching, sets a per-reel cap before first call, reconciles actuals via balance/transactions, and tags all thresholds [ESTIMATE] pending owner ratification"],
      "revisit_triggers": ["the owner ratifies or replaces the proposed thresholds", "preflight results populate the cost table, enabling real reel budgets", "an unexplained balance drop or a transactions-surface exercise changes reconciliation practice"],
      "base": { "criticality": 0.9, "business_value": 0.88, "user_value": 0.75, "technical_complexity": 0.45, "risk_if_wrong": 0.84, "cross_topic_coupling": 0.82, "irreversibility": 0.7, "confidence": 0.78, "node_conflict_pressure": 0.55, "acceptance_test_pass_rate": 0.84, "dependency_gate_pass_rate": 0.84, "prior_importance": 0.9, "evidence_confidence": 0.8, "failure_rate": 0.15, "downside_weight": 0.7, "uncertainty_interval": [0.14, 0.36], "update_signal": "preflighted prices, ratified thresholds, or a balance anomaly reshape the budget rules" }
    },
    {
      "id": "REEL_BRIEF_INTAKE",
      "topic": "brief_to_reel_spec_with_budget_and_slots",
      "definition": "Open every reel with an intake that converts the owner's brief into a production spec the ledger can track: reel local id; format facts (9:16 at 1080x1920 delivery, 3-6 s clips stitched — the owner's stated format, with every both-frames video model on the surface accepting durations in the 3-6 s band except seedance1_5 min 4, minimax_hailuo min 6, marketing_studio_video min 12 [FACT — duration fields]); the clip/seam slot list (slot count from target length arithmetic: a 15 s reel is 3-5 shots, a 30 s reel 6-9 at the stated clip lengths); persona/wardrobe/location refs resolved against the registry (missing rows block intake, not generation); the spend cap; and the escalation-sensitive flags declared upfront — does the brief touch likeness risk, new Souls, or untested cost cells. The creative shot/transition planning that fills the slots belongs to the shot-pipeline KB; intake's job is that no reel enters production with unresolved registry refs, no cap, or an undeclared escalation surface.",
      "group": "pipeline",
      "dependencies": ["ASSET_NAMING_SCHEME"],
      "competency_question_refs": ["CQ_09"],
      "scope_boundary": { "included": ["the reel spec fields (id, format, slots, registry refs, cap, escalation flags)", "registry-gap blocking at intake", "slot arithmetic from the stated format"], "excluded": ["shot design and transition choice (shot-pipeline KB)", "posting strategy (media-manager scope)"] },
      "pros": [ { "claim": "Blocking unresolved registry refs at intake moves the cheapest failure to the cheapest moment", "example": "discovering the yacht-location Element does not exist during intake instead of when clip 3's prompt needs its token", "weight": 0.8 } ],
      "cons": [ { "claim": "Heavy intake ceremony on a two-line owner brief can read as bureaucracy and invite bypass", "example": "the owner voice-noting 'make one like last week's but at night' and expecting output, not a spec review", "weight": 0.55 } ],
      "failure_modes": ["starting generation with a slot list but no spend cap, so overrun is discovered instead of prevented", "accepting a brief whose likeness-adjacent ask ('make her look like that actress') is not flagged at intake"],
      "acceptance_tests": ["the intake spec carries reel id, format facts, slot list with arithmetic, resolved registry refs (gaps block), spend cap, and explicit escalation flags before any generation call"],
      "revisit_triggers": ["a reel enters production bypassing intake, revealing a process hole", "the format changes (longer reels, new aspect)", "recurring briefs justify a template that shortens intake without dropping gates"],
      "base": { "criticality": 0.82, "business_value": 0.78, "user_value": 0.76, "technical_complexity": 0.38, "risk_if_wrong": 0.7, "cross_topic_coupling": 0.8, "irreversibility": 0.35, "confidence": 0.8, "node_conflict_pressure": 0.42, "acceptance_test_pass_rate": 0.86, "dependency_gate_pass_rate": 0.86, "prior_importance": 0.82, "evidence_confidence": 0.76, "failure_rate": 0.14, "downside_weight": 0.5, "uncertainty_interval": [0.14, 0.36], "update_signal": "an intake bypass, a format change, or a stable brief pattern changes the intake template" }
    },
    {
      "id": "QC_GATE_CHECKPOINTS",
      "topic": "three_checkpoints_and_reroll_gate_rules",
      "definition": "Place the QC gates and enforce their rules; the rubric CONTENT (which artifacts to look for, in which order) is the realism research's scope — this node owns where the gates sit and what they trigger. Three checkpoints, per the grounding rubric: (1) start-frame image — text/logos, identity lock, object count, shadow/reflection geometry, background coherence, skin texture; (2) raw video — all items including contact physics and motion; (3) post-edit reel — top items re-checked at final resolution ON A PHONE, because the research's net doctrine is to optimize for the post-compression phone-screen viewing condition. Gate rules carried from the rubric: any single item scored 2 (visible at a glance) forces a re-roll; total >= 4 forces a re-roll; totals of 2-3 may pass to post only if the offending region is croppable/gradeable [the rubric ordering and gates are the research file's editorial synthesis, tagged ESTIMATE there and carried as the operating standard here]. Budget coupling: QC runs BEFORE any upscale/post spend on a clip — never polish an ungated asset. Every verdict is written to the provenance row; a gate without a written score did not happen.",
      "group": "pipeline",
      "dependencies": ["REEL_BRIEF_INTAKE"],
      "competency_question_refs": ["CQ_10"],
      "scope_boundary": { "included": ["the three checkpoint placements and their item subsets", "the re-roll gate rules and croppable-exception", "QC-before-upscale/post and written-verdict discipline"], "excluded": ["the artifact taxonomy itself (realism research)", "who performs QC (an automated VLM critic cannot be the only gate — the research notes MLLM judgments misalign with human perception [FACT — Artifact-Bench abstract])"] },
      "pros": [ { "claim": "Fixed checkpoints with written verdicts make quality a ledger property instead of an operator mood", "example": "auditing why a garbled-badge clip shipped and finding checkpoint 3 was skipped, not that the rubric missed it", "weight": 0.84 } ],
      "cons": [ { "claim": "Hard gate rules generate re-roll spend on borderline clips that post could have cropped", "example": "re-rolling a clip for an edge-of-frame melted extra that a 9:16 punch-in would have excised for free", "weight": 0.58 } ],
      "failure_modes": ["running final QC on the editing monitor instead of a phone at feed resolution", "spending upscale credits on a clip that then fails raw-video QC"],
      "acceptance_tests": ["the gate spec places the three checkpoints with their item subsets, states the single-2 and total>=4 re-roll rules with the croppable exception, mandates phone-screen final QC, QC-before-upscale, and written verdicts on the provenance row"],
      "revisit_triggers": ["a shipped reel draws AI-tell comments, indicating a gate miss", "the rubric content is recalibrated against real batch data", "an automated critic is added and its miss pattern needs a human-check rebalance"],
      "base": { "criticality": 0.88, "business_value": 0.84, "user_value": 0.85, "technical_complexity": 0.5, "risk_if_wrong": 0.8, "cross_topic_coupling": 0.85, "irreversibility": 0.45, "confidence": 0.78, "node_conflict_pressure": 0.55, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.84, "prior_importance": 0.88, "evidence_confidence": 0.74, "failure_rate": 0.16, "downside_weight": 0.65, "uncertainty_interval": [0.16, 0.4], "update_signal": "shipped-reel feedback or batch-calibrated rubric changes move the gates or their thresholds" }
    },
    {
      "id": "KEYFRAME_STILL_STAGE",
      "topic": "stills_first_generation_and_qc_before_video",
      "definition": "Run the stills stage as the cheap place to fail: generate the start/end keyframes for every clip slot, QC them at checkpoint 1, and only promote QC-passed pairs to video generation. The cost ordering is category-level fact (stills are cheaper than video on credit-based platforms as a class; exact Higgsfield prices [UNKNOWN] until preflighted), and the mechanism benefit is grounded: image-to-video from a QC-approved start frame locks anatomy, wardrobe and identity at t=0, converting image-QC wins into video wins and shrinking identity-drift exposure [ESTIMATE in the realism research — mechanism reasoning, high confidence]. Operationally: each still is generated on a registry-verified identity path (soul_id on Soul models for solo shots; element tokens on element-compatible models for two-persona or wardrobe-bearing shots), logged with its prompt version, and seam pairs are QC'd AS PAIRS (position, brightness, color, blur direction match) before any video call — pairs failing the match are regenerated at still prices, which is the entire point of the stage.",
      "group": "pipeline",
      "dependencies": ["QC_GATE_CHECKPOINTS", "SOUL_ASSET_LIFECYCLE", "ELEMENT_ASSET_LIFECYCLE"],
      "competency_question_refs": ["CQ_09", "CQ_10"],
      "scope_boundary": { "included": ["stills-before-video ordering and pair-level seam QC", "identity-path selection per shot type", "promotion criteria to the video stage"], "excluded": ["still prompt craft (shot-pipeline KB)", "video generation params (video node)"] },
      "pros": [ { "claim": "Failing at still prices instead of video prices multiplies the re-roll budget the same credits buy", "example": "burning four cheap still re-rolls to fix a garbled clutch logo before the expensive both-frames video call runs once", "weight": 0.84 } ],
      "cons": [ { "claim": "Stills cannot QC motion, so the stage catches composition and identity but never contact physics", "example": "a perfect start frame producing a foot-sliding walk that only raw-video QC can catch", "weight": 0.6 } ],
      "failure_modes": ["promoting an unmatched seam pair and paying for a video whose cut can never be hidden", "generating a two-persona still on a Soul model where the second identity cannot be held"],
      "acceptance_tests": ["the stage promotes only checkpoint-1-passed stills, QCs seam pairs as pairs on position/brightness/color/blur-direction, selects the identity path per the registry rules, and logs prompt versions per still"],
      "revisit_triggers": ["preflighted prices change the still/video cost ratio materially", "pair-match failures cluster on a transition type, indicating a planning gap", "a video model's start-frame adherence proves weak, reducing the stage's leverage for it"],
      "base": { "criticality": 0.84, "business_value": 0.8, "user_value": 0.76, "technical_complexity": 0.5, "risk_if_wrong": 0.72, "cross_topic_coupling": 0.8, "irreversibility": 0.4, "confidence": 0.78, "node_conflict_pressure": 0.42, "acceptance_test_pass_rate": 0.84, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.84, "evidence_confidence": 0.76, "failure_rate": 0.16, "downside_weight": 0.55, "uncertainty_interval": [0.16, 0.4], "update_signal": "measured cost ratios or start-frame adherence data re-weight how much QC lives at the stills stage" }
    },
    {
      "id": "VIDEO_GEN_STAGE",
      "topic": "video_generation_with_full_logging_and_reroll_policy",
      "definition": "Run video generation as a fully-logged, budget-aware batch process. Every call: model chosen per the failure ledger (or a declared A/B on a new shot type), params inside the model's enumerated bands (durations are clamped to nearest allowed [FACT — generate_* envelope]), count 1-4 used deliberately (multiple candidates per slot when re-roll probability is high), get_cost preflight on any untested cell, and the provenance row opened at submit. Chained slots pass the promoted keyframe handles (start_image/end_image; both-frames models only for seam-bearing slots — the both-frames list is enumerated fact). Re-roll policy binds to the QC gates: a rubric-triggered re-roll re-uses the same prompt version unless the failure class implicates the prompt, in which case a new version is minted (the QC-to-prompt feedback loop); re-rolls per slot are counted and the count is what trips the credit escalation before the cap does. Kept clips close their provenance row; killed clips stay as failure-ledger evidence.",
      "group": "pipeline",
      "dependencies": ["KEYFRAME_STILL_STAGE", "CREDIT_SPEND_ACCOUNTING", "KEYFRAME_CHAIN_PROVENANCE"],
      "competency_question_refs": ["CQ_06", "CQ_09"],
      "scope_boundary": { "included": ["ledger-driven model choice with declared A/B exceptions", "preflighted, in-band, provenance-opened calls", "the re-roll policy and its prompt-version and escalation couplings"], "excluded": ["which camera move or transition to prompt (shot-pipeline KB)", "QC content (realism research)"] },
      "pros": [ { "claim": "Deliberate count usage buys multiple candidates per expensive slot in one preflighted call", "example": "requesting count 3 on a known-hard two-persona seam slot so one submission yields a pick instead of three sequential waits", "weight": 0.78 } ],
      "cons": [ { "claim": "Batch momentum erodes logging discipline exactly when the most money is moving", "example": "an evening of A/B sweeps where half the killed clips never got failure classes recorded, starving the ledger", "weight": 0.62 } ],
      "failure_modes": ["calling an out-of-band duration and shipping a clamped clip whose real length breaks the beat plan", "re-rolling with a silently tweaked prompt so the A/B result is unattributable"],
      "acceptance_tests": ["every call is preflighted when its cell is untested, in-band on duration/aspect, opens a provenance row at submit, chains promoted keyframes on both-frames models for seam slots, and re-rolls follow the same-version-unless-prompt-implicated rule with counts feeding escalation"],
      "revisit_triggers": ["clamping or param rewriting is observed, needing as-sent/as-executed logging", "a model update shifts keep-rates mid-production", "re-roll counts trip escalation repeatedly, indicating miscalibrated caps or shot plans"],
      "base": { "criticality": 0.86, "business_value": 0.82, "user_value": 0.78, "technical_complexity": 0.58, "risk_if_wrong": 0.78, "cross_topic_coupling": 0.84, "irreversibility": 0.55, "confidence": 0.76, "node_conflict_pressure": 0.55, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.86, "evidence_confidence": 0.76, "failure_rate": 0.18, "downside_weight": 0.62, "uncertainty_interval": [0.18, 0.42], "update_signal": "observed clamping, model updates, or repeated escalation trips reshape the batch discipline" }
    },
    {
      "id": "IDENTITY_DRIFT_TOLERANCE",
      "topic": "drift_detection_baseline_tolerance_and_response",
      "definition": "Operationalize identity drift — the industry-named failure where a character's face geometry, eye color, hair or wardrobe changes across frames or shots [FACT — named phenomenon per the realism research's cited sources] — as a measurable production property. Baseline: the persona reference sheet, which is exactly the Soul's recorded training-image set (or the Element's source images), so the baseline is a ledger fact, not a memory. Detection: the identity-lock rubric item at every QC checkpoint, within-shot and across shots. Tolerance: no fetched source quantifies an acceptable drift threshold [UNKNOWN — quantified drift tolerance; blocker: no study or surface number exists in the grounding], so tolerance is a policy: any drift a viewer could name (different eye color, changed face shape, wrong wardrobe module) is beyond tolerance; subtler drift accumulating across a reel is judged at the phone-screen checkpoint. Response beyond tolerance: quarantine the clip lineage, diagnose the path (Soul vs Element vs prompt), and ESCALATE with a retrain/re-create decision — because retraining a Soul spends unknown credits and resets the persona baseline the whole archive was QC'd against. The Souls-vs-Elements fidelity gap is itself [UNKNOWN — only testable by generation], which is why the first A/B is a scheduled iteration item, not an assumption.",
      "group": "governance",
      "dependencies": ["QC_GATE_CHECKPOINTS", "SOUL_ASSET_LIFECYCLE"],
      "competency_question_refs": ["CQ_12"],
      "scope_boundary": { "included": ["the reference-sheet baseline as a ledger fact", "the nameable-drift tolerance policy and phone-screen accumulation check", "quarantine-diagnose-escalate with the retrain decision"], "excluded": ["face-swap or identity post-fixes (not on the enumerated surface)", "the likeness question of WHO the persona resembles (likeness node)"] },
      "pros": [ { "claim": "Anchoring drift to the recorded training images makes 'is this still her' answerable by comparison instead of recollection", "example": "settling a mid-reel dispute by diffing the suspect clip against the Soul's five ledger-recorded reference handles", "weight": 0.8 } ],
      "cons": [ { "claim": "A qualitative nameable-drift threshold leaves borderline calls to judgment until quantified data exists", "example": "two reviewers splitting on whether a slightly narrower jaw in clip 4 is drift or angle", "weight": 0.6 } ],
      "failure_modes": ["measuring drift against the previous clip instead of the reference sheet, letting drift ratchet", "retraining a Soul casually and invalidating the baseline every archived clip was approved against"],
      "acceptance_tests": ["the policy states the reference-sheet baseline, the nameable-drift threshold with the accumulation check at phone QC, the quarantine-diagnose-escalate response, and carries the tolerance-quantification and fidelity-gap [UNKNOWN]s by name"],
      "revisit_triggers": ["the first Souls-vs-Elements fidelity A/B lands", "a viewer or the owner reports persona inconsistency on a shipped reel", "a retrain is executed, resetting the baseline and forcing archive re-annotation"],
      "base": { "criticality": 0.86, "business_value": 0.82, "user_value": 0.84, "technical_complexity": 0.55, "risk_if_wrong": 0.8, "cross_topic_coupling": 0.82, "irreversibility": 0.55, "confidence": 0.74, "node_conflict_pressure": 0.5, "acceptance_test_pass_rate": 0.8, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.86, "evidence_confidence": 0.72, "failure_rate": 0.18, "downside_weight": 0.65, "uncertainty_interval": [0.2, 0.45], "update_signal": "fidelity A/B data or a shipped-drift incident converts the qualitative threshold toward a measured one" }
    },
    {
      "id": "POST_ASSEMBLY_GRADE",
      "topic": "assembly_grade_matching_and_master_export_stage",
      "definition": "Track the post stage as gated operations with recorded artifacts (the craft lives in the editing KB; this node owns the checklist slots and what the ledger captures). Ordered sub-steps from the grounding: assemble and beat-lock the timeline; declare a HERO CLIP per reel (best skin + best exposure) and match every other clip to it (luminance on waveform, white balance on parade, skin on vectorscope — professional Resolve practice per the editing research); apply the per-model trim presets from the failure ledger BEFORE hero matching; apply the house look last; QC every cut boundary so both frames sit within a tight luminance/hue window; export the 1080x1920 master; and run the phone-screen checkpoint on an actual device. Ledger capture per reel: hero clip id, per-clip trim/grade lineage, cut-boundary QC verdicts, master file hash and export settings. The stage consumes only QC-passed, provenance-complete clips — post never launders an ungated asset into a reel.",
      "group": "pipeline",
      "dependencies": ["VIDEO_GEN_STAGE", "MODEL_FAILURE_LEDGER"],
      "competency_question_refs": ["CQ_09", "CQ_13"],
      "scope_boundary": { "included": ["the ordered post sub-steps as checklist gates", "hero-clip declaration and per-model trims before matching", "ledger capture (hero id, grade lineage, boundary verdicts, master hash)"], "excluded": ["grading technique detail (editing KB)", "cover design craft (editing KB; recorded at release)"] },
      "pros": [ { "claim": "Recording grade lineage per clip makes the reel re-exportable and the house look reproducible across reels", "example": "rebuilding a reel's grade after a re-rolled clip replaces slot 3 by replaying the recorded trim and hero-match chain", "weight": 0.78 } ],
      "cons": [ { "claim": "Stage gating adds friction to a craft process editors prefer to keep fluid", "example": "an editor's spontaneous regrade of two clips leaving the ledger's lineage stale until reconciled", "weight": 0.55 } ],
      "failure_modes": ["assembling a quarantined clip because post trusted the folder instead of the ledger state", "applying the house look before per-clip balancing, amplifying cross-model mismatch (the LUT-on-unbalanced-clips error the research names)"],
      "acceptance_tests": ["the stage consumes only kept/provenance-complete clips, orders trims-then-hero-match-then-look, records hero id, grade lineage, boundary verdicts and master hash, and ends at the phone-screen checkpoint"],
      "revisit_triggers": ["measured per-model color signatures replace the guessed trims", "a boundary-QC failure pattern implicates a transition type or model pair", "delivery specs change (bitrate, resolution, platform)"],
      "base": { "criticality": 0.8, "business_value": 0.78, "user_value": 0.8, "technical_complexity": 0.55, "risk_if_wrong": 0.68, "cross_topic_coupling": 0.78, "irreversibility": 0.45, "confidence": 0.76, "node_conflict_pressure": 0.42, "acceptance_test_pass_rate": 0.82, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.8, "evidence_confidence": 0.74, "failure_rate": 0.16, "downside_weight": 0.55, "uncertainty_interval": [0.18, 0.42], "update_signal": "measured signatures, boundary-failure patterns, or delivery-spec changes reshape the post gates" }
    },
    {
      "id": "RELEASE_PACKAGING",
      "topic": "cover_post_verify_and_close_the_reel",
      "definition": "Close each reel with a release package: the selected cover (chosen per the editing research's cover rules — center-square crop safety, face/emotion presence, grid coherence with the house template; the craft is the editing KB's, the RECORD is this node's), the caption/disclosure decisions as briefed, the upload executed at highest quality from the app per the compression guidance, and the post-publication verification (the reel plays, the cover crops survive feed/grid, the first-frames hook is intact). Trial Reels exist as a native non-follower test mechanism (announced 2024-12-10, available to public accounts >= 1,000 followers; the announcement figures are carried one notch down because the official page was proxy-blocked and the numbers came via secondary writeups [FACT/excerpt per the editing research]) — whether to use one is a brief-level choice recorded here, not decided here. Closing the reel writes the release row: post timestamp, cover id, master hash posted, trial-vs-direct, and links every kept clip's provenance into the published artifact — the moment the archive becomes auditable end-to-end. HARD PRECONDITION: no open escalation on any asset in the reel; an open likeness/policy flag or unresolved drift quarantine blocks release entirely.",
      "group": "pipeline",
      "dependencies": ["POST_ASSEMBLY_GRADE"],
      "competency_question_refs": ["CQ_09"],
      "scope_boundary": { "included": ["the release row (timestamp, cover, master hash, trial choice, provenance links)", "post-publication verification checks", "the no-open-escalation precondition"], "excluded": ["cover design and A/B craft (editing KB)", "posting cadence and analytics strategy (media-manager scope)"] },
      "pros": [ { "claim": "A release row that links published output to full provenance makes any later question — takedown, re-edit, 'which model made this' — answerable in one lookup", "example": "responding to an owner query about a three-week-old reel's clip 2 by following the release row to its provenance in seconds", "weight": 0.78 } ],
      "cons": [ { "claim": "Release ceremony sits between a finished reel and the owner's desire to see it live", "example": "a finished Friday reel held for verification steps while the owner watches the golden posting hour pass", "weight": 0.55 } ],
      "failure_modes": ["posting while a likeness flag is open because the flag lived in chat, not on the asset row", "publishing without recording the master hash, so what is live cannot be matched to what was QC'd"],
      "acceptance_tests": ["release writes the full row (timestamp, cover id, master hash, trial choice, provenance links), runs post-publication verification, and is blocked by any open escalation, likeness flag or drift quarantine on any constituent asset"],
      "revisit_triggers": ["a posted reel needs takedown or edit, exercising the audit path", "trial-reel usage begins and its results need row fields", "platform disclosure/labeling requirements change what the release row must carry"],
      "base": { "criticality": 0.8, "business_value": 0.8, "user_value": 0.78, "technical_complexity": 0.4, "risk_if_wrong": 0.74, "cross_topic_coupling": 0.76, "irreversibility": 0.75, "confidence": 0.78, "node_conflict_pressure": 0.45, "acceptance_test_pass_rate": 0.84, "dependency_gate_pass_rate": 0.84, "prior_importance": 0.8, "evidence_confidence": 0.74, "failure_rate": 0.14, "downside_weight": 0.65, "uncertainty_interval": [0.16, 0.4], "update_signal": "an audit exercise, trial-reel adoption, or disclosure-rule changes extend the release row" }
    },
    {
      "id": "LIKENESS_POLICY_GATE",
      "topic": "real_person_identifiability_halt_and_escalate",
      "definition": "Enforce the closed likeness lane. Trigger conditions: a generated persona or any output resembles an identifiable real individual; a brief, comment, or internal suggestion asks to evoke a specific real person (by name, by 'like that actress', by supplying a real person's photo as a reference); or a reference-image set for a Soul/Element contains a real identifiable person without documented consent lineage. Response: HALT the affected lineage (no further generations, no post, no polish), flag every ledger row it touches, and escalate to the owner with the evidence — the resemblance, the asset lineage, and the requested decision. What this node deliberately does NOT contain, by standing policy: any technique for approximating a real person's likeness while obscuring or altering the face — the lane is closed, and 'closed' means no workaround guidance exists anywhere in the system, not that a clever framing is pending approval. The gate applies from the earliest cheap point: the persona reference sheets at registry bootstrap are the first likeness review, before any credits are spent, and every QC checkpoint re-checks because generation is stochastic and resemblance can emerge unprompted.",
      "group": "governance",
      "dependencies": ["PERSONA_REGISTRY_BOOTSTRAP"],
      "competency_question_refs": ["CQ_11"],
      "scope_boundary": { "included": ["the trigger conditions including emergent unprompted resemblance", "halt-flag-escalate with evidence", "the review points (reference sheets first, every QC checkpoint after)"], "excluded": ["any likeness-evocation or face-obscuring technique (closed lane)", "platform disclosure mechanics (media-manager scope)"] },
      "pros": [ { "claim": "Reviewing likeness at the reference-sheet stage catches the highest-consequence failure at the lowest-cost moment", "example": "rejecting a proposed female-persona reference set that skews toward one recognizable model's face before any Soul trains on it", "weight": 0.86 } ],
      "cons": [ { "claim": "Aggressive resemblance-flagging on generic attractive faces can stall production on false positives", "example": "halting a batch because one reviewer sees a resemblance a second reviewer cannot name", "weight": 0.5 } ],
      "failure_modes": ["treating an owner's casual 'make her look like X' as a creative note instead of a gate trigger", "letting a flagged lineage keep generating 'while we wait' so sunk cost argues against the halt"],
      "acceptance_tests": ["the gate names all trigger conditions including emergent resemblance and consent-less real-person references, halts the lineage immediately, flags all touched rows, escalates with evidence, and contains zero workaround technique"],
      "revisit_triggers": ["any likeness flag fires in production", "the owner issues a standing ruling that refines the trigger definitions", "platform or legal policy context shifts what identifiability means for this account"],
      "base": { "criticality": 0.95, "business_value": 0.85, "user_value": 0.8, "technical_complexity": 0.4, "risk_if_wrong": 0.92, "cross_topic_coupling": 0.8, "irreversibility": 0.85, "confidence": 0.82, "node_conflict_pressure": 0.5, "acceptance_test_pass_rate": 0.86, "dependency_gate_pass_rate": 0.86, "prior_importance": 0.95, "evidence_confidence": 0.78, "failure_rate": 0.08, "downside_weight": 0.9, "uncertainty_interval": [0.1, 0.32], "update_signal": "a fired flag, an owner ruling, or a policy-context shift refines the triggers — never the closed-lane status" }
    },
    {
      "id": "ESCALATION_MATRIX",
      "topic": "owner_escalation_triggers_packets_and_precedence",
      "definition": "Compose the escalation system: WHAT goes to the owner, with what evidence, and which trigger wins. Four trigger families: (1) credit-spend — any threshold from the accounting node fires (first Soul training, oversized single call, reel cap breach, balance floor; thresholds [ESTIMATE] until ratified); (2) likeness/policy — any likeness-gate flag, carrying the psych-boundary framing that audience 'this is AI' accusations will happen even on clean content (the research's Dunning-Kruger finding), so POLICY flags escalate but mere accusation-comments do not; (3) identity drift beyond tolerance with the retrain decision attached; (4) any real-person identifiability, which is family 2's hard core. Precedence when several fire: likeness/identifiability outranks everything and halts rather than queues; drift-beyond-tolerance halts its lineage; credit triggers pause new spend but let QC and post on already-generated assets continue. Every escalation is a packet — trigger, ledger-row evidence, halted/continuing state, specific decision requested — and every owner ruling is written back as a ledger event so the same question is not re-escalated. Escalation is a designed interface, not an apology: the system's autonomy is defined by exactly these boundaries.",
      "group": "governance",
      "dependencies": ["CREDIT_SPEND_ACCOUNTING", "LIKENESS_POLICY_GATE", "IDENTITY_DRIFT_TOLERANCE"],
      "competency_question_refs": ["CQ_11"],
      "scope_boundary": { "included": ["the four trigger families and their precedence", "the packet format and ruling write-back", "what continues vs halts under each trigger"], "excluded": ["threshold values themselves (accounting node, owner-ratified)", "the likeness triggers' definitions (likeness node)"] },
      "pros": [ { "claim": "Written precedence prevents the worst failure — likeness risk waiting in a queue behind a budget question", "example": "a Friday batch where a resemblance flag and a cap breach fire together and the resemblance halts posting while the cap merely pauses new spend", "weight": 0.84 } ],
      "cons": [ { "claim": "Over-broad triggers train the owner to skim escalations, dulling the channel exactly when a real one arrives", "example": "weekly cap-breach packets for expected overruns burying the one drift escalation that mattered", "weight": 0.6 } ],
      "failure_modes": ["escalating in chat prose without ledger evidence so the owner rules on a summary", "failing to write the ruling back, re-escalating the same threshold next reel"],
      "acceptance_tests": ["the matrix lists all four families, states likeness-outranks-all precedence with halt-vs-pause semantics per family, mandates evidence packets, and writes rulings back as ledger events"],
      "revisit_triggers": ["the owner ratifies thresholds or changes the precedence", "an escalation fires and the packet proves insufficient for a ruling", "escalation frequency indicates triggers are too tight or too loose"],
      "base": { "criticality": 0.9, "business_value": 0.85, "user_value": 0.78, "technical_complexity": 0.45, "risk_if_wrong": 0.85, "cross_topic_coupling": 0.85, "irreversibility": 0.6, "confidence": 0.78, "node_conflict_pressure": 0.55, "acceptance_test_pass_rate": 0.84, "dependency_gate_pass_rate": 0.82, "prior_importance": 0.9, "evidence_confidence": 0.74, "failure_rate": 0.14, "downside_weight": 0.75, "uncertainty_interval": [0.14, 0.38], "update_signal": "owner ratification, a fired escalation's post-mortem, or trigger-frequency data retunes the matrix" }
    }
  ],
  "edges": [
    { "from": "ASSET_NAMING_SCHEME", "to": "PRODUCTION_LEDGER", "edge_type": "dependency", "why_related": "ledger rows are keyed by the local id grammar, so the naming scheme must exist before the first row", "relation_strength": 0.85 },
    { "from": "ASSET_NAMING_SCHEME", "to": "PROMPT_VERSION_CONTROL", "edge_type": "dependency", "why_related": "prompt version ids are minted from the same grammar's versioning rule", "relation_strength": 0.78 },
    { "from": "ASSET_NAMING_SCHEME", "to": "PERSONA_REGISTRY_BOOTSTRAP", "edge_type": "dependency", "why_related": "registry rows are authored under local ids before any Soul or Element exists", "relation_strength": 0.76 },
    { "from": "ASSET_NAMING_SCHEME", "to": "REEL_BRIEF_INTAKE", "edge_type": "dependency", "why_related": "intake mints the reel id and slot ids from the grammar", "relation_strength": 0.72 },
    { "from": "PRODUCTION_LEDGER", "to": "PLATFORM_HANDLE_REGISTRY", "edge_type": "dependency", "why_related": "the handle registry is the ledger's foreign-key layer, so the canonical-ledger decision precedes it", "relation_strength": 0.8 },
    { "from": "PRODUCTION_LEDGER", "to": "CREDIT_SPEND_ACCOUNTING", "edge_type": "dependency", "why_related": "spend caps, cached preflight prices and reconciled actuals live as ledger rows and events", "relation_strength": 0.78 },
    { "from": "PERSONA_REGISTRY_BOOTSTRAP", "to": "SOUL_ASSET_LIFECYCLE", "edge_type": "dependency", "why_related": "Souls are trained against pre-authored registry rows, not improvised", "relation_strength": 0.82 },
    { "from": "PERSONA_REGISTRY_BOOTSTRAP", "to": "ELEMENT_ASSET_LIFECYCLE", "edge_type": "dependency", "why_related": "Elements (personas, wardrobe, locations) execute the bootstrap plan's row designs", "relation_strength": 0.82 },
    { "from": "PLATFORM_HANDLE_REGISTRY", "to": "SOUL_ASSET_LIFECYCLE", "edge_type": "dependency", "why_related": "Soul training consumes media_id/job_id handles and mints a soul_id the registry must map", "relation_strength": 0.76 },
    { "from": "PLATFORM_HANDLE_REGISTRY", "to": "ELEMENT_ASSET_LIFECYCLE", "edge_type": "dependency", "why_related": "Element creation consumes image handles and mints element_ids referenced as prompt tokens", "relation_strength": 0.76 },
    { "from": "ELEMENT_ASSET_LIFECYCLE", "to": "MODEL_ID_COLLISION_GUARD", "edge_type": "dependency", "why_related": "the worst documented collision (nano_banana display names) lives on the Elements routing path, so the guard is defined against the Element lifecycle", "relation_strength": 0.74 },
    { "from": "PROMPT_VERSION_CONTROL", "to": "SHOT_PROVENANCE_RECORD", "edge_type": "dependency", "why_related": "the provenance row's prompt field is a version id, which must exist to be referenced", "relation_strength": 0.82 },
    { "from": "PLATFORM_HANDLE_REGISTRY", "to": "SHOT_PROVENANCE_RECORD", "edge_type": "dependency", "why_related": "provenance rows join model outputs through job_id/media_id semantics the handle registry defines", "relation_strength": 0.8 },
    { "from": "SHOT_PROVENANCE_RECORD", "to": "KEYFRAME_CHAIN_PROVENANCE", "edge_type": "dependency", "why_related": "seam rows reference the frame handles that clip provenance rows carry", "relation_strength": 0.8 },
    { "from": "SHOT_PROVENANCE_RECORD", "to": "MODEL_FAILURE_LEDGER", "edge_type": "dependency", "why_related": "keep-rates and failure classes aggregate over provenance rows including killed generations", "relation_strength": 0.8 },
    { "from": "REEL_BRIEF_INTAKE", "to": "QC_GATE_CHECKPOINTS", "edge_type": "dependency", "why_related": "the gates attach to the reel's slot list and escalation flags declared at intake", "relation_strength": 0.72 },
    { "from": "QC_GATE_CHECKPOINTS", "to": "KEYFRAME_STILL_STAGE", "edge_type": "dependency", "why_related": "the stills stage promotes only checkpoint-1-passed frames, so the gate rules must be fixed first", "relation_strength": 0.8 },
    { "from": "SOUL_ASSET_LIFECYCLE", "to": "KEYFRAME_STILL_STAGE", "edge_type": "dependency", "why_related": "solo hero stills run on soul_id via the Soul-only models the lifecycle defines", "relation_strength": 0.76 },
    { "from": "ELEMENT_ASSET_LIFECYCLE", "to": "KEYFRAME_STILL_STAGE", "edge_type": "dependency", "why_related": "two-persona and wardrobe-bearing stills run on element tokens on compatible models", "relation_strength": 0.76 },
    { "from": "KEYFRAME_STILL_STAGE", "to": "VIDEO_GEN_STAGE", "edge_type": "dependency", "why_related": "video calls consume promoted keyframe handles; unpromoted stills never reach video spend", "relation_strength": 0.85 },
    { "from": "CREDIT_SPEND_ACCOUNTING", "to": "VIDEO_GEN_STAGE", "edge_type": "dependency", "why_related": "video batches run inside preflighted cost cells and the reel cap the accounting sets", "relation_strength": 0.8 },
    { "from": "KEYFRAME_CHAIN_PROVENANCE", "to": "VIDEO_GEN_STAGE", "edge_type": "dependency", "why_related": "seam-bearing slots pass chained handles per the seam rows, on both-frames models", "relation_strength": 0.76 },
    { "from": "VIDEO_GEN_STAGE", "to": "POST_ASSEMBLY_GRADE", "edge_type": "dependency", "why_related": "post consumes only kept, provenance-complete clips the video stage closes", "relation_strength": 0.82 },
    { "from": "MODEL_FAILURE_LEDGER", "to": "POST_ASSEMBLY_GRADE", "edge_type": "dependency", "why_related": "per-model trim presets from the failure ledger apply before hero-clip matching", "relation_strength": 0.72 },
    { "from": "POST_ASSEMBLY_GRADE", "to": "RELEASE_PACKAGING", "edge_type": "dependency", "why_related": "release packages the exported master and its recorded grade lineage", "relation_strength": 0.82 },
    { "from": "QC_GATE_CHECKPOINTS", "to": "IDENTITY_DRIFT_TOLERANCE", "edge_type": "dependency", "why_related": "drift detection rides the identity-lock item at the checkpoints the gate node places", "relation_strength": 0.78 },
    { "from": "SOUL_ASSET_LIFECYCLE", "to": "IDENTITY_DRIFT_TOLERANCE", "edge_type": "dependency", "why_related": "the drift baseline is the Soul's recorded training-image set, a lifecycle artifact", "relation_strength": 0.78 },
    { "from": "PERSONA_REGISTRY_BOOTSTRAP", "to": "LIKENESS_POLICY_GATE", "edge_type": "dependency", "why_related": "the first likeness review runs on the reference sheets the bootstrap plan authors", "relation_strength": 0.76 },
    { "from": "CREDIT_SPEND_ACCOUNTING", "to": "ESCALATION_MATRIX", "edge_type": "dependency", "why_related": "the credit trigger family fires on the thresholds the accounting node defines", "relation_strength": 0.8 },
    { "from": "LIKENESS_POLICY_GATE", "to": "ESCALATION_MATRIX", "edge_type": "dependency", "why_related": "likeness flags are the matrix's highest-precedence input", "relation_strength": 0.85 },
    { "from": "IDENTITY_DRIFT_TOLERANCE", "to": "ESCALATION_MATRIX", "edge_type": "dependency", "why_related": "beyond-tolerance drift escalates with the retrain decision attached", "relation_strength": 0.8 },
    { "from": "QC_GATE_CHECKPOINTS", "to": "CREDIT_SPEND_ACCOUNTING", "edge_type": "conflict", "why_related": "the gates demand re-rolls until clean while the accounting caps spend — quality appetite versus budget", "relation_strength": 0.72, "resolution_rule": "below the reel cap and thresholds, the QC gate wins and re-rolls run; at a threshold the credit trigger fires and the owner decides — the system never silently relaxes a gate to save credits, and never silently overspends to satisfy one" },
    { "from": "SOUL_ASSET_LIFECYCLE", "to": "ELEMENT_ASSET_LIFECYCLE", "edge_type": "conflict", "why_related": "Souls promise identity fidelity but reach only two image models with one Soul per generation; Elements reach most models and multi-persona shots but with unproven fidelity", "relation_strength": 0.7, "resolution_rule": "route solo hero stills through Souls on soul_2/soul_cinematic and all two-persona or non-Soul-model shots through Elements; revisit the split when the fidelity A/B lands, and never force a two-Soul shot the surface forbids" },
    { "from": "MODEL_FAILURE_LEDGER", "to": "VIDEO_GEN_STAGE", "edge_type": "conflict", "why_related": "the ledger argues for exploiting the known-best model per shot type while the owner's try-different directive argues for exploration", "relation_strength": 0.68, "resolution_rule": "repeat shot types default to the ledger winner; declared A/B exploration is reserved for new shot types, post-model-update revalidation, or a ledger keep-rate below par — exploration is a budgeted decision, not a habit" },
    { "from": "LIKENESS_POLICY_GATE", "to": "RELEASE_PACKAGING", "edge_type": "conflict", "why_related": "posting momentum and owner enthusiasm push toward publishing while an open likeness flag demands a halt", "relation_strength": 0.74, "resolution_rule": "the likeness flag always wins: nothing posts while any flag on any constituent asset is open, whatever the schedule cost — the closed lane admits no posting-deadline exception" },
    { "from": "PROMPT_VERSION_CONTROL", "to": "VIDEO_GEN_STAGE", "edge_type": "conflict", "why_related": "frozen prompt versions serve attribution while batch iteration wants mid-flight wording tweaks", "relation_strength": 0.64, "resolution_rule": "any wording change mints a new version id before the call — the tweak is allowed, the untracked tweak is not; batches reference frozen versions so A/B results stay attributable" },
    { "from": "VIDEO_GEN_STAGE", "to": "MODEL_FAILURE_LEDGER", "edge_type": "feedback", "why_related": "every batch's keep/kill outcomes and failure classes flow back into the per-model ledger that steers the next batch", "relation_strength": 0.78 },
    { "from": "QC_GATE_CHECKPOINTS", "to": "PROMPT_VERSION_CONTROL", "edge_type": "feedback", "why_related": "QC failure classes that implicate wording drive new prompt versions, closing the quality-to-prompt loop", "relation_strength": 0.72 },
    { "from": "ESCALATION_MATRIX", "to": "RELEASE_PACKAGING", "edge_type": "constraint", "why_related": "an open escalation on any constituent asset blocks the release gate until the owner rules", "relation_strength": 0.8 }
  ],
  "conflict_axes": [
    { "name": "qc_perfection_vs_credit_budget", "description": "Re-rolling until every rubric item is clean burns credits without bound, while hard caps tempt shipping visible tells; with per-generation prices [UNKNOWN] the tension cannot be priced away upfront.", "poles": ["re-roll until clean", "stay under the cap"], "resolution_hint": "QC wins below thresholds; a tripped threshold escalates rather than silently relaxing either side", "affected_nodes": ["QC_GATE_CHECKPOINTS", "CREDIT_SPEND_ACCOUNTING", "VIDEO_GEN_STAGE"] },
    { "name": "soul_fidelity_vs_element_flexibility", "description": "Souls are the identity-faithful path but reach two image models with one Soul per generation; Elements reach the broad catalog and two-persona shots with an unproven fidelity gap.", "poles": ["train and trust Souls", "reference everything as Elements"], "resolution_hint": "dual-track per persona (one Soul plus one Element) and let the first fidelity A/B set the production split", "affected_nodes": ["SOUL_ASSET_LIFECYCLE", "ELEMENT_ASSET_LIFECYCLE", "PERSONA_REGISTRY_BOOTSTRAP"] },
    { "name": "explore_models_vs_exploit_ledger", "description": "The owner's try-different directive rewards exploration while the failure ledger's whole value is exploiting accumulated winners.", "poles": ["A/B every shot", "always use the ledger winner"], "resolution_hint": "exploit on repeat shot types, explore as a budgeted declared decision on new types or after model updates", "affected_nodes": ["MODEL_FAILURE_LEDGER", "VIDEO_GEN_STAGE"] },
    { "name": "iteration_speed_vs_provenance_discipline", "description": "Fast creative iteration wants to tweak and re-fire; complete provenance wants every call versioned, preflighted and rowed before it runs.", "poles": ["move fast, log later", "no unrowed call ever"], "resolution_hint": "make logging cheap (templates, write-at-submit) rather than optional; the untracked tweak, not the tweak, is the violation", "affected_nodes": ["PROMPT_VERSION_CONTROL", "SHOT_PROVENANCE_RECORD", "VIDEO_GEN_STAGE"] },
    { "name": "posting_cadence_vs_policy_hold", "description": "Feed momentum and owner enthusiasm reward shipping on schedule, while open likeness flags, drift quarantines, or escalations demand holds of unpredictable length.", "poles": ["ship on schedule", "hold until cleared"], "resolution_hint": "policy holds always win; protect cadence by keeping a cleared-reel buffer, not by shortening reviews", "affected_nodes": ["LIKENESS_POLICY_GATE", "RELEASE_PACKAGING", "ESCALATION_MATRIX"] },
    { "name": "local_ledger_vs_platform_surfaces", "description": "Duplicating bookkeeping locally costs discipline, but the platform's history surfaces have [UNKNOWN] retention/query semantics and cannot hold prompt versions or QC verdicts.", "poles": ["trust show_generations/transactions", "trust only the local ledger"], "resolution_hint": "local ledger is canonical; platform surfaces are reconciliation inputs whose semantics get documented as they are exercised", "affected_nodes": ["PRODUCTION_LEDGER", "PLATFORM_HANDLE_REGISTRY", "CREDIT_SPEND_ACCOUNTING"] },
    { "name": "registry_upfront_design_vs_first_output_speed", "description": "Designing registry rows, naming and caps before generating delays the first visible reel, which owner enthusiasm punishes.", "poles": ["bootstrap fully first", "generate now, organize later"], "resolution_hint": "bootstrap is one session's work and every skipped row is paid back with interest as orphaned lineage; show the owner the plan as the first deliverable", "affected_nodes": ["PERSONA_REGISTRY_BOOTSTRAP", "ASSET_NAMING_SCHEME", "REEL_BRIEF_INTAKE"] },
    { "name": "autonomy_vs_owner_interrupts", "description": "Tight escalation triggers keep the owner in control but interrupt them constantly; loose triggers preserve flow but let spend or policy risk run unsupervised.", "poles": ["escalate everything", "escalate almost nothing"], "resolution_hint": "escalate on the four families only, batch non-urgent packets, write rulings back so the same question never re-escalates", "affected_nodes": ["ESCALATION_MATRIX", "CREDIT_SPEND_ACCOUNTING", "IDENTITY_DRIFT_TOLERANCE"] },
    { "name": "asset_reuse_vs_fresh_generation", "description": "Reusing archived keyframes, Souls and prompt versions compounds their value and consistency, but stale assets can lock in an early look or a drifted baseline.", "poles": ["reuse the archive", "regenerate fresh"], "resolution_hint": "reuse anything whose QC verdict and baseline still stand; a retrain or house-look change stales the affected archive explicitly rather than by decay", "affected_nodes": ["KEYFRAME_CHAIN_PROVENANCE", "SOUL_ASSET_LIFECYCLE", "POST_ASSEMBLY_GRADE"] }
  ],
  "edge_cases": [
    { "description": "The catalog carries duplicate model ids (soul_2 and soul_v2, identical schemas), and per-model statistics split across the two spellings", "trigger": "operators alternate ids across batches", "affected_nodes": ["MODEL_ID_COLLISION_GUARD", "MODEL_FAILURE_LEDGER"], "mitigation": "canonicalize to soul_2 in the ledger with soul_v2 recorded as alias; merge any split history on discovery", "severity": "medium" },
    { "description": "An Elements-routed generation may execute on a different engine than the id suggests (nano_banana_2 displays as 'Nano Banana Pro'; nano_banana_flash is catalog-absent), corrupting attribution", "trigger": "the first element-token batch runs before routing is resolved", "affected_nodes": ["MODEL_ID_COLLISION_GUARD", "SHOT_PROVENANCE_RECORD"], "mitigation": "instrument the first element-routed generation to record requested vs reported ids and hold per-model conclusions on element batches until routing is resolved", "severity": "medium" },
    { "description": "Soul training cost is [UNKNOWN] and training cannot be get_cost-preflighted the way generate_* calls can, so the first training is a blind spend", "trigger": "the bootstrap plan reaches its first Soul training", "affected_nodes": ["SOUL_ASSET_LIFECYCLE", "CREDIT_SPEND_ACCOUNTING", "ESCALATION_MATRIX"], "mitigation": "escalate before the first training with the balance snapshot; record the observed cost as the cached price for future trainings", "severity": "high" },
    { "description": "A persona generation is requested while the registry is still empty (the workspace's actual state), leaving identity lineage nowhere to attach", "trigger": "owner enthusiasm demands output before bootstrap completes", "affected_nodes": ["PERSONA_REGISTRY_BOOTSTRAP", "REEL_BRIEF_INTAKE"], "mitigation": "intake blocks on unresolved registry refs; offer the owner the bootstrap plan plus a non-persona test reel as the immediate deliverable", "severity": "high" },
    { "description": "A generation job fails after its provenance row was opened, or a job_id later fails to resolve, leaving orphan rows", "trigger": "job failure, retention expiry, or workspace switch between submit and reconcile", "affected_nodes": ["SHOT_PROVENANCE_RECORD", "PLATFORM_HANDLE_REGISTRY"], "mitigation": "reconcile open rows against job_display after every batch; orphaned rows move to a failed state with the error recorded, never deleted", "severity": "medium" },
    { "description": "A two-persona shot is planned with two soul_ids, which the surface forbids (ONE soul_id per generation)", "trigger": "a shot plan treats Souls as freely combinable", "affected_nodes": ["SOUL_ASSET_LIFECYCLE", "ELEMENT_ASSET_LIFECYCLE", "KEYFRAME_STILL_STAGE"], "mitigation": "route all two-persona shots through multiple element tokens on an element-compatible model; the intake check catches the plan before spend", "severity": "high" },
    { "description": "Identity drift is first noticed at the post-edit phone check, after still, video and grading spend are already sunk", "trigger": "subtle per-clip drift accumulates across the reel below single-clip thresholds", "affected_nodes": ["IDENTITY_DRIFT_TOLERANCE", "QC_GATE_CHECKPOINTS", "POST_ASSEMBLY_GRADE"], "mitigation": "enforce the identity-lock item at checkpoint 1 against the reference sheet, and add a cross-clip side-by-side review before assembly on persona-heavy reels", "severity": "high" },
    { "description": "A kept clip is discovered without a prompt version (an untracked manual tweak produced it), making its look unreproducible", "trigger": "batch momentum bypassed the version-minting rule", "affected_nodes": ["PROMPT_VERSION_CONTROL", "SHOT_PROVENANCE_RECORD"], "mitigation": "quarantine the clip; backfill the version from call logs if recoverable, else regenerate from the nearest recorded version before the clip anchors further work", "severity": "medium" },
    { "description": "The credit balance drops more than the ledger's recorded spend explains", "trigger": "parallel workspace activity, plan renewal, or an unrecorded call", "affected_nodes": ["CREDIT_SPEND_ACCOUNTING", "ESCALATION_MATRIX"], "mitigation": "reconcile against the transactions surface, freeze new spend until the gap is explained, and escalate with the discrepancy packet if it is not", "severity": "high" },
    { "description": "A generated persona turns out to resemble an identifiable real person even though nothing in the prompt asked for it", "trigger": "stochastic generation lands near a real face; a comment or reviewer names the person", "affected_nodes": ["LIKENESS_POLICY_GATE", "ESCALATION_MATRIX", "RELEASE_PACKAGING"], "mitigation": "halt the lineage, flag all touched rows, escalate with the resemblance evidence; if already posted, the owner's ruling covers takedown — the closed lane offers no restyle-and-continue shortcut", "severity": "high" },
    { "description": "An upstream clip is re-rolled after downstream clips were chained on its end_image job_id, silently invalidating the stitch", "trigger": "a late QC failure or owner note forces a re-roll mid-assembly", "affected_nodes": ["KEYFRAME_CHAIN_PROVENANCE", "VIDEO_GEN_STAGE"], "mitigation": "stale-marking fires on the re-roll and blocks assembly until every downstream seam is re-linked or regenerated", "severity": "medium" },
    { "description": "The wardrobe-module library is built out before the Souls-vs-Elements fidelity gap [UNKNOWN] is tested, and the chosen mechanism underperforms", "trigger": "registry enthusiasm outruns the scheduled first A/B", "affected_nodes": ["ELEMENT_ASSET_LIFECYCLE", "PERSONA_REGISTRY_BOOTSTRAP", "IDENTITY_DRIFT_TOLERANCE"], "mitigation": "cap the pre-A/B library at the minimum viable set (one wardrobe module per persona) and gate expansion on the recorded A/B verdict", "severity": "medium" }
  ],
  "workflow": [
    { "action": "Fix the naming grammar and stand up the production ledger", "node_ref": "ASSET_NAMING_SCHEME", "description": "Author the local id grammar for all five asset classes plus reels, and open the append-only ledger keyed on it with platform handles as foreign keys", "artifact": "naming convention + empty ledger with row schema", "gate": "every asset class has an id form, versioning is monotone, and platform handles are foreign-key fields only" },
    { "action": "Author the persona-asset registry bootstrap plan", "node_ref": "PERSONA_REGISTRY_BOOTSTRAP", "description": "Design rows from the verified-empty workspace: one Soul plus one Element per persona, wardrobe as prop/character Elements, locations as environment Elements, each with reference-image lineage", "artifact": "registry with authored-before-created rows", "gate": "both personas have Soul and Element rows, wardrobe/location rows exist, and the likeness review of reference sheets is recorded" },
    { "action": "Create the identity assets and record their handles", "node_ref": "SOUL_ASSET_LIFECYCLE", "description": "Escalate the first (unknown-cost) Soul training, then train Souls and create Elements per the plan, writing soul_id/element_id and source-image handles to their rows", "artifact": "populated registry rows with platform handles", "gate": "each created asset's row carries its handle, source lineage and status; the training cost is cached; no creation ran without its row" },
    { "action": "Intake the reel brief into a tracked production spec", "node_ref": "REEL_BRIEF_INTAKE", "description": "Mint the reel id, resolve slots and registry refs, set the spend cap, and declare escalation-sensitive flags", "artifact": "reel spec with slot list, cap and flags", "gate": "no unresolved registry refs, a cap is set, and likeness/new-Soul/untested-cost flags are explicit" },
    { "action": "Preflight every untested cost cell for the reel", "node_ref": "CREDIT_SPEND_ACCOUNTING", "description": "Run get_cost:true on each new model x resolution x duration cell the plan touches and cache results in the ledger against the cap", "artifact": "priced cost table for the reel", "gate": "every planned call has a cached preflighted price and the summed plan fits the cap or has escalated" },
    { "action": "Generate and QC the keyframe stills, seam pairs as pairs", "node_ref": "KEYFRAME_STILL_STAGE", "description": "Produce start/end stills per slot on the correct identity path, run checkpoint 1, and match seam pairs on position/brightness/color/blur direction", "artifact": "promoted keyframe set with QC scores", "gate": "only checkpoint-1-passed stills and matched pairs are promoted; failures re-rolled at still prices" },
    { "action": "Run the video batches with full provenance", "node_ref": "VIDEO_GEN_STAGE", "description": "Call ledger-chosen models in-band with chained keyframes, opening provenance rows at submit and closing them at keep/kill with failure classes", "artifact": "kept clips with complete provenance rows", "gate": "every call preflighted where new, every output rowed, re-roll counts recorded, killed clips retained as evidence" },
    { "action": "Gate raw video at checkpoint 2 including identity lock", "node_ref": "QC_GATE_CHECKPOINTS", "description": "Score each clip on the rubric, enforce the single-2 and total>=4 re-roll rules, and check drift against the reference sheet", "artifact": "written QC verdicts on every provenance row", "gate": "no clip proceeds unscored; re-roll rules enforced; any beyond-tolerance drift quarantines and escalates" },
    { "action": "Assemble, match to the hero clip, and export the master", "node_ref": "POST_ASSEMBLY_GRADE", "description": "Beat-lock, apply per-model trims then hero matching then the house look, QC cut boundaries, export 1080x1920 and run the phone-screen checkpoint", "artifact": "graded master with grade lineage and boundary verdicts", "gate": "only kept clips assembled; boundary frames within the match window; phone-screen pass recorded" },
    { "action": "Package and release the reel", "node_ref": "RELEASE_PACKAGING", "description": "Record cover choice, post at highest quality, verify live playback and crops, and write the release row linking published output to provenance", "artifact": "release row with master hash and provenance links", "gate": "no open escalation/flag/quarantine on any constituent asset; verification checks pass; the row is complete" },
    { "action": "Sweep escalation triggers and update the ledgers", "node_ref": "ESCALATION_MATRIX", "description": "Close the reel by checking all four trigger families, sending any packets with ledger evidence, writing owner rulings back, and folding batch outcomes into the failure ledger", "artifact": "escalation packets and updated failure-ledger entries", "gate": "no unsent trigger, no unrecorded ruling, and the failure ledger reflects every batch" }
  ],
  "dominance_rules": [
    { "rule": "A likeness/identifiability flag dominates every other consideration", "rationale": "the lane is closed by policy and the harm is irreversible once posted", "trigger": "any real-person resemblance, request, or consent-less reference surfaces at any stage", "action": "halt the lineage, flag all touched rows, escalate with evidence; nothing on that lineage generates, polishes or posts until the owner rules" },
    { "rule": "The registry bootstrap dominates the first persona generation", "rationale": "the workspace is verifiably empty, and identity lineage generated before rows exist is orphaned from day one", "trigger": "a persona generation is requested before its registry rows exist", "action": "block at intake and deliver the bootstrap plan (or a non-persona test) instead" },
    { "rule": "get_cost preflight dominates any untested-cost call", "rationale": "every per-generation price is [UNKNOWN] on the read surface and the probe is free", "trigger": "a call touches a model x resolution x duration cell with no cached price", "action": "preflight, cache the price, then submit — or escalate if the price breaks the cap" },
    { "rule": "Provenance completeness dominates keeping a clip", "rationale": "an unreproducible clip cannot be regraded, re-rendered, audited or defended later", "trigger": "a clip is about to be kept with any missing provenance field", "action": "quarantine until backfilled from evidence or regenerate; incomplete rows never enter timelines" },
    { "rule": "QC-before-polish dominates upscale/post spend", "rationale": "polishing an ungated clip spends credits on output that may be re-rolled anyway", "trigger": "an upscale or post operation is queued for a clip without a written checkpoint-2 verdict", "action": "run the gate first; spend polish credits only on gated keeps" },
    { "rule": "The credit threshold dominates re-roll appetite", "rationale": "with prices unknown and the balance finite (1,010 at enumeration), unbounded quality-chasing is the likeliest silent failure", "trigger": "re-roll counts or summed spend reach a threshold or the reel cap", "action": "pause new spend, keep QC/post running on existing assets, and escalate with the spend packet" },
    { "rule": "The reference-sheet baseline dominates adjacent-clip comparison for drift", "rationale": "measuring drift clip-to-clip lets it ratchet; the training images are the fixed truth", "trigger": "an identity-lock check is about to compare against the previous clip only", "action": "compare against the Soul/Element source images recorded in the registry, then check accumulation at phone QC" },
    { "rule": "Exact model id strings dominate display names everywhere in the ledger", "rationale": "the surface's documented naming collisions make display names actively misleading", "trigger": "any row, ledger entry or report is about to reference a model by display name", "action": "record the exact id string, canonicalize known aliases, and instrument element-routed calls until routing resolves" },
    { "rule": "The local ledger dominates platform history surfaces on any disagreement", "rationale": "the platform surfaces have [UNKNOWN] semantics and cannot carry prompt versions or QC verdicts", "trigger": "show_generations/transactions and the ledger disagree about an asset or a spend", "action": "treat the ledger as canonical, investigate the gap, and record the resolution as a ledger event" }
  ],
  "anti_rework_rules": [
    { "rule": "Fix the naming grammar and ledger schema before the first generation call", "prevents": "a mass rename and lineage reconstruction across hundreds of assets once ad-hoc names collide" },
    { "rule": "Write provenance at submit time, never retrospectively", "prevents": "unreconstructable kept clips, because no platform surface archives prompt versions or QC verdicts to backfill from" },
    { "rule": "QC stills and seam pairs before any video call", "prevents": "paying video prices to discover composition, identity or pair-match failures that still prices would have caught" },
    { "rule": "Preflight each new cost cell with get_cost before first use", "prevents": "a single mispriced batch consuming a visible fraction of the 1,010-credit balance and forcing an emergency owner conversation" },
    { "rule": "Create both personas' Elements before planning any two-persona shot", "prevents": "replanning a reel mid-production around the one-Soul-per-generation limit discovered at generation time" },
    { "rule": "Mint the prompt version before the batch, not after the keeper is found", "prevents": "unattributable A/B results that force re-running the comparison to learn which wording won" },
    { "rule": "Run the identity-lock check at checkpoint 1 against the reference sheet", "prevents": "sinking video, grading and assembly spend into a drifted persona caught only at the phone-screen pass" },
    { "rule": "Record per-model failure classes and trim presets from the first batches onward", "prevents": "re-discovering each model's quirks and color signature on every subsequent reel" },
    { "rule": "Review reference sheets for likeness before any Soul trains or Element is created", "prevents": "retiring a trained persona and its whole archive because the resemblance was caught after production scaled" }
  ],
  "iteration_protocol": [
    { "trigger": "a likeness or real-person-identifiability flag fires anywhere in production", "action": "halt the lineage, flag all touched ledger rows, send the evidence packet, and record the owner's ruling as a ledger event before any affected work resumes", "nodes": ["LIKENESS_POLICY_GATE", "ESCALATION_MATRIX", "RELEASE_PACKAGING"], "priority": 1 },
    { "trigger": "credit spend reaches a threshold, the reel cap, or an unexplained balance gap appears", "action": "pause new spend, reconcile against balance/transactions, and escalate with the spend packet; resume only under a recorded ruling", "nodes": ["CREDIT_SPEND_ACCOUNTING", "ESCALATION_MATRIX"], "priority": 1 },
    { "trigger": "identity drift beyond tolerance is confirmed on any clip or accumulating across a reel", "action": "quarantine the lineage, diagnose Soul vs Element vs prompt, and escalate with the retrain/re-create decision; a retrain mints new rows and stales the old baseline's archive", "nodes": ["IDENTITY_DRIFT_TOLERANCE", "SOUL_ASSET_LIFECYCLE", "ESCALATION_MATRIX"], "priority": 1 },
    { "trigger": "the first Souls-vs-Elements fidelity A/B completes", "action": "record the verdict, set the production split between the two identity paths, and unlock or cap the wardrobe-library buildout accordingly", "nodes": ["SOUL_ASSET_LIFECYCLE", "ELEMENT_ASSET_LIFECYCLE", "PERSONA_REGISTRY_BOOTSTRAP"], "priority": 2 },
    { "trigger": "the first instrumented element-routed generation resolves (or deepens) the model-id routing question", "action": "update the collision guard's canonical map, and re-attribute or annotate any element-batch entries in the failure ledger", "nodes": ["MODEL_ID_COLLISION_GUARD", "MODEL_FAILURE_LEDGER"], "priority": 2 },
    { "trigger": "the failure ledger shows a repeated failure class or a keep-rate collapse for a model x shot-type cell", "action": "switch that shot type's default model, note the deprecation in the ledger, and re-run a budgeted A/B only if no cell performs", "nodes": ["MODEL_FAILURE_LEDGER", "VIDEO_GEN_STAGE"], "priority": 2 },
    { "trigger": "a provenance gap, orphan row, or ledger/platform mismatch is discovered in an audit or reconcile pass", "action": "quarantine affected assets, backfill from job_display/transactions evidence where possible, and patch the logging hole that produced the gap", "nodes": ["SHOT_PROVENANCE_RECORD", "PRODUCTION_LEDGER", "PLATFORM_HANDLE_REGISTRY"], "priority": 2 },
    { "trigger": "a later surface enumeration changes the catalog (new models, resolved element-compatibility list, exercised history-surface semantics, or plan change)", "action": "re-run the affected registry and ledger schema checks, update cached constraints (both-frames list, duration bands, compatible models), and re-price impacted cost cells", "nodes": ["PLATFORM_HANDLE_REGISTRY", "ELEMENT_ASSET_LIFECYCLE", "CREDIT_SPEND_ACCOUNTING"], "priority": 3 }
  ],
  "priority_rationale": "The foundations (naming grammar, canonical ledger, prompt versioning, platform-handle semantics) come first because every registry row, provenance record and escalation packet is keyed on them; the registry trio (bootstrap plan, Soul lifecycle, Element lifecycle, collision guard) comes next because the workspace is verifiably empty and identity assets must exist with recorded lineage before any persona generation; the provenance layer (clip rows, seam chains, failure ledger, credit accounting) precedes the pipeline stages because rows are written AT generation time, not after; the pipeline stages (intake, QC gates, stills, video, post, release) then execute in cost order — cheapest failure first; and the governance nodes (likeness gate, drift tolerance, escalation matrix) bind across all stages, with the likeness closed lane outranking everything because its failures are the only irreversible ones.",
  "eval_objective": "verify_core_properties_of_director_production_ops_kb"
}
