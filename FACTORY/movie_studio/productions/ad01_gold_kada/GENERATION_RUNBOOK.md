# GENERATION RUNBOOK — `ad01_gold_kada` (execute when Higgsfield reconnects)

_Concrete ordered steps with the CHOSEN assets baked in. Full per-shot prompts live in `parts/10_generation_prompts.md`; this is the execution checklist. Every video call is `get_cost`-preflighted (free) before spend. Cut = 7 beats, ~25s, 9:16, VO-driven, gold-PLATED honesty rules (no solid-gold implication)._

## Chosen assets (locked)
- **Presenter A** — job `4e74d9c4-8f18-4cbc-9d0e-20c41a0d6656`, **seed 246906** (reuse this seed for identity).
- **Chain A** — job `295bf33f-237b-46c9-8783-1578d64b2a62` (Cuban-curb, gold-plated).
- **Kada** — the owner's real piece. Two paths: (a) owner uploads the photo via `media_upload_widget`/`media_upload`→`media_confirm` (best fidelity to the exact piece), OR (b) generate a representative kada from the observed description in `parts/00` (matches the style; label as representative render). Owner chooses.
- **Facts:** brand rhodiumgold · @rhodiumgold · 24k gold PLATING (never "solid gold").

## Step 0 — PREFLIGHT (free, answers "how many credits")
`get_cost:true` on the video candidates at our durations, 9:16:
- `seedance_2_0` (primary) — 4s and 3s, mode:std, 1080p
- `kling3_0` (alt) — pro, 4s/3s
- `veo3_1` (alt) — 4s/3s
Report the line-item total (7 clips + ~1.5–2× retries + upscales) BEFORE spending. Confirm balance covers it.

## Step 1 — Presenter identity (cheap)
Make a reusable `<<<presenter>>>` Reference Element from Presenter A (job above). Verify face matches on one solo still (seed 246906). This carries him across shots 2 & 6 on element-compatible models.

## Step 2 — Product references
- Chain: promote Chain A to `<<<chain>>>` element (or use its job as image-reference).
- Kada: per owner choice in "Chosen assets" — upload OR generate representative hero.

## Step 3 — Start frames (nano_banana_pro 4K product / cinematic_studio_2_5 for presenter+product), per shot:
1. Hook — kada on wrist, diamond-cut flare (kada ref)
2. Reveal — presenter (element) wearing kada + chain (kada + chain refs)
3. Band macro — kada texture (kada ref)
4. Terminal macro — kada motif (kada ref)
5. Chain hero macro — chain A (chain ref)
6. Authenticity — presenter (element) beside kada on stand
7. CTA — product(s) at rest

## Step 4 — Video clips (seedance_2_0 primary; both-frame where seamed), per shot durations 4/4/4/3/3/4/3s.
Generate → run realism QC (parts/70): gold color, believable specular, link/band physics, identity lock, no hands fumbling, no fabricated hallmark. Re-roll fails. Keep winners; log job ids to ASSETS.md.

## Step 5 — Post (outside Higgsfield)
Stitch per parts/50 (cut-on-beat, transitions), cross-model color-match grade, add VO (rhodiumgold script, parts/00 §5) + licensed music (parts/40) + composited CTA card "@rhodiumgold · link in bio" (never model-generated text). Pick cover (parts/50).

## Step 6 — Publish gate (parts/60)
Meta AI-info label ON · honest caption (24k gold-plated, no hallmark) · watermark-clean · likeness stop-check items 1–2 = NO.

## Blocking gate before Step 4 spend
Owner: (a) confirm kada path (upload vs representative render), (b) confirm the preflighted total is within budget.
