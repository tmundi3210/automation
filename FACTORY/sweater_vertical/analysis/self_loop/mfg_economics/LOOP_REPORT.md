# Manufacturing-Economics Specialist — Self-Improvement Loop Report

**Slug:** `mfg_economics` | **Segment:** T12 | **Rounds run:** 2 (max) | **Generated:** 2026-07-01

---

## Health verdict (one line)

**CONVERGED — HEALTHY-WITH-HARDENING.** The mfg_economics specialist is honest, in-lane and correct on every load-bearing joint; no HIGH-severity finding survives. Three mechanical corrections (`apply_now`) surface or bind facts that already live in the specialist or its grounded KBs — a breakeven stand-in copy-up, an "A2-floor advantage dies at the gate" caveat, and an iteration-cell-linker bright-line — and three genuine surfacing/routing gaps are backlogged (`propose`) because closing them adds interpretive weight or a cross-lane routing note the honesty binding forbids fabricating past.

---

## Round-by-round story

- **Round 1** fired 12 adversarial questions at the load-bearing joints (viability deferral, the single Vardhman-54% anchor, bounding a band around the [UNKNOWN] knit+LINK rate, the FF-costing 60% ratio, the B1-vs-B2 lean, the own-knitting breakeven, the iteration-loop vs bulk boundary, the dye-lot-MOQ arithmetic, off-paper/inverted-duty handling, the two acrylic penalties, EPF/ESI vs off-paper, and falsifiability). **Five passed cleanly** (Q3 band-around-[UNKNOWN], Q5 B2-lean, Q7 iteration-loop carve-out, Q10 US-duty vs GST-inversion with cotton ~16.5%, Q12 falsifiable verdict). One `apply_now` (Q1 GATED-posture pointer), five MED/LOW `propose`/`watch`, one LOW overclaim (Q4). `dry=false` → a second round was warranted.
- **Round 2** re-aimed 10 sharper questions at the four survivors plus fresh angles the KBs confess (A2-as-floor duty inversion, the applied Q1 pointer under an anti-A3 stress case, the iteration-cell used-linker equipment, the two-season supply/demand squeeze, the EPF/ESI-on-a-tiny-sliver arithmetic, and the parked-vs-decided meta-audit). The specialist answered self-critically: **four clean OK** (R2Q6 A3-cost-orthogonal-to-A3-hero, R2Q9 EPF/ESI on-roll-only, R2Q10 rate-independent-verdicts, R2Q4 dissolved to ok). Round 2 sharpened the former Q6 breakeven gap into a **mechanical `apply_now`** (the worked stand-in is a verbatim copy-up from KB2, not authored content), surfaced two new `apply_now` (A2-floor caveat, iteration-cell-linker bright-line), and confirmed two carried `propose` gaps (micro-uplift binding, compliant-core flag) plus one new `propose` (seasonal demand-window routing).
- **Stop condition:** `max_rounds=2` reached with MED findings still open (`dry=false`). Per the synthesizer rule (`converged=true` unless a **HIGH**-severity finding is open at the cap), and since **no HIGH finding is open** — every residual is a self-sufficiency / surfacing / artifact-binding hole, never a wrong or fabricated answer — this report records **converged=true** with a real health grade.

---

## Findings table (deduped across rounds)

| ref | type | severity | evidence | fix | disposition |
|---|---|---|---|---|---|
| M1 (R1-Q06 / R2-Q01) | gap | MED | Own-knitting breakeven asserts `>1.0` at the spec surface; the worked stand-in `owned_fixed ~$7,500/mo / (saving ~$5 * capacity ~800) = 1.875` lives only in `KB2 OWN_KNITTING_BREAKEVEN`. Spec summary shows a claim of arithmetic with no self-contained case. | Inline the KB2 placeholder verbatim, keeping the input as the SAVING band and the [UNKNOWN] rate never quoted. Copy-up, no new value. | **apply_now** |
| M2 (R2-Q05) | gap | MED | `glossary 'per-article ex-factory bands'` calls A2 "the floor" bare; the same article is the ~32% MMF US-duty outlier (T9's) and the inverted-duty/GST refund-drag victim, so its ex-factory lead does not survive the hand-off. Invites an "acrylic wins" mis-read. | Attach a standing caveat ("ex-factory FLOOR only; advantage dies at the gate; landed ranking is unit_economics'") using only in-file facts (`boundaries[2]` + glossary 'inverted-duty / GST drag'). No ~32% leak. | **apply_now** |
| M3 (R2-Q07) | gap | MED | The `KB2 'own the iteration loop'` equipment list includes a used LINKER (~$500-4,000) — the same machine class as the linking bottleneck the ladder in-sources LAST — with no bright-line, so the speed-axis carve-out could authorise owning the scarce linker early through the sampling-cell door. Untested in R1. | Add the bright-line from existing KB2 content: iteration-cell linker links sample/PPS/proto only; the instant it links saleable bulk it must clear the `LINKING_LAST` gate; capacity remains `knit_machine_strategist`'s lane; build=NEVER. | **apply_now** |
| M4 (R1-Q08 / R2-Q02) | gap | MED | The band is the T9 deliverable but nothing BINDS the micro-volume x1.3-2 uplift to it. At a 4-colour ~150-pc launch true cost is midpoint ~$5.75 x1.3-2 -> ~$7.5-11.5, TOP BREACHING the $8 A2 band top; a bare band understates a launch-scale run by up to ~2x. | Add a self-labelling hand-off rule: bands are a FLOOR at repeat volume; the launch figure to T9 MUST be the volume-tier-adjusted band with its uplift, never bare. Binds an existing KB3 number but adds semantic weight. | **propose** |
| M5 (R1-Q11 / R2-Q03) | gap | MED | The compliant-core-vs-off-paper structural cost gap is real, [UNKNOWN], and Ludhiana's authority, but present only implicitly inside the `KB1 'compliance loading'` line — not named-and-deferred as a first-order feature of the ex-factory economics. | Add an explicit named flag: compliant core vs partly-off-paper base, saving embedded in the [UNKNOWN] job-work rate (no double-count), magnitude [UNKNOWN], mechanism-not-accusation, authority `ludhiana_sourcing_strategist`. New named flag + cross-lane deferral. No magnitude invented. | **propose** |
| M6 (R2-Q08) | gap | LOW | The Jul-Oct supply-peak caution is one-sided: it names the Ludhiana SUPPLY peak but not that the warm-California DEMAND window is a separate un-owned half whose non-alignment with the ~6-12 wk conversion + ocean lead can leave working-capital timing un-closeable. | Have the seasonal caution hand the demand-window-timing half to brand_market/unit_economics and name the supply-avoid + warm-demand-window non-alignment as an un-sized scheduling squeeze. No demand number invented. | **propose** |
| M7 (R1-Q04 / R2-Q04) | overclaim→ok | LOW | R1 flagged the FF ~60% link:knit ratio as "transferable shape"; on re-exam vs `KB2 LINKING_LAST` the linking-last SEQUENCING rests on THREE ratio-free mechanisms (throughput ceiling, hardest re-hire, QC-critical seam), so the finding DISSOLVES. | Optional one-line: state the linking-last sequencing does NOT depend on the LOW-MED ratio (used for cost SHAPE only). Clarity, not correction. | **watch** |
| M8 (R2-Q09) | ok/watch | LOW | EPF/ESI ~15%+ correctly on the on-roll portion only (no double-count with the job-worker's formality in the [UNKNOWN] rate), but the spec does not say it is therefore near-negligible at a subcontract launch and materially a FUTURE owned-stage cost. | Optional one-clause clarity add to pre-empt a base-case-launch-driver mis-read. Principle already present. | **watch** |
| M9 (R1-Q01 / R2-Q06) | ok/watch | LOW | The GATED-conditional-GO / G1-G4 framing is a brain/DECISION_MEMO construct imported at the router layer (N7), not native to the mfg spec. R1's Q1 apply_now added an in-spec pointer; R2Q6 verified it does real work (holds "A3 costs more ex-factory" orthogonal to the A3-hero config). | Already applied in R1. Recorded so the inherited-vs-native posture is not re-flagged. | **watch** |

---

## apply_now (3 — mechanical, must re-gate ALL GREEN via build.sh)

1. **M1 — Breakeven stand-in copy-up.** On `capabilities[3].method` (or the glossary breakeven entry): inline the KB2 placeholder `[ESTIMATE, LOW-MED] owned_fixed ~$7,500/mo / (saving ~$5/sweater * capacity ~800/mo) = 1.875 > 1.0 → subcontract wins (utilisation starved)`, with the input explicitly the SAVING band, never the [UNKNOWN] rate. Numbers already exist verbatim in `KB2 OWN_KNITTING_BREAKEVEN`.
2. **M2 — A2-floor caveat binding.** On glossary `'per-article ex-factory bands'`: attach "A2 is the ex-factory FLOOR only; the advantage does not survive the hand-off — acrylic/MMF ~32% US duty (T9) + A2-specific inverted-duty/GST refund drag (magnitude [UNKNOWN]); landed ranking is unit_economics'." Uses only existing facts; keeps the ~32% out of the band.
3. **M3 — Iteration-cell linker bright-line.** On `boundaries[4]` / `conflicts_and_dominance[5]`: an iteration-cell used linker links only sample/PPS/proto pieces, never a unit entering the saleable order; the moment it links saleable bulk it is in-sourced bulk linking that must clear the `LINKING_LAST` gate; linker CAPACITY remains `knit_machine_strategist`'s lane; build=NEVER intact.

*Re-gate rule: after applying, re-run `bash FACTORY/build.sh FACTORY/sweater_vertical/specialists/mfg_economics` → ALL GREEN, and confirm no ~7% duty residue and no new untagged number.*

## propose (3 — add interpretive/routing weight; backlog, never fabricate)

- **M4** — Bind the micro-volume x1.3-2 uplift to the band as a single hand-off artifact (bands are a FLOOR; the launch figure to T9 must carry its uplift).
- **M5** — Add an explicit named compliant-core-vs-off-paper structural-cost-gap flag with cross-lane deferral to `ludhiana_sourcing_strategist`, magnitude [UNKNOWN].
- **M6** — Have the Jul-Oct seasonal caution route the warm-California demand-window-timing half to brand_market/unit_economics and name the supply/demand non-alignment as an un-sized squeeze.

## watch (3 — recorded so they are not rediscovered)

- **M7** — FF 60% link:knit ratio: overclaim DISSOLVED (linking-last is ratio-free); optional one-line clarifier.
- **M8** — EPF/ESI on-roll-only is near-negligible at launch / a FUTURE owned-stage cost; optional clarity clause.
- **M9** — In-spec GATED-posture pointer already applied in R1 and verified substantive; inherited-vs-native N7 posture is a design fact, not an error.

---

## Verified strengths (the clean 'ok' answers — balance, not just defects)

1. **Viability deferral (R1-Q01, R2-Q06).** Refuses the number-anchored go/no-go read, defers to unit_economics + DECISION_MEMO, carries the GATED-conditional-GO posture and the four hard gates; a good-looking A2 factory cost is explicitly not a GO signal. N7 satisfied.
2. **Band around an [UNKNOWN] line is honest (R1-Q03).** Width from the shape-only FF cross-check (a range, not a point), ~25-45% a labelled SHARE estimate, implied lower bound disclosed via the LOW-MED tag — honest estimation, not smuggled fabrication.
3. **Breakeven from a SAVING band, never the [UNKNOWN] rate (R1-Q06, R2-Q01).** `7500/(5*800)=1.875>1.0` holds for any jobwork rate whose saving-over-owned-variable is ~$5; owned_variable also [UNKNOWN], so no Rs/piece is smuggled. N2 satisfied.
4. **B2-over-B1 a real qualitative lean (R1-Q05).** B2 deletes 2 of the 4 honest counters; the flip-to-B1 condition (FOB MOQ too high to fund / material-control value) is named — not the "never chose an implementation" error, not an un-tested assertion.
5. **Genuinely falsifiable verdict (R1-Q12, R2-Q10).** Names concrete disproving quote outcomes (high FOB MOQ → B1; high knit+LINK rate → breakeven below 1.0 → own knitting) and correctly notes a LOW rate ENTRENCHES subcontracting; the [UNKNOWN] rate is a gate only until a named cheap quote run.
6. **Duty trio N5-clean, no lane crossing (R1-Q10, R2-Q05).** Cotton ~16.5% (never ~7%), wool ~16%, acrylic-MMF ~32% the outlier; the US ~32% kept OUT of the ex-factory band and routed to T9; the Indian GST inversion (A2 cash-cycle drag, magnitude [UNKNOWN]) cleanly distinguished.
7. **Off-paper / compliant-core stays mechanism (R1-Q11, R2-Q03).** No named-party accusation, magnitude [UNKNOWN], informal-labour saving located inside the [UNKNOWN] job-work rate (no double-count), authority deferred to Ludhiana. N3 satisfied.
8. **Iteration-loop on the speed axis (R1-Q07).** A sampling cell is NOT owning bulk knitting; the utilisation argument reattaches the instant output becomes sellable; machines-lane capacity authority honoured. N4 satisfied.
9. **EPF/ESI on-roll-portion-only, no double-count (R2-Q09).** Near-negligible at a subcontract launch, material only once owned stages graduate.
10. **Decisive-yet-honest split (R2-Q10).** Seven rate-INDEPENDENT verdicts survive any quote (the two NEVERs, dye-lot-MOQ-is-binding, working-capital-trap-is-the-killer, bands-are-a-FLOOR, own-design/buy-upstream/linking-last, ex-factory-is-least-decisive); only band WIDTHS and crossover LOCATIONS are parked, each behind a named cheap quote run.

---

## Honesty audit (N1–N7)

PASS on all. Every money/duty/CAC/capacity claim tagged; the breakeven placeholder and the x1.3-2 uplift both labelled `[ESTIMATE, LOW/LOW-MED]`; the three governing blockers (Ludhiana knit+LINK rate, live blended CAC, forward India duty) **and** the twin FOB price+MOQ blocker held [UNKNOWN] throughout; the breakeven illustration used a SAVING band and never quoted the [UNKNOWN] rate (N2); off-paper / compliant-core strictly structural mechanism with authority deferred to Ludhiana (N3); no build-our-own-machine, no owned dyehouse, iteration-cell held speed-axis-only, AI a multiplier not a substitute (N4); cotton duty ~16.5% with **no ~7% residue anywhere** and the ~32% MMF kept out of the ex-factory band (N5); the four hard gates surfaced where leaned on (N6); viability carries the GATED-conditional-GO posture with "A3 costs more ex-factory" held orthogonal to the A3-hero config (N7).
