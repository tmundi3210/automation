# PROPOSED BACKLOG — Sweater Vertical Self-Loop (T12)

_Consolidated `propose` items across all nine specialists, grouped by specialist, each phrased as a concrete next action (what to measure / research / add). Aggregated verbatim-in-substance from the `feedback.json` files — no fixes invented._

> **Honesty note.** These are proposals, not applied edits. The governing **measurement blockers stay `[UNKNOWN]` until actually measured**: the Ludhiana **knit+LINK per-piece rate (G2)**, the **live blended CAC (G1)**, the **forward India duty (G5)**, and the achieved **PPS pilling grade (ICI ≥3-4 post-wash on our constructions, G4)**. Any backlog item touching these keeps the magnitude `[UNKNOWN]`/`[ESTIMATE]` and adds no invented number. Items marked *propose* (not apply_now) because they need owner judgment, a new rule/procedure, or new research — never auto-apply.

---

## ludhiana

- **F7 — Re-anchor the "edge can never be cost" conclusion off any material-share number.** Rewrite so the conclusion rests on the compliance premium + moat mirror + no-captive-integration (it survives a 45% counterfactual because it never depended on a share threshold). Note the Vardhman FY20 54% `[FACT]` is a single-firm/single-year INTEGRATED-SPINNER figure that anchors DIRECTION only and transfers qualitatively (not as a point) to the non-integrated knit-garment stack, whose shares are `[ESTIMATE]` order-of-magnitude. *Optional research:* an on-the-ground MEASURED knit-garment cost-share read — never invented.
- **F8 — Define "export-legitimacy" in-text** where it is introduced as the headline edge: the compliant-core STANDING that makes this micro-brand a credible US importer/seller (paper-clean EPF/ESI/GST/statutory wages, no off-paper model, no owned dyeing node) — a compliance concept, NOT a RoSCTL/RoDTEP/drawback rebate or a landed-cost number (those route to unit_economics/T9 + DGFT/broker).

## yarn

- **P1 — Scope the A2 fibre-cost read to the anti-pill higher-dpf GRADE acrylic** (not cheapest 1D, which fails the pilling gate); flag the anti-pill-grade premium/delta as a LOW-confidence `[ESTIMATE]`/`[UNKNOWN]` **to harden by a live importer/mandi quote**; keep "yarn price does not govern" as a magnitude claim vs the `[UNKNOWN]` knit+LINK rate (G2). Fold into the F4 cost-band annotation; invent no premium figure.
- **P2 — Add the pilling accept/reject ORDERING:** grade the BINDING gate on the finished-construction **post-wash PPS swatch** (finishing is INSIDE the gate); the bare-yarn/greige swatch is only an indicative early screen; only a structurally-below-floor fibre/twist/ply/dpf choice (which no finishing can lift to 3-4) is an unrescuable yarn-floor reject.
- **P3 — Add a supply-CONTINUITY rule to the greige+job-work-dye graduation:** "never own a dyehouse" neutralises capital/penalty LIABILITY, not supply CONTINUITY; a hero colour on job-work must carry a mitigation (dual-source the dye step / hold hero-shade safety stock / retain a bought-dyed fallback). Regulatory/CETP-NGT risk stays a structural cluster mechanism, never a named-party accusation.
- **P4 — Chain the dye-METHOD axis in decision_procedure step 7** (pairs with P3): the post-wash pilling VETO dominates the dye-method PREFERENCE, so if a hero colour clears the gate ONLY via greige+job-work, the veto overrides the bought-dyed default and the added dye-lot MOQ / shade-consistency / cluster-continuity risk is accepted for that colour.
- **P5 — Add ONE labeled arithmetic illustration** (mirroring the SCOPE_GLOSSARY worked-illustration pattern): a directional 12GG count with the Tex/Ne/Nm pivot (Nm = 1.693×Ne) and one WRONG folded label that silently doubles fabric weight (2/30 vs 1/30), explicitly marked sampling-to-confirm. Invent no vendor count.

## machines

- **F4 — Add a 12GG fine-gauge FIT GATE to the buy-used threshold:** the first owned buy must be a used machine AT the product's target fine gauge (~12GG), which may price at or above the cheap Cixing-class anchor; a coarse-gauge bargain is the wrong machine at any price. **Whether ~$4-15k reliably buys a 12GG-capable used unit is `[UNKNOWN]` pending the landed quote (the #1 input gap).**
- **F5 — Add a founding-machine toolchain-continuity dominance rule:** for the founding, programmer-anchoring machine, live-toolchain continuity DOMINATES the used-price bargain; do not found the line on a discontinued-ecosystem machine (post-2025 Stoll, Obertshausen-only M1plus); a used Stoll is at most a later, diligence-gated capacity-add once a live toolchain (Shima or Chinese) is the house standard.
- **F6 — Add a 3-criterion OPERATIONAL periphery-vs-core test** to the "periphery-only reverse-engineering rule": a part is CORE (NEVER clone; buy OEM) if ANY of (1) it sets/holds knit geometry/registration to needle-pitch tolerance, (2) it touches the yarn path in a way that changes stitch formation, or (3) it embeds vendor firmware/knit-CAD IP; otherwise periphery. Explicit guard: cost/size/"just a cheap spare" is NEVER a reclassifier.
- **F7 — Make linker labour SUPPLY its own capacity/acquisition gate:** an owned line is not staffed until a re-hirable/retainable linker pool is secured; unstable lean-season supply makes the daily-output ceiling itself unstable, so a line that clears cost breakeven can still fail to SHIP. **The linker labour-market read is consumed from ludhiana_sourcing (T1), not assumed away.**
- **F11 — Append the four flip-conditions** under which deleted-linking (whole-garment) beats the panel+linker path, keeping whole-garment micro-economics `[UNKNOWN]`: (1) linker labour scarce/unstable enough that deleting the step removes a binding SUPPLY risk; (2) repeating-program volume high enough to amortise the ~$120k-250k+ new / ~$20k-90k used capital; (3) the slower ~6-15/shift per-garment penalty outweighed by deleting linking throughput + wages + seam-quality variance; (4) product suited to seamless construction.
- **F1 (CAPSTONE — apply only after F4/F5/F6/F7 resolve) — Restate the subcontract-vs-own verdict** as: subcontract into the Ludhiana mesh INDEFINITELY (capital free for design + CAC); own a line ONLY when a CONJUNCTION lands — a fine-gauge (~12GG) landed used-machine quote, a re-hirable/retainable linker pool, a live-toolchain machine, a **measured AI programmer-offset**, and a **real compliant G2 knit+LINK rate** proving a repeating program clears breakeven; NEVER build. Frame buy-used as gate-conjunction-triggered, not a near-term phase.

## mfg_economics

- **M4 — Bind the mandatory micro-volume ×1.3-2 uplift to the ex-factory band as a single hand-off artifact:** the figure handed to T9 for a launch-scale run MUST be the volume-tier-adjusted band carrying its micro-uplift (band midpoint × ~1.3-2 at launch MOQ), never the bare repeat-volume floor.
- **M5 — Add an explicit compliant-core-vs-off-paper structural cost-gap flag:** the entrant runs a compliant core against a partly-off-paper cluster cost base for identical work — a first-order structural cost gap held `[UNKNOWN]`; the informal-labour saving is embedded inside the `[UNKNOWN]` job-work rate (so not a separate line, no double-count). Structural mechanism, Ludhiana-owned, never a named-party accusation.
- **M6 — Split the seasonal caution's two halves:** it names the Ludhiana SUPPLY peak (Jul-Oct) to avoid but does not flag the separate warm-California DEMAND window (short cool season) on a different calendar. Hand the demand-window-timing half to brand_market (T7) / unit_economics (T9) and name the supply-avoid + warm-demand non-alignment as a NAMED, un-sized scheduling squeeze.

## quality_checker

- **P1 — Stage the reject-willingness gate as primarily UPSTREAM:** enforced via supplier-borne PO pass-thresholds + PPS approval; a LANDED-lot rejection still yields an owned quality accept/reject decision, but its economic executability is bounded (write in the upstream-vs-landed distinction).
- **P2 — Add an explicit per-article yarn-acceptance percentile row:** A1/A2 acrylic-blend at ≤25% USTER; **A3 premium tighter-than-25%, exact percentile `[UNKNOWN]`, co-owned with yarn_sourcing and validated on the PPS swatch** (directionally supported but the A3 figure stays `[UNKNOWN]`).

## finishing

- **F4 — Add a per-article blend caveat** (routes on pilling): the four anti-pill routes and A1_COTTON_RECIPE are written on PURE-fibre archetypes but A1 SHIPS as a cotton-blend; directions transfer but magnitudes shift — gas-singe applicability degrades and cellulase yield falls with rising acrylic fraction (thresholds `[UNKNOWN]` until PPS); route acrylic-fraction pilling to the yarn PO. **Requires PPS measurement — do not apply blind.**
- **F5 — Add a fault-isolation procedure for a stacked pilling fail** (A1 = gas-singe AND cellulase): isolate with a singed-yarn-only and a bio-polished-garment swatch re-test plus a both-ends retained-strength/hairiness read so the delta localizes yarn-floor vs under/over-dosed finish.
- **F6 — Add an in-run-control caveat for outsourced milling:** the felt/area-shrink gate is POST-HOC (rejects already-dead wool); no real-time brand control, so protection reduces to (a) vendor felt-control qualification, (b) PPS/first-piece setpoint dial-in, (c) post-hoc reject; **price the over-mill risk into A3 working capital.**
- **F7 — Require the buyer-approved reference / PPS sample to be POST-WASH-graded:** approve on the washed hand, not the showroom hand, so the sign-off reference and the acceptance gate share the same post-wash condition and rejection authority is contractual (otherwise a lot matching a freshly-softened masked reference can pass sign-off while failing post-wash).

## brand_market

- **P1 — Attach an auditable worked method/basis/confidence line to the three CAC endpoints:** the ~$35 blended survival THRESHOLD, the ~$20-30 / ~$22 organic-heavy OPERATING estimate, and the ~$45-75 cold-paid CAC that fails a sub-$90 order. All three are already `[ESTIMATE]`-tagged and correctly ORDERED and none hardens the **live blended CAC `[UNKNOWN]` (G1)** — the ask is only an inline derivation, not a hardened number.

## design

- **P1 (non-binding) — Name the single end-to-end resolving trial in-spec:** one controlled trial (one real render → T3 programmer-authored program → physical PPS swatch → compare an AI-generated program vs the human's on gauge + integer stitch grade + post-wash pilling grade on our actual acrylic/cotton/merino fabric); until it passes, **AI-design→knit-program autonomy stays `[UNKNOWN]` and AI is a multiplier not a substitute.**
- **P2 (non-binding) — Cross-link the `[UNKNOWN]` live Central-Valley-youth micro-trend to DECISION_MEMO gate G8** so the ship-gate mapping (a MEASURED hype read as a pre-launch ship-gate) is explicit; keep the listening-loop + pop-up-recon + own-channel-sell-through close. **The live trend signal stays `[UNKNOWN]` until measured.**

## unit_economics

- No `propose` items. The specialist ran a single fully-clean (`dry`) round with all 11 answers judged `ok` and emitted no feedback.json — the honest signal that nothing needed proposing. Its governing blockers (G1/G2/forward India duty) and both binding legal flags were held correctly.

---
_Measurement blockers recap — stay `[UNKNOWN]` until actually measured: knit+LINK per-piece rate (G2), live blended CAC (G1), forward India duty (G5), PPS pilling grade (G4). Do not invent fixes; harden these only with a real quote / audit / PPS swatch._
