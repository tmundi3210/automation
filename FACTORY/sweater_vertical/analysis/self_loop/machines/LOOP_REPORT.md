# SELF-IMPROVEMENT LOOP REPORT — `machines` (knit_machine_strategist)

**Segment T12 · slug=machines · rounds run: 2 of 2 (max) · R4 synthesizer merge**

---

## Health verdict (one line)

**NOT-CONVERGED — health strong on the qualitative spine (build-NEVER, AI-as-multiplier, 8447≠6110 duty discipline, off-paper-as-mechanism all verified clean), but the buy-used ladder step is a structural over-reach: 7 material (med) findings survive at the round cap, 0 high — the specialist is high-integrity (findings are honest self-concessions, not defended false claims), yet it advertises owning iron sooner than its own unresolved gates allow.**

Converged: **false** (material med findings remain at max_rounds → flagged NOT-CONVERGED per loop stop_conditions, not silently truncated). Health score: **NOT-CONVERGED**.

---

## Round-by-round story

**Round 1 (dry=false; 6 med, 3 low, 2 ok).** The questioner attacked the marquee joints. The specialist answered itself honestly and largely self-convicted:
- **Strengths confirmed:** the 8447 machine-import stack is kept categorically separate from the 6110 garment duty with no ~7% leak (Q2, the cleanest answer); the buy-vs-subcontract breakeven is declared *structurally undecidable* until the G2 knit+LINK rate is measured rather than fabricated (Q4); off-paper handled as mechanism, routed, magnitude [UNKNOWN] (Q9).
- **6 med findings**, all the same family — *load-bearing numbers/claims narrated with more certainty than the spec text delivers*: the undecomposed `$2M-10M+` build magnitude (Q1); throughput bands narrated as *proof* of a 1:1 linker bottleneck they don't prove (Q3); the "capacity plan with counts" only producible as a structure-only sketch (Q10); the year-old Stoll [FACT] whose downstream "pool deepens / parts flow" is narrated as fixed (Q11); the subcontract default silent on its off-paper entanglement (Q9). Two needed synthesis not a relabel and were held as **propose**: the used-Stoll toolchain-lock vs programmer-hinge (Q6-r1) and the non-operational periphery-vs-core rule (Q7-r1).

**Round 2 (dry=false; 7 med, 2 low, 1 ok → NOT-CONVERGED at the cap).** The round-2 questioner re-attacked the two surviving propose items at their hardest edge and opened fresh un-probed joints. The specialist again mostly conceded real, un-adjudicated joints:
- The two round-1 propose items **remain open** (Q2-r2 toolchain rule still unsynthesised; Q3-r2 periphery test still non-operational).
- **Fresh findings:** the buy-used first-buy is priced at the cheap Cixing tier with no 12GG fine-gauge fit gate (Q1-r2); the spec's own `$20k-90k` used whole-garment band **breaches** the `sub-$20k` anchor the build-NEVER comparison quotes (Q4-r2); linker **labour supply** (T1's hardest-to-re-hire role) is never made its own constraint (Q5-r2); the AI programmer-offset that makes the bottleneck "winnable" is [UNKNOWN] in magnitude yet quietly load-bearing (Q6-r2); whole-garment's escape-hatch [UNKNOWN] needs published flip-conditions (Q7-r2); a round-1-fix **residue** survives verbatim at sibling addresses ("prove it" at line 179, "count named" at lines 41/139) (Q8-r2); the 2025-anchored stack needs a mid-2026 currency gate (Q9-r2); and the merged verdict advertises buy-used as a near-term phase when the honest gate-conjunction pushes ownership much further out (Q11-r2).
- **Honesty gate N1–N7: PASS on all 11** (verified by the round-2 judge): tags_ok on every answer, no fabricated named-builder figure, off-paper stays mechanism, AI stays a multiplier, cotton garment duty stays ~16.5% and never conflated with the machine 8447 stack, governing blockers stay [UNKNOWN], viability carries the GATED posture.

**Why not converged:** round 2 is the max round and still surfaces material findings. The un-converged core is a single structural theme — *the buy-used step of the ladder is advertised as a reachable near-term Phase 1, but the honest conjunction of the specialist's own unresolved gates (gauge fit + linker labour supply + live toolchain + measured AI offset + landed quote + compliant G2 rate) keeps ownership out of reach.* Five joints (Q1, Q2, Q3, Q5, Q6) plus the capstone (Q11) need synthesis/measurement/judgment and cannot be mechanically closed.

---

## Findings table (deduped, merged across rounds, ranked by severity then leverage)

| ref | type | severity | evidence (spec location) | fix | disposition |
|-----|------|----------|--------------------------|-----|-------------|
| F1 (r2-Q11) | overclaim | med | `capabilities[5]` merged verdict + `decision_procedure` 4-7 vs DECISION_MEMO G2/GATED posture | restate ladder: "subcontract indefinitely; own only when the gate-conjunction lands; never build" — capstone once F4/F5/F6/F7 resolve | **propose** |
| F2 (r2-Q4) | contradiction | med | `conflicts_and_dominance[1]`, `escalation_triggers[2]`, `decision_procedure` step 6 ("sub-$20k used") vs glossary whole-garment "~$20k-90k used" | restate build-NEVER as "orders of magnitude below build cost **regardless of machine class**" (cite class range, not the single sub-$20k anchor); change no dollar band | **apply_now** |
| F3 (r2-Q8; r1-Q1/Q3/Q10) | overclaim (residue) | med | verified residue: `competency_questions_covered` item 2 line 179 "PROVE it"; `capabilities[1].outputs` line 41 + `validation_checklist` line 139 "count named" | propagate round-1 softening: "prove"→"indicate"; "count named"→"that must be secured"; re-grep to confirm zero load-bearing residue | **apply_now** |
| F4 (r2-Q1) | gap | med | `conflicts_and_dominance[4]` + glossary "buy-used threshold" (Cixing ~$4-15k) vs `capabilities[0]` 12GG product; "gauge" only a per-unit check | add 12GG fine-gauge fit gate to buy-used threshold; hold whether ~$4-15k buys a 12GG-capable unit as [UNKNOWN] (invent no fine-gauge price) | **propose** |
| F5 (r2-Q2; r1-Q6) | gap | med | `conflicts_and_dominance[0]`+`[4]` + glossary programmer/Stoll-exit vs `escalation_triggers[5]` (ex-Stoll parts DEFER only) | add dominance rule: for the founding programmer-anchoring machine, live-toolchain continuity **dominates** the used-price bargain; used-Stoll only a later diligence-gated capacity-add | **propose** |
| F6 (r2-Q3; r1-Q7) | gap | med | glossary "periphery-only reverse-engineering rule" + "core kinematic chain" + `conflicts_and_dominance[6]` + `boundaries[2]` (two lists, no test) | add 3-criterion operational test (sets/holds knit-geometry-to-pitch \| touches yarn path affecting stitch \| embeds firmware/CAD IP ⇒ CORE); guard: cost/"just a spare" never reclassifies | **propose** |
| F7 (r2-Q5) | gap | med | glossary "linker bottleneck" ("hardest to re-hire") + `conflicts_and_dominance[0]` + `capabilities[1]` + `validation_checklist` (seats counted, hands never secured) | make linker labour **supply** its own capacity/acquisition gate; consume the linker labour-market read from ludhiana_sourcing (T1) | **propose** |
| F8 (r1-Q1) | overclaim | med→low | `conflicts_and_dominance[1]` + `capabilities[5].method` (`$2M-10M+` [ESTIMATE, HIGH on direction]) — no component build-up in spec/KB | retag magnitude "order-of-magnitude / direction-only, not a decomposed engineering estimate"; NEVER rests on kinematic-chain wall + Groz-Beckert + software-only facts (folds into F2 framing) | **apply_now** (via F2) |
| F9 (r2-Q9; r1-Q11) | stale | low | glossary "HSN 8447 import stack" ([UNKNOWN] on 2025-26) + "Stoll flat-knit exit (2025)" + `boundaries[1]`; current date 2026-07-01 | add dated currency gate: re-confirm live BCD/IGST and current used-pool/parts state as of buy date before any 2026+ capex; keep 8447 strictly separate from 6110 | **apply_now** |
| F10 (r2-Q6; r1-Q5) | overclaim | low | `capabilities[5].method` ("highest-value... offsets the scarce programmer") + glossary "winnable" vs `boundaries[3]` autonomy [UNKNOWN] | attribute "winnable" to the human skill's **acquirability**; label AI offset [ESTIMATE, LOW]-direction / [UNKNOWN]-magnitude; no capacity/acquisition number may lean on it until T8 measures | **watch** |
| F11 (r2-Q7) | gap | low | `conflicts_and_dominance[7]` + glossary "whole-garment no-linking trade" (bare [UNKNOWN]-and-later) | keep micro-economics [UNKNOWN]; publish 4 flip-conditions (scarce/unstable linker labour \| amortising volume \| knit-time penalty outweighed \| seamless-suited product) | **propose** |
| F12 (r2-Q10; r1-Q9) | ok / visibility | none | glossary "subcontract-into-the-mesh default" + `decision_procedure` step 4; N3 clean | optional one-liner: Phase-0 default sized on a **compliant** G2 rate; off-paper layer = mechanism/[UNKNOWN]/routed-to-ludhiana, never a claimable edge | **watch** |

Merge notes: r1-Q1 (F8) folds into the F2 apply_now (same order-of-magnitude reframing). r1-Q3/Q10 overclaims were round-1 apply_now relabels whose **residue** is now F3. r1-Q5→F10, r1-Q7→F6, r1-Q6→F5, r1-Q9→F12, r1-Q11→F9 (same joints, strongest round-2 evidence kept). r1-Q4 (breakeven-undecidable) and r1-Q2 / r2-Q10 (8447≠6110, off-paper) resolved OK and recorded as strengths.

---

## apply_now (3 — high-confidence, in-scope, mechanical; still pass build.sh)

1. **F2 — Restate the build-NEVER comparison as "orders of magnitude below build cost regardless of machine class."** At `conflicts_and_dominance[1]`, `escalation_triggers[2]`, and `decision_procedure` step 6, cite the class range (`sub-$20k used Cixing panel` / `$20k-90k used` / `$120k-250k+ new whole-garment`, all vs `~$2M-10M+` build) instead of the single `sub-$20k` anchor breached by the spec's own whole-garment band. Retag `$2M-10M+` as an order-of-magnitude directional prior (folds in F8). **Change no dollar band.** Strengthens, not weakens, the flagship conclusion.
2. **F3 — Propagate the round-1 softening to residual sibling addresses.** Line 179 `competency_questions_covered` item 2 "how do the throughput bands **PROVE** it" → "how the throughput bands **indicate** that linking is the ceiling"; line 41 `capabilities[1].outputs` and line 139 `validation_checklist` "the matching linker/programmer **count named**" → "the matching linker/programmer **that must be secured** alongside the machine." Re-grep for `prove` / `count named` to confirm zero load-bearing residue.
3. **F9 — Add a dated currency gate to the 8447 stack.** In glossary "HSN 8447 import stack" / `capabilities[3]`: "the 8447 BCD/IGST stack and the Stoll-exit used-pool/parts read are 2025-anchored; re-confirm live BCD/IGST and the current used-pool/parts state as of the buy date before any 2026+ capex relies on them." Keep the 8447 machine stack explicitly separate from the 6110 garment duty. Invents no rate; changes no figure.

## propose (6 — real improvements needing synthesis/measurement/judgment → PROPOSED_BACKLOG.md)

1. **F4 (Q1)** — add a 12GG fine-gauge fit gate to the buy-used threshold; hold whether ~$4-15k buys a 12GG-capable used unit as [UNKNOWN].
2. **F5 (Q2)** — add a founding-machine dominance rule: live-toolchain continuity dominates the used-price bargain; do not found on a post-2025-Stoll dead-ecosystem machine (used-Stoll only a later diligence-gated capacity-add). *[surviving round-1 item]*
3. **F6 (Q3)** — add the 3-criterion operational periphery-vs-core test (geometry-to-pitch \| yarn-path-effect \| firmware/CAD-IP ⇒ CORE), with cost never a reclassifier. *[surviving round-1 item]*
4. **F7 (Q5)** — make linker labour SUPPLY its own capacity/acquisition gate; consume the linker labour-market read from ludhiana_sourcing (T1).
5. **F11 (Q7)** — keep whole-garment micro-economics [UNKNOWN] but publish the four flip-conditions that would beat the panel+linker path.
6. **F1 (Q11) — CAPSTONE** — restate the merged verdict as "subcontract indefinitely; own only when the gate-conjunction lands; never build." Apply *after* F4/F5/F6/F7 resolve, since it aggregates them.

## watch (2 — low-severity / already-mitigated; recorded so not rediscovered)

1. **F10 (Q6)** — attribute programmer-bottleneck "winnable" to the human skill's acquirability; keep the AI offset an [ESTIMATE, LOW]-direction / [UNKNOWN]-magnitude multiplier no number leans on until T8 measures it. Already largely mitigated by the existing [UNKNOWN]-autonomy / multiplier framing.
2. **F12 (Q10)** — optional visibility one-liner (round-1 Q9): the Phase-0 default is sized on a compliant G2 rate; the off-paper layer is mechanism / [UNKNOWN] / routed-to-ludhiana, never a claimable edge. The answer itself is fully supported and needs no correction.

## Verified STRENGTHS (the clean 'ok' answers — report is balanced, not just a defect list)

- **8447 ≠ 6110 duty discipline (r1-Q2, r2-Q9): PASS, cleanest in the set.** The machine-into-India HSN-8447 stack (~7.5% BCD [ESTIMATE] + ~0.75% surcharge + 18% IGST [FACT], classification [FACT], 2025-26 changes [UNKNOWN]) is kept categorically distinct from the garment-into-US heading-6110 trio (cotton ~16.5% / wool ~16% / acrylic-MMF ~32%). **No residual ~7% cotton figure; no 6110 rate leaked into the machine math.** N5 clean.
- **Breakeven honesty (r1-Q4): PASS, honesty-exemplary.** The buy-vs-subcontract breakeven is declared *structurally undecidable* until the G2 knit+LINK per-piece rate is measured — the specialist refuses to invent a `jobwork_unit_rate` to make the formula compute. Governing blocker held [UNKNOWN], "decided around, never invented."
- **Off-paper as mechanism (r1-Q9, r2-Q10): PASS.** The subcontract-default dependency is framed strictly as structural mechanism (fragmented cash-heavy micro-trade, informal cash job-work, thekedar labour, patchy enforcement), names no party, holds magnitude [UNKNOWN], routes the mechanism to ludhiana_sourcing and the compliant rate to mfg_economics (G2). N3 + N2 clean.
- **Build = highest-confidence NEVER: held throughout.** Rests on the core-kinematic-chain wall + Groz-Beckert-makes-the-needles-even-for-Shima/Stoll [FACT] + serious-open-research-is-software/compiler-only [FACT], corroborated by the DECISION_MEMO. The dollar magnitude is the only weak spot (direction-only), and the conclusion survives on order-of-magnitude separation robust to band width.
- **AI kept a multiplier, autonomy [UNKNOWN]: held.** design→program assist is a human-in-the-loop draft-and-correct multiplier on the scarce programmer; end-to-end autonomy on our acrylic/blend fabrics stays [UNKNOWN], routed to T8. N4 clean (residual is phrasing risk only, F10 watch).
- **Honesty gate N1–N7: PASS on all 11 round-2 answers** (tags_ok verified true throughout both rounds).

---

## Re-gate / verification note (for the finalizer)

The 3 apply_now edits are pure string/framing edits that invent no number, keep every governing blocker [UNKNOWN], preserve the off-paper mechanism and the 8447/6110 separation, and are expected to stay ALL GREEN under `bash FACTORY/build.sh FACTORY/sweater_vertical/specialists/machines`. After applying, **re-grep for residue**: `prove` and `count named` (F3), any `~7%` cotton figure (must be zero), and confirm the whole-garment band framing (F2). The 6 propose items go to `PROPOSED_BACKLOG.md` — do NOT silently apply them (F5/F6 need a new dominance rule / operational test; F1/F4/F7 need judgment; F1 is the capstone). Because a med finding remains open at the round cap, the specialist stays **NOT-CONVERGED** until a follow-up pass (or owner action on the backlog) closes the buy-used-ladder joints.
